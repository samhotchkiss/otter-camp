# Spec Issues

Issues found during spec review that must be resolved before building starts.
Each issue must be resolved by Sam before Codex picks up the affected task.

| # | Severity | Affected Docs | Issue | Status |
|---|----------|--------------|-------|--------|
| 1 | BLOCKER | 05, 14 | Doc 05 schema defines `budget_cap_cents` (integer, per-agent cost cap in cents) on the `agent` table. Doc 14 resolved question #24 explicitly states: "rename `budget_cap_cents` to `budget_cap_tokens`" and specifies that all three budget levels (org, project, agent) use tokens as the unit, with dollar estimates shown display-only in the UI. The doc 05 schema was not updated to reflect this resolution. The column name and type need to be decided and doc 05 updated before the agent table DDL can be finalized. | **RESOLVED: `budget_cap_tokens` (int). Budget caps are always tokens. Dollar estimates are display-only. Doc 05 DDL updated.** |
| 2 | AMBIGUOUS | 04, 14 | Doc 04 describes a 7-step bootstrap sequence (steps 1–7: migrations → org → first user → starter trio → provider connections → org session → audit event). Doc 14 resolved question #7 explicitly states "doc 04's bootstrap sequence is stale — doc 14's 10-step sequence is authoritative." The 10-step sequence in doc 14 adds: create org-level skills repo + default skills (step 4), seed model profiles (step 5), seed default flow templates (step 6) — all before seeding starter trio (step 7). Doc 04's text has not been updated to acknowledge this and still presents its own 7-step sequence as current. A developer reading doc 04 alone will implement the wrong sequence. Doc 04 should be updated to defer to doc 14 or include the correct 10-step sequence. | Open |
| 3 | GAP | 03 | `inbox_item.item_type` includes `'browser_handoff'` as a valid value in the schema check constraint in doc 03. However, neither doc 03 nor any of the other 6 docs read in this iteration define what a `browser_handoff` inbox item is, when it is created, what actions are available on it, or what its `action_payload` looks like. Likely defined in doc 11 (System Integration — CLI and Browser). Must be confirmed when doc 11 is read. If doc 11 does not define it, this is a gap that needs to be filled before `inbox_item` can be implemented. | Open |
| 4 | AMBIGUOUS | 05, 14 | Doc 05 states: "Private memory: Default for staff PMs, Frank, Lori, and Ellie: true. Default for all other staff and all temps: false." Doc 14 resolved question #8 addresses a prior inconsistency about Lori's private memory and confirms "doc 05 is authoritative — all staff agents (including Lori) have private memory enabled." The current doc 05 text already includes Lori in the true set, so these are consistent. However, the doc 14 bootstrap dataset section says under Lori: "Private memory: enabled (default for all staff — doc 05)" which appears to generalize to ALL staff, not just PMs, Frank, Lori, and Ellie. This wording conflicts with the doc 05 body text which explicitly limits private_memory=true to those four roles. Clarify whether all staff agents get private_memory=true or only the named four. | Open |
| 5 | AMBIGUOUS | 05 | The `agent.lifecycle_status` column combines staff and temp state machines into one column with values: draft, active, paused, retired, cancelled, expired, promoted. The check constraint allows all 7 values but the spec text says staff agents use draft/active/paused/retired/cancelled and temp agents use active/expired/promoted. There is no DB-level constraint preventing a staff agent from reaching 'expired' or a temp agent from reaching 'draft' — only application-layer enforcement is implied. This should either have a check constraint referencing `agent_class` or be explicitly acknowledged as application-layer-only enforcement to avoid ambiguity for implementers. | Open |
| 6 | GAP | 03 | The `flow_node` table references a `flow_node_skill` join table ("Skills are linked via flow_node_skill join table (see doc 10)") but this table is not defined in docs 01–05 or 14. It must be defined in doc 10 (Skills Integration). Flag for confirmation when reading doc 10. If doc 10 does not define the `flow_node_skill` table with columns, this is a gap. | RESOLVED by doc 10: `flow_node_skill` fully defined with columns (id, flow_node_id, skill_id, position, created_at). See ISSUE #16 for the related flow_node schema conflict. |
| 7 | AMBIGUOUS | 03, 03a | The `merge_queue_entry.archived_at` field is described in two slightly different contexts: doc 03 says "archived_at on merge_queue_entry" (set on merge completion) and doc 03a says "archived_at set when the deploy including this entry completes successfully." These are different triggers — one is merge completion, the other is deploy completion. Doc 03a's resolved decision #12 and the DDL comment in doc 03 both reference deploy completion. However, doc 03's prose description says "archived_at on merge_queue_entry" without clearly tying it to deploy completion. The authoritative rule (from doc 03a) is: rows are never hard-deleted, archived_at is set when the deploy including the entry completes. Confirm this is the single correct definition and update doc 03's prose to be unambiguous. | Open |
| 8 | GAP | 05 | The `agent` table has `temp_scope_id` which is described as "the project, task, or session this temp is scoped to" — yet temp_scope_type can also be 'ttl'. When `temp_scope_type = 'ttl'`, what does `temp_scope_id` point to? The spec implies it would be the project the temp belongs to (since all temps are always project-assigned), but this is not explicitly stated. The spec says TTL-scoped temps are "scoped to a time duration" and uses `temp_ttl_seconds` + `temp_expires_at` for the duration. Clarify whether temp_scope_id is always required (pointing to the associated project for ttl-scoped temps) or is nullable for ttl-scoped temps. | Open |
| 9 | GAP | 06 | `memory_source` has a `session_id uuid` column described as "chat session where this was captured" and is used to link memories to their originating session. The DDL does not define a foreign key constraint `session_id REFERENCES chat_session(id)` — it is a plain `uuid` column. The spec says "provenance enforcement: every active memory must have at least one memory_source record" but doesn't address whether session_id integrity is DB-enforced. This is partially application-layer provenance only. Clarify whether a FK should be added (and with what ON DELETE behavior), or explicitly acknowledge that session_id is a soft reference (allowing records to survive session deletion). | Open |
| 10 | GAP | 06 | `memory_source.source_id` is polymorphic: when `source_type = 'chat_message'` it references `chat_message.id`, but when `source_type = 'event'` or `source_type = 'file'`, there is no corresponding database table — events are bus events (no event_log table defined in docs 01–06) and files are in object storage / git repos. The DDL makes source_id a nullable uuid with no FK constraint. This means provenance for event-sourced and file-sourced memories has no DB-level referential integrity at all. Clarify: (a) does an event log table exist somewhere (possibly doc 12 or 16) that event source_ids reference? (b) for file sources, is source_id the memory_import.id, or something else? | Open |
| 11 | GAP | 07, 14 | Doc 07 states: "If no profile is found anywhere in the hierarchy [flow_node > agent > project > org], the request fails with a clear error. There is no hidden default — the org must configure at least an org-level default profile." Doc 14 bootstrap step 5 says "seed model profiles (high-capability, standard, haiku)" but does not explicitly say whether org-level `model_profile_assignment` rows are also created during bootstrap. If the bootstrap seeds profile rows but does NOT create org-level assignments, every fresh org would have zero assignments and every model invocation would immediately fail. Clarify: (a) does bootstrap step 5 (or a later step) create the org-level `model_profile_assignment` rows for the standard/haiku/high-capability profiles? (b) if system-provided profiles (organization_id=null) are supposed to be universally available without an org assignment, that contradicts the spec's "no hidden default" rule. One of these must be the authoritative behavior. | RESOLVED by doc 08: Provider Connection Bootstrap step 5 explicitly creates the org-level model_profile_assignment row pointing to the primary default profile. Bootstrap creates both the profile rows (from doc 14 step 5) and the org-level assignment (from doc 08 bootstrap step 5). |
| 12 | GAP | 07, 16 | `model_invocation` has a `run_attempt_id uuid references run_attempt(id)` column, introducing a `run_attempt` entity as a FK target. No `run_attempt` table has been defined in any doc read through iteration 2. This is a forward reference to doc 16. Flag for confirmation when reading doc 16: if doc 16 does not define `run_attempt`, this is a gap that must be resolved. | RESOLVED by doc 16: `run_attempt` fully defined with columns (id, run_step_id, attempt_number, trigger, status, failure_reason, failure_class, output, output_summary, worker_type, worker_id, input_tokens, output_tokens, started_at, completed_at, duration_ms, metadata). |
| 13 | GAP | 08, 12 | Doc 08 references a `domain_event` table as the durable backing store for the PostgreSQL-based event bus: "domain events are written to a durable domain_event table (see 12-api-events-and-realtime.md)." This table is not defined in any doc read through iteration 2 (docs 01–08, 14). It is a forward reference to doc 12. Flag for confirmation: doc 12 must define this table with its schema. If it does not, this is a gap. | Open |
| 14 | GAP | 09, 16 | `mcp_execution_log` has a `tool_execution_id uuid references tool_execution(id)` FK. The `tool_execution` table is not defined in any doc read through docs 01–09. This is a forward reference to doc 16. Flag for confirmation: doc 16 must define `tool_execution` with the expected schema. If it does not, this is a gap. (Note: this also applies to the `run` table referenced in the same log entry.) | RESOLVED by doc 16: `tool_execution` fully defined with columns (id, run_id, run_step_id, run_attempt_id, tool_name, tool_tier, tool_domain, capability, policy_decision, input, output, status, error_message, started_at, completed_at, duration_ms, metadata). `run` also fully defined. |
| 15 | AMBIGUOUS | 09 | Doc 09 states "resource reads are tier 1 (chat-layer) governed by a basic scope check — does the agent have access to this connection's project?" But the exact enforcement mechanism for this check is not specified: Is it a check against `agent_project_assignment` (verifying the agent is assigned to the connection's project)? Does it use the same `mcp.connection.use` capability check as tier 2 tools but at a lower enforcement level? Or is it a simple `project_id` null check (any agent in the org can read org-scoped connection resources)? Implementers need to know what query/check defines "has access to this connection's project" for resource reads without capability grants. | Open |
| 16 | BLOCKER | 03, 09, 10 | Three docs create an inconsistent picture of the `flow_node` table's columns related to skills and MCP tools. Doc 03 defines `flow_node` with a `skills jsonb` column. Doc 09 says to ADD a `mcp_tools jsonb` column to `flow_node` (currently absent from doc 03 DDL). Doc 10 says to DROP the `skills jsonb` column from `flow_node` in favor of the `flow_node_skill` join table. The result is that the definitive `flow_node` schema is scattered across three docs with contradictory instructions. The authoritative flow_node DDL must be established: (a) drop `skills jsonb`; (b) add `mcp_tools jsonb`; (c) keep all other columns from doc 03. Doc 03 must be updated with the final schema. | **RESOLVED: Doc 03 DDL updated. Final schema: no `skills jsonb`, has `mcp_tools jsonb` and `tool_domains jsonb` (see also ISSUE #25). All task file callouts updated.** |
| 17 | AMBIGUOUS | 16 | Doc 16 defines `capability_policy.policy_layer` with an 'instance' value (indicating instance-level safety policy rules are stored as rows in the `capability_policy` table) but also states "Instance safety rules are compiled into the broker at startup and refreshed on deploy" and "Hardcoded. Cannot be overridden by any lower layer." This raises two questions: (a) can instance-level rows be created/modified via the POST /control/policies API, and if so, who can do this (only system bootstrap creates them)? (b) if the broker compiles them at startup from the DB, what prevents an org admin from inserting a row with policy_layer='instance' that overrides a safety rule? The spec says they cannot be overridden but provides no DDL constraint or access control mechanism to enforce this. Clarify: are instance policy rows write-protected at the API level or only by convention? | Open |
| 18 | AMBIGUOUS | 07, 16 | Doc 16 states "model invocations are tracked in the model_invocation table defined in doc 07. That table carries run_id, run_step_id, and run_attempt_id FK columns." Doc 07's DDL for model_invocation includes these three FK columns as nullable `uuid references run(id)`, `uuid references run_step(id)`, `uuid references run_attempt(id)`. However, a model invocation can occur OUTSIDE of a control plane run (e.g., during a turn in a sync session where the agent is just chatting, not executing a tier 2 action). Doc 16 says "a model invocation can belong to a run (control plane action), a chat turn (conversation), or both." This means run_id is nullable by design. Both docs agree on this. However, the doc 16 token aggregation section says "per RunAttempt: sum of all model invocations in the attempt." The aggregation query `WHERE run_attempt_id = $1` would only work if model invocations triggered during a run have run_attempt_id set. Clarify: when a model call happens as part of the agent's turn loop (not as a worker action), does it get a run_attempt_id assigned? Or is run_attempt_id only set for worker-domain model calls (e.g., vision model analyzing a screenshot)? | Open |
| 27 | AMBIGUOUS | 12, 21 | Doc 21 E2E test examples use paths `POST /api/sessions/{id}/turns` and `GET /api/projects`, while doc 12 defines all routes under the `/v1/` prefix (e.g., `POST /v1/chat-sessions/:id/messages`). Additionally, the resource name differs: doc 21 says `/sessions/` while doc 12 says `/chat-sessions/`. Also, doc 21 sends a message via `POST /api/sessions/{id}/turns` while doc 12 sends messages via `POST /v1/chat-sessions/:id/messages` (different path structure). Clarify: are doc 21's test code examples pseudocode/illustrative, or is there an `/api/` prefix variant of the API? Doc 12's `/v1/` routes are authoritative for implementation. | Open |
| 26 | BLOCKER | 11, 20 | Doc 20 defines `browser.evaluate` as a native browser tool that "Execute[s] JavaScript in the page context." Doc 11 (System Integration — Browser) defines the complete browser action API and does NOT include any JavaScript execution tool. Doc 11's complete action list is: navigate, click, type, select, hover, scroll, press_key, screenshot, extract_text, extract_structured, get_page_info, wait_for, wait_for_navigation, back, forward, refresh. JavaScript execution is notably absent from doc 11. These two specs directly contradict each other: doc 20 says browser.evaluate exists, doc 11 does not define it and does not include it in the browser action model. This must be resolved before the browser tool catalog can be finalized. | Open |
| 25 | GAP | 03, 09, 10, 20 | Doc 20 introduces `flow_node.tool_domains` as a new optional jsonb field used for stage 3 soft tool deprioritization. This field is not mentioned in doc 03 (flow_node DDL), doc 09 (which adds mcp_tools jsonb), or doc 10 (which removes skills jsonb). The definitive flow_node schema now requires THREE cross-doc changes: (a) DROP skills jsonb (doc 10), (b) ADD mcp_tools jsonb (doc 09), (c) ADD tool_domains jsonb (doc 20). Doc 03's flow_node DDL is now out of date on all three counts. Updating ISSUE #16 to include tool_domains as a third required change to the flow_node schema. | **RESOLVED with ISSUE #16. Doc 03 DDL updated with all three changes.** |
| 24 | GAP | 14, 15 | Doc 14 partially resolved the question of starter trio profile updates on upgrade: "system_prompt updated on startup; operator_instructions field never overwritten." But doc 15 still lists this as an open question and says the options (migration script, version check, CLI command) are deferred. The doc 14 resolution only addresses system_prompt — it does not address tool policy updates, model policy assignment updates, or skill attachment updates to the starter trio on version upgrades. Implementers need a complete spec for the upgrade path before they can safely deploy subsequent versions without overwriting operator customizations. | Open |
| 23 | BLOCKER | 05, 13, 16 | Doc 13 open question #1 explicitly flags that the agent-level budget system (doc 05 `budget_cap_cents`/`budget_period` columns) conflicts with the org/project-level `token_budget` table defined in doc 13 — both units (cents vs tokens) and the interaction between the three budget levels (org, project, agent) are unspecified. Doc 16 describes budget enforcement in the control plane broker that checks "applicable budgets" but does not specify how per-agent, per-project, and per-org limits are combined (additive? most-restrictive wins? hierarchical?). This BLOCKER must be resolved before the budget enforcement path in the control plane can be implemented. | Open |
| 22 | AMBIGUOUS | 08, 13 | Doc 08 defines health endpoints as `GET /health` (liveness, no dependency checks) and `GET /ready` (readiness, checks DB/pgvector/object storage/migrations). Doc 13 refers to these as `/health/live` and `/health/ready`. These are different paths. Implementers need the canonical paths. One of the two specs must be updated. | Open |
| 21 | BLOCKER | 13, 14 | Doc 13 Retention table states: "Model invocation logs: 30 days." Doc 14 resolved question on retention states: "model invocations: 90 days (rollups forever)." These are direct contradictions — 30 days vs 90 days for model_invocation retention. Doc 13 is authoritative for the security/retention spec; doc 14 is authoritative for resolved decisions. One must supersede the other. Since doc 14 resolutions are meant to be the final word, 90 days is likely correct, but doc 13 must be updated. | **RESOLVED: 90 days. Doc 13 updated (4 instances). All task callouts updated.** |
| 20 | AMBIGUOUS | 04, 12, 16 | Doc 12 defines `domain_event.actor_type` as ('human','agent','system','supervisor'). Doc 16 defines `run_event.actor_type` also including 'supervisor'. The canonical principal convention from doc 04 defines only 3 types: 'human','agent','system'. Doc 12 explicitly notes the 'supervisor' extension and says "code that switches on actor_type must handle all four values." However, the `audit_event` table (doc 04) has `principal_type check (principal_type in ('human','agent','system'))` — no 'supervisor'. This means supervisor-initiated actions (stuck run recovery, timeout cancellation) would generate domain_event and run_event rows with actor_type='supervisor', but cannot be logged to audit_event. Clarify: (a) should audit_event.principal_type be updated to allow 'supervisor', or (b) are supervisor-initiated actions intentionally excluded from audit_event (tracked only via domain_event and run_event)? | Open |
| 19 | AMBIGUOUS | 11, 16 | Doc 11's `cli_execution` DDL defines `run_step_id uuid not null` (NOT NULL). However, doc 16's execution model allows tool_execution records where run_step_id is nullable (tool_execution.run_step_id nullable). The `cli_execution` table is a domain-specific extension of the control plane's run/run_step framework — if every cli_execution must have a non-null run_step_id, that means the broker always creates a RunStep before dispatching CLI commands. This is likely correct (each tool call is one RunStep) but the NOT NULL constraint on cli_execution creates an implicit requirement that the control plane always creates RunStep before cli_execution. Clarify whether this is an intentional design constraint (all CLI calls always have a RunStep) or whether run_step_id should be nullable to match the browser_action table (which also has run_step_id NOT NULL — same constraint). If all domain-specific tool records always have a run_step_id, doc 16 should explicitly state this as a constraint on the broker's execution flow. | Open |
| 28 | GAP | 02, 19 | Doc 19 states that push notification preferences (per urgency tier, per project, quiet hours, per event type) are stored server-side and synced between mobile and web UI. No doc defines where these preferences are stored. Doc 02's notification system covers in-app notification filtering, but doc 19's push preferences are a superset: push enable/disable per urgency tier, per-project push overrides, and quiet hours are not part of doc 02's notification_preference structure (if one exists). The mobile app requires server-side storage for these to sync across devices. Need: either extend doc 02's notification preference model to include push-specific fields (push_enabled, quiet_hours_start, quiet_hours_end per tier per project), or define a new `push_notification_preference` table. Without this, the mobile app cannot persist or sync push settings. | Open |
