# 0327a Run Log

- 2026-03-27 22:08:07 MDT - Traced the next task-lane efficiency leak from the live Sam.blog top-token sessions for task 37. Postmortem on sessions [`08fe54d5-f4e2-4459-8053-a812fcae5be8`](/Users/sam/dev/otter-camp/issues/log-0327a.md) and [`7c7666c7-640a-450a-8dde-b1aed1a3ac7a`](/Users/sam/dev/otter-camp/issues/log-0327a.md) showed repeated `cli_execute` calls with no arguments being rewritten into `file.write` executions whose `content` was the assistant’s own troubleshooting narration: “the file currently contains the assistant’s prose text ... the `file.write` calls without `content` ...”. That prose was slipping through the draft-reject matcher as if it were a reusable deliverable body.
- 2026-03-27 22:08:07 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so recovery/task draft rejection now explicitly catches that root-cause troubleshooting narration family (`assistant's prose text`, ``file.write` calls without `content``, `writing empty/chat text`). Added focused turn-engine coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) proving the reject matcher now blocks that prose and `latestSubstantiveAssistantFinalForTurn(...)` prefers a real file body over the later troubleshooting narration. Verified with `go test ./internal/turn -run 'Test(RecoveryFileWriteDraftRejectReasonRejectsRootCauseTroubleshootingNarration|LatestSubstantiveAssistantFinalForTurnSkipsLaterToolTroubleshootingNarration|LatestSubstantiveAssistantFinalForTurnSkipsLaterIntentPlaceholder)$' -count=1` and `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegrationNonRecoveryEmptyCLIExecuteStopsEarlyAfterCorrectionEX311$' -count=1`.
- 2026-03-27 22:08:07 MDT - Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. Fresh live proof landed on the same hot task family: after `22:04 MDT`, there were zero `tool_execution` rows whose `input` still contained `assistant's prose text`, while task 37’s fresh session `a80aa505-f06a-4e2c-a959-c482d7a54dd7` moved to direct deliverable-context checks instead of replaying the old prose-write loop.
- 2026-03-27 21:54:20 MDT - Pulled a fresh live queue-efficiency snapshot and isolated the next Sam.blog async waste seam: `task_queue_processor` kickoff dedupe still produced duplicate user messages for the same `run_id` on long-lived task sessions. Root cause: kickoff reconciliation depended on `ListMessages(... Limit: 200)`, but the chat service sorts ascending before slicing, so the dedupe window only saw the oldest 200 messages instead of the newest run history.
- 2026-03-27 21:54:20 MDT - Patched [`internal/controlplane/task_queue_processor.go`](/Users/sam/dev/otter-camp/internal/controlplane/task_queue_processor.go) and [`internal/chat/service.go`](/Users/sam/dev/otter-camp/internal/chat/service.go) so wakeup reconciliation now prefers a run-scoped lookup (`ListMessagesByRunID`) while still keeping the in-process singleflight guard for concurrent same-run appends. Added focused concurrency coverage in [`internal/controlplane/task_queue_processor_test.go`](/Users/sam/dev/otter-camp/internal/controlplane/task_queue_processor_test.go) plus a long-history regression in [`internal/controlplane/task_queue_processor_integration_test.go`](/Users/sam/dev/otter-camp/internal/controlplane/task_queue_processor_integration_test.go) that seeds 220 historical messages and proves sequential same-run wakeups still append exactly once. Verified with `go test ./internal/controlplane -run 'Test(AppendWakeupKickoffSuppressesConcurrentDuplicateRunKickoffs|DispatchTaskQueueWakeupFlowTransitionUsesExecutionSession|DispatchTaskQueueWakeupFlowCurrentUsesExecutionSession)$' -count=1`, `go test -tags=integration ./internal/controlplane -run 'Test(TaskQueueProcessorAppendWakeupKickoffDoesNotDuplicateRunBeyondListLimit|TaskQueueProcessorIntegrationAssignedWakeupIgnoresProjectScopedRunSessionEX294)$' -count=1`, and `go test ./internal/chat -count=1`.
- 2026-03-27 21:55:53 MDT - Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. Fresh live proof landed immediately after restart: there were 16 new `task_queue_processor` user messages in the last five minutes across Sam.blog tasks 17, 18, 19, 20, 21, 22, 23, and 30, while the post-restart duplicate query for same-session / same-`run_id` kickoff messages returned zero rows. The pre-fix duplicate pairs from `21:35-21:45 MDT` remained in history, but no fresh same-run duplicates were minted on the new runtime.
- 2026-03-27 10:19:47 MDT - Reloaded [`issues/samblog-project.md`](/Users/sam/dev/otter-camp/issues/samblog-project.md) after user saved changes and adopted its logging requirements for this run.
- 2026-03-27 10:19:47 MDT - Reloaded [`ralph-loop-instructions.md`](/Users/sam/dev/otter-camp/ralph-loop-instructions.md) and confirmed the active runtime is healthy via `./bin/ottercamp health --output json` returning `status=ok`.
- 2026-03-27 10:19:47 MDT - Re-read the summary sections at the top of the `docsv2/*.md` spec files to refresh the intended product behavior before resuming end-to-end testing.
- 2026-03-27 10:19:47 MDT - Observed that the current repo no longer contains `issues/01-ready`, `issues/02-in-progress`, `issues/03-needs-review`, or `issues/04-in-review`; logging that operator workflow mismatch and proceeding with the current live tree instead of stale lane assumptions.
- 2026-03-27 10:41:26 MDT - Worked the Ralph restart gate before starting the Sam.blog TUI run: repaired stale `internal/turn` restart-smoke integration fixtures, repaired stale `internal/controlplane` kickoff smoke assertions, and tightened kickoff duplicate-create stopping so the pre-run baseline matches current runtime behavior.
- 2026-03-27 10:41:26 MDT - Re-ran the exact flaky control-plane smoke path (`TestTaskQueueProcessorIntegrationQueuedFlowTaskStartsFlowAndRun`) under integration flags and confirmed it now passes against the live tree.
- 2026-03-27 10:41:26 MDT - Re-ran `./build/verify-restart-readiness.sh` and got a full PASS, clearing the restart-readiness gate for the upcoming `oc-test` Sam.blog session.
- 2026-03-27 10:44:10 MDT - Committed and pushed the pre-run baseline slice as `158fdb5d` (`Fix restart smoke coverage and kickoff retry churn`) so the Sam.blog run can start from a green restart gate and current runtime semantics.
- 2026-03-27 10:56:18 MDT - Restarted the smoke-gated runtime, opened the dedicated `oc-test` TUI session, reset the org chat with `:clear`, and attempted the required “fresh start” cleanup from inside the TUI.
- 2026-03-27 10:56:18 MDT - Observed that cleanup via Frank was provider-backed off into a delayed retry (`agent_turn` rescheduled around 11:00 MDT) and also found a stale extra TUI client (`ottercamp-tui`) producing avoidable API noise; closed the stale client before continuing.
- 2026-03-27 10:56:18 MDT - Added, committed, and pushed a native TUI project-archive path as `24f98464` (`Add direct TUI project archive commands`) so the operator can archive the selected project or all active projects without spending a model turn.
- 2026-03-27 10:58:43 MDT - Added a follow-up dashboard refresh on TUI project archive, committed and pushed as `9188f8a6` (`Refresh dashboard after TUI project archive`), so org cleanup updates both the sidebar and operator dashboard immediately.
- 2026-03-27 11:02:56 MDT - Live-tested `:archive-project all` in `oc-test`, saw it archive the first two projects, then fail on a short-lived API 429 burst while the TUI was also doing background refresh traffic.
- 2026-03-27 11:02:56 MDT - Added and pushed `991be6f6` (`Retry TUI cleanup commands on rate limits`) so direct TUI cleanup commands ride out transient local API 429s instead of partially failing.
- 2026-03-27 11:11:52 MDT - Verified the org is fully archived from Postgres and reproduced one remaining preflight bug in the live TUI: the dashboard still showed hundreds of blocked items on a zero-active-work org because archived-project paused and human-input rows were leaking into blocked counts.
- 2026-03-27 11:11:52 MDT - Patched the operator dashboard loader to ignore archived projects in the paused-project and human-input blocked branches, added focused control-plane integration coverage, rebuilt/restarted the runtime, and confirmed the live `oc-test` dashboard no longer shows the historical blocked backlog after refresh.
- 2026-03-27 11:11:52 MDT - Committed and pushed the dashboard fresh-start fix as `8d70c21f` (`Ignore archived projects in blocked dashboard counts`), which removes hundreds of dead historical rows from every blocked-dashboard refresh on archived orgs and restores a truthful zero-work operator view.
- 2026-03-27 11:13:48 MDT - Sent the full Sam.blog project brief to Frank inside the `oc-test` tmux TUI, using only the user-provided requirements and instructing OtterCamp to create and execute the project autonomously.
- 2026-03-27 11:13:48 MDT - Confirmed the first live kickoff succeeded immediately: Postgres now shows `active|1` and `archived|105`, meaning the new Sam.blog project was created from the TUI prompt without reintroducing stale cleanup noise.
- 2026-03-27 11:32:06 MDT - Reproduced the next live Sam.blog bootstrap failure family: freshly restarted bootstrap sessions kept re-reading broad project/workspace state (`project.list`, `task.list`, `file.list`) at the checklist-only staffing phase, then archiving and spawning another restart project instead of staffing the project.
- 2026-03-27 11:43:01 MDT - Used the now-wired TUI session reset path to get back to a true fresh-start org chat: cancelled the pinned stale Frank turn with `:cancel`, then entered command mode correctly and ran `:clear`, which archived the old org session and created a new empty organization chat session.
- 2026-03-27 11:43:28 MDT - Relaunched the Sam.blog kickoff from the fresh org session and confirmed a new active project (`sam-blog-rebuild`) was created cleanly without reviving the older archived restart projects.
- 2026-03-27 11:44:10 MDT - Verified the new bootstrap guard live on the fresh project session: the runtime blocked checklist-only restart rediscovery with explicit `project.list`/workspace reread guard errors, then the next continuation pivoted into `bootstrap.setup.persist` and advanced bootstrap steps instead of archiving the project again.
- 2026-03-27 12:14:02 MDT - Reviewed the current live-tree bootstrap delta and finished the in-flight `bootstrap.setup.persist` hardening: converted the tool from prompt-trusting checklist persistence to structural gating against real repo binding, assignment, task-shape, flow-template, first-wave, and sign-off state.
- 2026-03-27 12:14:02 MDT - Repaired the associated native integration fixtures, reran the full focused bootstrap persist integration slice, and reran the broader `go test ./internal/tools/native -count=1` package pass; both are now green and ready to commit before resuming the Sam.blog end-to-end run.
- 2026-03-27 12:17:44 MDT - Committed and pushed the bootstrap setup persistence fix as `ed18a178` (`Harden bootstrap setup structural persistence`), rebuilt `./bin/ottercamp`, and restarted the `codex-e2e-20260324` tmux runtime on the new binary.
- 2026-03-27 12:17:44 MDT - Verified `./bin/ottercamp health --output json` returned `status=ok`, then checked the live backend state and confirmed there are currently zero active projects; the visible `oc-test` project pane is stale chat context rather than live active work.
- 2026-03-27 12:38:13 MDT - Reviewed the in-flight Sam.blog bootstrap staffing fixes already sitting in the live tree and found the new role-aware `bootstrap.setup.persist` gate was only partially wired: the role counters were present in [`internal/tools/native/mutation_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/mutation_tools.go), but the missing-role helper/message path had not been implemented yet, so the package did not compile.
- 2026-03-27 12:38:13 MDT - Finished the role-aware bootstrap staffing gate, added a new partial-staffing regression, repaired the affected bootstrap persist integration fixtures to seed a full PM/worker/reviewer team when the test intends `staff-project` to pass, and reran the focused bootstrap integration slice plus the impacted `internal/tools/native` and `internal/turn` package tests; all are green and ready to commit.
- 2026-03-27 13:08:55 MDT - Traced the next live Sam.blog bootstrap failure to the bootstrap phase/checkpoint machine itself: draft-only staffing and partial staffing could still advance `last_successful_checkpoint`, stale completed checkpoints could carry forward out of order, and late resume/scaffold prompts could still steer partial staffing turns into `bootstrap.setup.persist` or scaffold-only logic too early.
- 2026-03-27 13:08:55 MDT - Patched the turn engine so bootstrap success checkpoints now require a real PM/worker/reviewer roster, persisted state healing clears stale out-of-order staffing carry-forward on older sessions, resume state exposes role-aware staffing counts, and partial-staffing resume/scaffold branches stay focused on finishing assignments instead of broad rereads or premature `bootstrap.setup.persist`; focused unit and integration slices are green.
- 2026-03-27 13:08:55 MDT - Rebuilt `./bin/ottercamp`, recreated the `codex-e2e-20260324` tmux runtime cleanly after a failed in-place restart, and confirmed `./bin/ottercamp health --output json` returned `status=ok` on the fresh serve/worker panes.
- 2026-03-27 13:08:55 MDT - Checked the current live Sam.blog project after the restart. The visible `restart-12` failure state narrowed to a later structural issue (`project_manager=1`, `worker=1`, `reviewer=0` on the live roster) rather than the old draft-staffing checkpoint bug, but that specific project/session evidence was from the pre-restart turn window; a fresh post-restart bootstrap turn is still needed for direct live proof of the new checkpoint-healing slice.
- 2026-03-27 13:08:55 MDT - Committed the bootstrap-state checkpoint repair as `d8bd09cf` (`Require full bootstrap staffing checkpoints`). I still owed the fix-log entries for the earlier pushed staffing gate commit and this new checkpoint slice, so I staged those as the final note-only follow-up before pausing.
- 2026-03-27 13:49:38 MDT - Reproduced the next live bootstrap-repair gap on `sam-blog-rebuild-restart-12`: org turn `ad3446ce-fffc-467f-9c7a-796ca198c203` repaired reviewer staffing and successfully called `bootstrap.setup.persist`, but the project still stayed paused on the stale reviewer-missing failure because the org-scope bootstrap refresh hook only recognized assignment/task mutations, not `bootstrap.setup.persist`.
- 2026-03-27 13:56:16 MDT - Patched the org-scope bootstrap refresh path so `bootstrap.setup.persist` counts as a qualifying project mutation in [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go), added the matching integration regression in [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go), reran the focused unit/integration slice, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed health returned `status=ok`.
- 2026-03-27 14:00:08 MDT - Live-proved the fix from the watched `oc-test` TUI: forced org turn `07bc2266-9967-4dbf-b4e1-521724092b81` to begin with `bootstrap.setup.persist`, then verified immediately from Postgres that project `a6dbd331-7205-42d9-b0df-10105d5b5330` cleared its stale reviewer-missing pause instead of remaining gated.
- 2026-03-27 14:00:11 MDT - The same live proof narrowed the next structural gap: once the stale reviewer pause cleared, bootstrap state advanced to a later failure class (`first_wave_execution_missing`) showing that first-wave child tasks had entered flow execution but no runnable `agent_turn` jobs were created for their execution sessions. The stale-repair family is fixed; the next family is first-wave execution dispatch.
- 2026-03-27 14:28:14 MDT - Diagnosed the first-wave execution stall to a worker recovery blind spot: [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) only requeued active execution sessions that already had a kickoff `chat_message`, while the stranded Sam.blog first-wave task sessions had active `flow_node_execution` rows with `message_count=0`, `turn_count=0`, and zero `agent_turn` jobs.
- 2026-03-27 14:28:14 MDT - Added a no-message recovery path in the worker that synthesizes an execution-bound `task_queue_processor` kickoff only when an active execution session truly has no usable kickoff (or only an already-consumed generic supervisor resume), then reran the broader `RequeueActiveExecutionSessionsWithoutTurns` integration cluster to preserve adjacent stale-run, cancelled-latest, and recovery-halt behavior.
- 2026-03-27 14:28:14 MDT - Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, confirmed `./bin/ottercamp health --output json` returned `status=ok`, and live-verified the Sam.blog first wave from Postgres: previously stranded task sessions now have execution-bound kickoff messages plus runnable `agent_turn` jobs instead of sitting at zero messages/zero jobs.
- 2026-03-27 14:57:04 MDT - Followed the next live Sam.blog first-wave stall through the queue layer and found a second recovery family: once a first-wave task had both an older execution-owned `task_queue_processor` kickoff and a newer generic `supervisor recovery: resume task` message, the worker treated the newer supervisor message as the authoritative continuation across three different seams (`PurgeStaleAgentTurnJobs`, `claimPendingAgentTurns`, and `RequeueActiveExecutionSessionsWithoutTurns`).
- 2026-03-27 14:57:04 MDT - Added execution-aware ordering to those worker paths so active execution-bound messages (`task_queue_processor`, `task_review_action`, `task_recovery_resume`) outrank a later generic supervisor resume, while ordinary non-execution task-session retries still keep the existing newest-message behavior. I added focused integration coverage for purge ordering, claim ordering, and the exact live recovery shape where a pending supervisor job must be dead-lettered and replaced by a fresh execution-bound kickoff.
- 2026-03-27 14:57:04 MDT - Re-ran the focused integration slice, rebuilt `./bin/ottercamp`, recreated the watched tmux runtime, and confirmed `./bin/ottercamp health --output json` returned `status=ok` again.
- 2026-03-27 14:57:04 MDT - Live Sam.blog evidence after the new runtime: task 27 no longer sat on the supervisor-only stall family. Its session history shows fresh execution-bound `task_queue_processor` wakeups at `2026-03-27 14:51:06 MDT`, and task 27 is now `done` with closed task sessions. Task 23 still has a preexisting supervisor-driven pending turn from the older path, so the newest selector branch itself is deployed/tested but still awaiting a fresh natural live recovery hit for that exact remaining session.
- 2026-03-27 15:07:29 MDT - Traced the next visible Sam.blog delay to queue fairness rather than broken task state: task 23 (`d4700498-439b-451d-b80e-53640e458914`) was still `in_progress` with a due supervisor retry at `14:50:35 MDT`, but the actual oldest due async task backlog was tasks 18 and 19 at `14:19 MDT`, followed by older task sessions at `14:25-14:31 MDT`.
- 2026-03-27 15:07:29 MDT - Changed ordinary cross-session `agent_turn` claim order to oldest-due first while preserving newest-first project continuation semantics and same-session latest-message selection, reran the focused worker integration slices, rebuilt/restarted `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` stayed `ok`.
- 2026-03-27 15:07:29 MDT - Live proof after restart: the first claimed ordinary task-session job was task 18 (`1f2c7217-f798-4672-aa78-c57c41330ee0`) with `run_after=2026-03-27 14:19:19 MDT` and `claimed_at=2026-03-27 15:07:01 MDT`, while task 19 (`a8fbe60a-393b-43a8-83c4-880a90a10e16`) and task 23 (`d4700498-439b-451d-b80e-53640e458914`, `run_after=14:50:35 MDT`) remained pending behind older due work instead of newer arrivals jumping the queue.
- 2026-03-27 15:30:12 MDT - Continued the live task-session efficiency review after the fairness deploy and found the next generic churn family in Sam.blog tasks 18 and 19: both sessions had already completed a recovery turn that wrote the target file successfully, but the worker was still able to resurrect the consumed `task_recovery_resume` message later and spend another `agent_turn` on the same recovery lane.
- 2026-03-27 15:30:12 MDT - Patched the worker in two places: startup/periodic stale-job purge now dead-letters pending `task_recovery_resume` dispatches whose trigger message already has a completed `file.write` with positive `byte_size`, and `RequeueActiveExecutionSessionsWithoutTurns(...)` now skips recreating those consumed recovery-resume jobs in the first place.
- 2026-03-27 15:30:12 MDT - Added focused worker integration coverage for both the purge and requeue sides, reran the targeted integration slice, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok` on the new binary.
- 2026-03-27 15:30:12 MDT - Post-restart live check: the currently queued tasks 18, 19, and 23 had already advanced into newer recovery/supervisor messages, so the old stale-successful-resume rows were no longer the active jobs by the time the new worker came up. The fix is deployed and test-proven, but fresh live proof for that exact stale-successful-resume family now depends on the next naturally occurring case rather than the older queue rows that had already rolled forward.
- 2026-03-27 15:43:33 MDT - Continued the task-session efficiency analysis and isolated the next churn family in Sam.blog task 19’s HTML recovery lane: `file.write` reported success for `templates/layout-04-magazine.html`, but the very next recovery pass still halted with “was not found on disk” and reopened recovery.
- 2026-03-27 15:43:33 MDT - Traced that false-negative durability check to a root mismatch in the turn engine: recovery verification/readback only searched project compatibility roots, while the HTML task’s `file.write` landed in the task worktree under `data/task-worktrees/.../task-19/templates/layout-04-magazine.html`.
- 2026-03-27 15:43:33 MDT - Patched recovery root resolution so task-scoped recovery reads and durability checks include the task worktree before falling back to project compatibility roots, added a new integration regression that writes only into the task worktree, and reran the focused integration and unit slices for the recovery path.
- 2026-03-27 15:47:21 MDT - Committed the task-worktree durability fix as `ac297e3b` (`Verify recovery writes against task worktrees`), rebuilt `./bin/ottercamp`, recreated tmux session `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok` on the new binary.
- 2026-03-27 15:47:21 MDT - Immediate post-restart live check: Sam.blog tasks 18, 19, and 23 still sit `in_progress`, but there were zero active `pending`/`in_progress` chat turns on those task sessions at the first check window. So this slice is now deployed and ready; fresh production proof of the fixed HTML recovery path depends on the next recovery turn rather than an already-running lane.
- 2026-03-27 15:57:02 MDT - Re-read the live one-hour token report on the new binary and found the next dominant waste family is still `validation_loop_blocked`, led by review task 13 and a handful of HTML/spec lanes with deterministic validation failures (`cli.execute command required`, `file.list not_found`).
- 2026-03-27 15:57:02 MDT - Traced the hottest review leak end-to-end on task 13: the review turn really did block at `15:47:16 MDT`, but supervisor recovery reopened it to `review` at `15:48:30 MDT` with `recovery_action=resume_validation_blocked_task`, creating a fresh pending review turn after the block.
- 2026-03-27 15:57:02 MDT - Patched supervisor blocked-task detection to skip `review_decision_required` review blocks before abandoning active execution or auto-resuming the task, then added a focused supervisor integration regression proving ordinary validation blocks still auto-resume while blocked review-decision tasks stay blocked with their validation guard and active execution intact.
- 2026-03-27 15:58:36 MDT - Committed the supervisor review-block fix as `3e00509e` (`Skip supervisor resumes for blocked review turns`), rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok` on the new binary.
- 2026-03-27 16:08:06 MDT - Continued the one-hour efficiency review on the current Sam.blog runtime and confirmed the main remaining cost is still async task-session churn rather than queue health: `./bin/ottercamp db token-usage --hours 1 --limit 15 --output json` reported about `4.15M` total tokens with `2.95M` cache-read tokens, dominated by `project_task async -> validation_loop_blocked` (`43` turns / `1.84M` tokens) and `project_task async -> max_tool_calls` (`20` turns / `0.94M` tokens).
- 2026-03-27 16:08:06 MDT - Traced the hottest concrete deterministic family to ordinary task turns that emitted `cli.execute` without `command`, received a system correction, and then still paid for malformed tier2 dispatches when the model repeated the same empty call in the same turn.
- 2026-03-27 16:08:06 MDT - Patched the task-turn empty-`cli.execute` path to record a synthetic validation failure before dispatch so the existing same-turn validation guard now stops the second identical retry without paying for malformed tool runs; reran the focused unit coverage plus the non-recovery integration slice, and noted separately that two older recovery EX311 expectations are currently stale/red and were not widened in this efficiency slice.
- 2026-03-27 16:10:49 MDT - Committed the empty-`cli.execute` efficiency fix as `79473bde` (`Bound task empty cli.execute retries`). The new binary is already rebuilt and live in tmux `codex-e2e-20260324`, `./bin/ottercamp health --output json` is still `ok`, and the post-restart one-hour token snapshot is still dominated by preexisting review/recovery blockers; the last observed `cli.execute command required` turn stop in the report was at `16:03:32 MDT`, which is pre-deploy for this slice.
- 2026-03-27 16:29:38 MDT - Re-ran the live one-hour task-session efficiency snapshot after the empty-`cli.execute` deploy and found two adjacent deterministic families still hot in HTML task lanes: repeated pathful `file_edit` attempts that omitted `new_string`, and false-positive `malformed _raw arguments` validation on successful `_raw` file writes.
- 2026-03-27 16:29:38 MDT - Traced the task-21 split-screen turn directly from Postgres and confirmed the root cause: live assistant metadata was still carrying `_raw` `file_edit` calls with `path` and `old_string` but no `new_string`, while `toolargs.Normalize(...)` had no `file.edit` recovery branch at all. That meant the task-turn repair handlers could not see the recovered path fields and the validation layer could still punish successful writes purely because the original call envelope contained `_raw`.
- 2026-03-27 16:29:38 MDT - Patched `toolargs` to normalize `_raw` `file.edit` arguments, taught the turn engine to bound non-recovery `file.edit` calls that omit `new_string` before dispatch, and relaxed validation classification so successful recovered `_raw` calls no longer count as malformed-tool failures. Focused `internal/toolargs` unit tests plus focused `internal/turn` unit/integration slices are green; the next step is to rebuild/restart and confirm the fresh blocker mix on the live runtime.
- 2026-03-27 16:35:19 MDT - Committed the raw-`file.edit` efficiency fix as `017a10b9` (`Normalize raw file.edit and bound missing-new_string retries`), rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok` on the new binary.
- 2026-03-27 16:35:19 MDT - Immediate post-restart live check: the earlier `malformed _raw arguments` task-21 stop has not reappeared on the new binary, but the same session is still surfacing a neighboring tool-routing failure where `file.write` drops the intended HTML body and the lane keeps reading back stale tool-error text. I logged that separately as the next live blocker instead of folding it into the raw-`file.edit` slice.
- 2026-03-27 16:46:12 MDT - Followed the next task-21 blocker to the turn-engine repair layer. Live assistant metadata is still frequently emitted with the `file_write` alias, while the task/recovery file-write repair helpers and the “already-synthesized file write” guard still treated only `file.write` as repairable. In the same lane, the stale troubleshooting text (“The tool infrastructure is routing all tool calls…”) was still admissible as a substantive draft candidate, which let the engine rewrite that error prose back into the target HTML file.
- 2026-03-27 16:46:12 MDT - Patched the turn engine to recognize `file_write` alongside `file.write` in the task/recovery repair helpers and task execution synthesis guard, and extended `recoveryFileWriteDraftRejectReason(...)` so the live tool-routing/operator-repair narration is rejected as tool-recovery troubleshooting instead of being reused as file content. Focused `internal/turn` unit coverage is green, including the new alias and stale-artifact regressions; the older integration fixture `TestTurnEngineIntegrationFileWriteMissingContentRetryStillBlocksTrulyInvalidCalls` remains red on this branch and appears to be separate existing test debt rather than a regression from this alias/troubleshooting slice.
- 2026-03-27 16:46:12 MDT - Committed the alias/troubleshooting hardening as `248d9ba7` (`Recognize file_write aliases and reject tool-routing drafts`), rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. No fresh post-restart task-21 turn has executed yet, so live proof for this exact slice is still pending the next natural HTML-task retry.
- 2026-03-27 16:51:50 MDT - Re-read the hottest post-restart task lanes and found the next deterministic failure in task 10’s content-migration path: after a max-tool-call/context-compression continuation, the new turn got the content-migration checkpoint system message but no synthetic continuation action prompt, and the model replied with generic chat (“I'm ready to help! … What would you like to know?”) instead of resuming the migration.
- 2026-03-27 16:51:50 MDT - Patched `continueTurn(...)` so content-migration checkpoints no longer short-circuit the rest of continuation setup. The new turn now keeps the checkpoint message and still appends the normal task continuation summary/action prompt. Strengthened the existing content-migration integration test so the second prompt must contain the direct task-action text and anti-generic-chat guard.
- 2026-03-27 16:51:50 MDT - Committed the continuation fix as `ebff2033` (`Continue content migration turns after checkpoints`), rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. Live proof landed immediately: task 10 session `8048c518-b8e2-4e95-8e54-2fa763325b31` no longer emitted the generic “What would you like to know?” reply after the checkpointed overflow turn; on the next post-restart continuation (`1cfc16e9-a3d3-4270-b151-8379a9a22ddc`), it resumed direct task action and went straight back into concrete workspace/tool inspection.
- 2026-03-27 16:57:43 MDT - Re-read the content-migration hotspots after the continuation fix and traced the next empty-branch loop on Sam.blog tasks 10 and 11: when a migration checkpoint had zero artifacts, scripts, and outputs, [`internal/taskcheckpoint/content_migration.go`](/Users/sam/dev/otter-camp/internal/taskcheckpoint/content_migration.go) returned no immediate-next-action guidance at all, so the next continuation still paid for `file.list` / `git.status` / `git.log` rediscovery against an empty task branch instead of pivoting to the upstream manifest/index deliverables named in the task.
- 2026-03-27 16:57:43 MDT - Patched the content-migration checkpoint helper to emit explicit empty-workspace guidance (“do not re-list the empty workspace; read the upstream manifest/index/source files; write the first concrete artifact or output”), added focused unit coverage in [`internal/taskcheckpoint/content_migration_test.go`](/Users/sam/dev/otter-camp/internal/taskcheckpoint/content_migration_test.go), reran the focused `internal/taskcheckpoint` package plus the existing content-migration integration slice in [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go), rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 16:59:44 MDT - Committed the empty-branch migration guidance slice as `9c4edfe5` (`Guide empty migration branches to upstream inputs`). Live proof landed immediately on task 11 session `1aea2d41-cdfe-4df5-ac17-897c006e702f`: the `16:59:01 MDT` checkpoint system message now includes the new empty-workspace guidance, and the lane pivoted into the upstream archive/index path instead of staying in branch rediscovery.
- 2026-03-27 17:07:55 MDT - Re-ran the one-hour live efficiency snapshot after the task-11 checkpoint fix and found the new top waste bucket is still the project PM lane: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` alone consumed about `1.98M` tokens over the hour, mostly in repeated `max_tool_calls` turns.
- 2026-03-27 17:07:55 MDT - Traced that PM churn to a gap in the project-continuation rediscovery guard. The synthetic continuation prompts at `16:54-16:57 MDT` named only active tasks (`30`, `25`, `24`, `23`) and no draft tasks, so the existing guard never activated; the project lane then reopened with `session.list`, broad `task.list`, root `file.list`, `git.log`, and repeated `task.get` on already-named tasks.
- 2026-03-27 17:07:55 MDT - Patched the project continuation prompt and guard to treat active-task-only snapshots as sufficient context, added focused unit coverage for `session.list`, `git.log`, root `file.list`, and named active-task `task.get`, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. No fresh post-restart project PM continuation has fired yet, so live proof for the new active-only guard is still pending the next natural project-lane wakeup.
- 2026-03-27 17:15:32 MDT - Re-read the hottest recovery task after the project-lane guard deploy and found another deterministic task-session leak in Sam.blog task 11: when the engine itself emitted a synthetic `task_recovery_resume` message, a generic assistant reply like “I'm ready to help! What would you like to know?” could still complete the turn silently instead of taking the normal generic-recovery-reply retry path.
- 2026-03-27 17:15:32 MDT - Traced that miss to `isRecoveryResumeMessage(...)` in [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go): it recognized supervisor resume text and explicit `recovery_action` metadata, but not the engine's own `metadata.source=task_recovery_resume` continuation prompts, so `handleCompletedTaskResumeWithoutUsableAssistant(...)` never classified those turns as recovery resumes.
- 2026-03-27 17:15:32 MDT - Patched the recovery-resume detector to recognize synthetic `task_recovery_resume` sources, added a focused unit regression for the exact live shape, reran the targeted `internal/turn` test slice, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 17:15:32 MDT - Post-deploy live check on task 11 session `1aea2d41-cdfe-4df5-ac17-897c006e702f`: the next synthetic recovery-resume continuation at `17:10:36 MDT` went straight back into concrete recovery action and wrote `content/technonymous/stop-preparing-your-kids-for-jobs.md` instead of falling into generic chat again. The exact generic-reply retry branch is now test-proven and deployed; fresh production proof for that specific fallback still depends on the next natural generic response, but the adjacent live task lane is healthy on the new runtime.
- 2026-03-27 17:16:40 MDT - Committed the synthetic recovery-resume detection fix as `c1975e83` (`Retry generic synthetic recovery resumes`). I left the task-11 live note marked as adjacent proof rather than over-claiming direct production proof for the specific generic-reply retry branch; that narrower proof will need the next natural generic response on a synthetic recovery-resume turn.
- 2026-03-27 17:38:33 MDT - Re-read the hottest recovery-efficiency family after the synthetic-resume fix and confirmed the next deterministic cost is still malformed recovery mutation churn in concrete file-output lanes: once a recovery turn emitted an empty `cli.execute` or empty `file.write`, the first correction message could still be overwritten by a later generic repeated-recovery stop, which discarded the sharper missing-command / missing-content context before the next resume.
- 2026-03-27 17:38:33 MDT - Patched the recovery checkpoint path so first corrections for empty `cli.execute` and empty `file.write` persist a specific failure reason when the task already has a concrete recovery target, repeated recovery-focus halts preserve or escalate that specific reason instead of downgrading back to generic read-only churn, and successful `cli.execute` / `file.edit` writes now clear the same checkpoint path just like successful `file.write`.
- 2026-03-27 17:38:33 MDT - Verified the slice with focused unit coverage for first-correction checkpoint persistence plus `cli.execute` checkpoint clearing, and with the focused EX307 integration path that exercises successful recovered `cli.execute` file-output cleanup. While checking adjacent recovery fixtures, I confirmed two older integration expectations are stale against the current persisted-draft rewrite behavior and logged that fixture drift separately instead of folding it into this change.
- 2026-03-27 17:51:29 MDT - Pulled a fresh one-hour efficiency snapshot on the live runtime after the checkpoint-preservation deploy. Queue health stayed good (`pending=0`), but the next deterministic review waste family was clear in Sam.blog tasks 24 and 10: review turns were still re-reading missing `planning/prd-spec/...` companion files even after the same turn had already established that current deliverables existed and were sufficient to continue bounded review.
- 2026-03-27 17:51:29 MDT - Traced the live task-10 session `b09b5b7b-8e38-4a3b-a59d-7468ab8939e4` from Postgres. Each retry successfully listed `content/posts`, then spent the next review pass on missing `planning/prd-spec/oc-34-*.md` reads, and only after three such retries did the existing blocked-review cap stop the loop. Task 24 showed the same shape at lower cost: a direct deliverable read plus a handful of missing planning reads before the retry finally rejected correctly.
- 2026-03-27 17:51:29 MDT - Patched the review retry path so when a review turn already proved deliverables exist and the only missing files were secondary `planning/` reads, the next retry prompt now stays on those deliverables instead of reopening planning-artifact discovery. The new prompt points the model back to the existing deliverable root/sample paths, explicitly bans rereading `planning/`, `task.get`, `git.log`, and broad workspace discovery for the same review, and directs the turn to finish bounded inspection plus `flow.review_decision`.
- 2026-03-27 18:02:20 MDT - Re-read the hot Sam.blog PM continuation after the active-only rediscovery guard and found the next narrower waste family in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: the prompt still surfaced draft parent tasks `33` and `34` as actionable work even though both already had active child execution lanes (`37/38` and `35/36`), so the project lane kept spending turns on those parent shells instead of letting their child work proceed.
- 2026-03-27 18:02:20 MDT - Patched the project continuation snapshot so draft parents with active child execution are no longer counted as actionable remaining drafts, now appear in their own `Draft parent tasks already have active child work:` line, and actionable draft refs carry explicit prerequisite blockers such as `assigned_agent_id=missing`, `flow_template_id=missing`, and `requires_human_review=true` when present. The continuation prompt now tells the PM lane to repair those exact prerequisites before queueing and to stop re-queueing or rediscovering parent drafts that already have active child lanes.
- 2026-03-27 18:02:20 MDT - Added focused `internal/turn` unit coverage for the new project-continuation snapshot semantics and prompt wording, including the live shape where a draft parent has two active child tasks and must move out of the actionable-draft line. Focused continuation/rediscovery tests are green; next step is to rebuild/restart and confirm the PM lane stops surfacing tasks `33/34` as runnable work on the new binary.
- 2026-03-27 18:12:32 MDT - Fresh post-deploy PM turns proved the snapshot wording itself was live, but exposed a second handoff bug. The latest synthetic user continuation prompt already contained the new `Draft parent tasks already have active child work:` line for tasks `34` and `33`, yet the project lane still spent turns on `session.list`, named active-task `task.get`, and `flow.get_execution` misuse. Root cause: continuation turns were still rooted on the summary system message (`[Continuation summary] ...`) rather than the synthetic user action prompt, so `rt.initialMessageText` never contained the guarded snapshot lines.
- 2026-03-27 18:12:32 MDT - Patched `appendContinuationSummaryAndAction(...)` so whenever it appends a synthetic continuation action prompt, the next turn’s trigger/history root is that synthetic user message instead of the summary system message. Added a focused unit regression for the live project PM shape to ensure project continuations now preserve the actionable prompt as `historyStartID` / `trigger_message_id`.
- 2026-03-27 18:25:38 MDT - Re-read the first fresh Sam.blog PM continuation turns after the root-history fix and found one more same-turn gap: the runtime now preserved `historyStartID` / `trigger_message_id` correctly, but the live turn still kept `rt.initialMessageText` and `rt.initialMessageID` pointed at the older pre-summary trigger while the synthetic continuation action prompt was appended inside the same turn before the model responded. Impact: the project-lane rediscovery guards still missed `task.get`, `session.list`, and `task.list` misuse during the exact turn that contained the guarded prompt.
- 2026-03-27 18:25:38 MDT - Patched `appendContinuationSummaryAndAction(...)` again so each appended synthetic continuation action prompt immediately becomes the live turn runtime’s effective initial message (`rt.initialMessageID` / `rt.initialMessageText`) for same-turn guard logic, not just the persisted history root. Extended the focused project-continuation unit regression to assert the same-turn `session.list` and named-task `task.get` guards fire after the synthetic prompt is appended.
- 2026-03-27 18:26:58 MDT - Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. Live proof landed immediately on Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: post-restart turn `1cda0a9d-649c-4e68-bde4-41bfa252e765` blocked named active-task `task.get` and broad `task.list` with the new project-continuation guard errors, and the following turn `b6f310c5-0a20-4778-a560-c9e6c7ba4db3` also blocked `session.list`, `inbox.list`, root `file.list`, and another broad `task.list` in the same PM lane. The next visible PM waste family is now narrower: rereads of scaffold `planning/prd-spec/oc-33-*` files after the broader rediscovery tools are already blocked.
- 2026-03-27 18:36:12 MDT - Re-read the next live PM retries after the same-turn guard fix and found two narrower but still-expensive continuation families in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: the project lane was still spending tools on recursive `file.list(path=content/posts)` / `file.list(path=templates)` artifact-root probes after the broader rediscovery guards fired, and the continuation summary itself was still sometimes a fenced pseudo-tool script (`task.get --task_id=...`) that primed the next turn to replay the same blocked tool calls.
- 2026-03-27 18:36:12 MDT - Patched the project-continuation artifact browse guard so `file.list` on `content/`, `content/posts`, and `templates` is treated as broad artifact-root rediscovery in the PM lane, added focused coverage for those exact roots, and extended `continuationSummaryLooksUnavailable(...)` so fenced pseudo-tool CLI summaries with real tool names plus argument flags (`--task_id`, `--scope_id`, `--path`, etc.) collapse back to the normal fallback summary instead of being reused.
- 2026-03-27 18:36:12 MDT - Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. Live proof landed on the same PM session immediately: retry attempt `22b2c7fc-1b50-4c04-8c36-e7683cb238e5` blocked `inbox.list` plus the exact `content/posts` and `templates` artifact-root `file.list` calls, and the next continuation summary at sequence `2260` fell back to the clean generic summary instead of repeating the earlier fenced `task.get --task_id=...` pseudo-tool script from sequence `2239`.
- 2026-03-27 18:42:44 MDT - Re-read the hot Sam.blog PM continuation after the artifact-root guard deploy and found the next efficiency leak one layer deeper. In session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, once a project continuation turn got back only blocked rediscovery guard results, the turn still paid for another assistant reply inside the same turn before the existing retry/block machinery took over. That meant already-blocked `task.get` / `task.list` / workspace-browse batches could still consume one extra model round per PM retry.
- 2026-03-27 18:42:44 MDT - Patched `dispatchTools(...)` in [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) to stop async project turns immediately when a blocked batch is composed only of project-continuation rediscovery guard errors and there are no runnable tool calls left in the batch. Added focused unit coverage for both the predicate and the dispatch-level early stop in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), reran the focused project-continuation turn slice, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 18:42:44 MDT - Fresh live proof landed immediately after the restart on post-`18:43:22 MDT` PM retry turn `7ff69bb7-db73-46a6-9268-cfc58676bbf2`: after blocked `task.list` plus blocked artifact-root `file.list(path='content')` / `file.list(path='.')` results, the turn emitted the new system message `Project continuation rediscovery guard blocked only broad rereads...` at sequence `2467` and completed with `stop_reason=validation_loop_blocked` instead of spending another same-turn assistant round on the blocked PM rediscovery batch.
- 2026-03-27 18:52:55 MDT - Stayed on the same Sam.blog PM session and traced the next residual waste family to the worker, not the turn engine. The auto-continued `project_execution_continuation` messages at sequences `2491` and `2514` were still the stale worker-authored text with no `Active project id`, no active-task snapshot, and no actionable-draft snapshot, so those turns reopened with broad `task.list` and then re-read tasks `34/35/36/11/12/13/10` until `max_tool_calls`.
- 2026-03-27 18:52:55 MDT - Patched [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so worker-synthesized `project_execution_continuation` prompts now build and append the same project snapshot guidance the turn engine already uses: active project id, named active tasks, named draft tasks, prerequisite hints, and anti-rediscovery guard text. Added a focused integration regression in [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go), reran the targeted jobqueue integration slice, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 18:52:55 MDT - Live proof landed on the first fresh worker-generated continuation after the restart: sequence `2651` in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` now includes `Active project id: ...`, `Already-active non-terminal tasks in the tree: ...`, and `Actionable draft tasks already in the tree: ...`, whereas the pre-restart worker prompt at sequence `2628` contained none of those lines. The very next PM turn (`2652-2657`) immediately hit the existing named-`task.get` rediscovery guard and ended with `Project continuation rediscovery guard blocked only broad rereads...` instead of reopening with broad `task.list`.
- 2026-03-27 18:57:10 MDT - Re-read the next fresh PM `max_tool_calls` turn after the worker snapshot fix and found the next remaining tail-waste family: the project lane is now reaching the real blocker on task 34, but only after spending most of the turn budget on intermediate rereads. In turn `a338ef09-935a-4e4e-b3d3-a60bf0d95d63`, the useful signal was `task.update -> task exceeds bounded size policy ... split the work into smaller reviewable tasks before queueing`, yet the turn still ended as `max_tool_calls` because the existing project-lane blocked-mutation stop did not recognize `output.error`-only bounded-size rejections.
- 2026-03-27 18:57:10 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so `shouldStopAfterBlockedProjectExecutionBlockedMutation(...)` now reads tool-result error codes from either `result.Error` or `result.Output["error"]`, treats project-lane `task.update` / `task.create` bounded-size-policy rejections as immediate stop conditions, and appends a follow-up system message telling the next continuation to split the broad parent into smaller reviewable child tasks instead of retrying the same queue attempt. Added focused unit coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), reran the targeted turn-engine slice, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 18:57:10 MDT - No fresh PM traffic has hit the new bounded-size early stop yet after the `18:56:31 MDT` restart, so this slice is currently test-proven and deployed but still awaiting direct production proof on the next task-34 promotion attempt.
- 2026-03-27 19:04:46 MDT - Re-read the next Sam.blog PM continuation waste family after the bounded-size stop deploy and found one remaining broad reread still slipping through the project-continuation snapshot guard: `flow.list_templates`. Live proof before the fix remained PM turn `a338ef09-935a-4e4e-b3d3-a60bf0d95d63`, which opened with `flow_list_templates` at sequence `2661` even though the continuation already named the active/draft task set and no actionable draft in the prompt was blocked on `flow_template_id=missing`.
- 2026-03-27 19:04:46 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so project-continuation rediscovery now blocks `flow.list_templates` / `flow_list_templates` whenever the continuation prompt already provides the active/draft task snapshot and no named draft is explicitly blocked on `flow_template_id=missing`. Added focused unit coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for both the newly blocked ordinary PM path and the still-allowed missing-template repair path, then reran the targeted project-continuation turn slice successfully.
- 2026-03-27 19:06:27 MDT - Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`. Fresh Sam.blog PM continuation traffic resumed immediately on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: the first post-restart retry (`2698-2703`) still hit the named-task rediscovery guards and there were no post-`19:05 MDT` `flow.list_templates` calls in that lane, so the slice is deployed with adjacent live evidence even though the exact new template-catalog guard has not yet been directly exercised by a fresh blocked tool result.
- 2026-03-27 19:08:12 MDT - Committed the PM template-catalog rediscovery fix as `df0bbbb7` (`Block PM template rediscovery without blockers`). Next likely PM waste family remains the task-34 decomposition path itself: after rediscovery is stripped down, the remaining project-lane turns are mostly about deciding how to split or advance the bounded-size-blocked parent rather than re-discovering context.
- 2026-03-27 19:12:41 MDT - Stayed on the same Sam.blog PM continuation family and tightened the focus-draft prompt itself. The live `project_execution_continuation` message was still telling the model to “Inspect the current task tree...” even when the focus draft already showed `child_tasks=2` and the active-task snapshot already named child lanes `35` and `36`, so the PM lane kept burning turns on the same blocked `task.get` rereads for the parent and its children.
- 2026-03-27 19:12:41 MDT - Patched both prompt builders, [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) and [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go), so when the focus draft already has child tasks and the active-task snapshot already names those child lanes, the continuation prompt now explicitly says not to reread the parent or child task records first and instead to queue the draft directly, split it into smaller reviewable work if bounded-size policy still blocks it, or report one concrete blocker sentence. Added focused unit and worker integration coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) and [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go), reran both targeted slices, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 19:14:56 MDT - Live prompt proof landed on the first fresh worker-generated PM continuation after the restart: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, sequence `2753`, now includes the new `If that focus draft already has child tasks... do not reread the parent or child task records first...` line. The follow-on assistant still ignored that prompt and reopened with blocked `task.get` calls at sequences `2754-2758`, which means the prompt hardening is live but it exposed a deeper remaining gap: prompt-only steering is no longer enough for this repeated task-34 child-reread loop.
- 2026-03-27 19:16:18 MDT - Committed the PM child-reread prompt hardening as `e49ae9a6` (`Guide PM continuations away from child rereads`). The next live step is now clear: this family has crossed from “missing prompt context” into “repeated ignored guidance,” so the next high-leverage fix will need runtime memory or a stronger cross-turn guard, not just more prompt text.
- 2026-03-27 19:40:42 MDT - Re-read the same hot Sam.blog PM session and confirmed another structural reason the task-34 loop stayed alive after the prompt-only hardening: the project lane still classified decomposed draft parents as “actionable drafts” unless one of their children was actively executing. Live proof before the fix stayed session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, where worker-generated continuation message `2894` still said `There are 1 remaining draft project tasks...` for task 34 even though child tasks 35 and 36 already existed and were both `blocked`.
- 2026-03-27 19:40:42 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) and [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so project continuations now treat any draft parent that already has child tasks as non-actionable PM work. That change now applies consistently to the project snapshot builder, worker-side remaining-draft counts, worker-generated continuation prompts, and `nextRunnableDraftProjectTask(...)`, so decomposed parents stop showing up as the focus draft or as auto-queue candidates.
- 2026-03-27 19:40:42 MDT - Updated focused coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) and [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go) to prove the exact blocked-child live shape: draft parents with existing child tasks are now summarized under the separate child-work line, excluded from the actionable-draft line/focus slot, excluded from worker remaining-draft counts, and skipped by `nextRunnableDraftProjectTask(...)`. Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 19:40:42 MDT - Live proof for the new prompt shape is still pending the next post-restart PM continuation. The currently claimed session turn (`232`) and its trigger message `2894` were created before the `19:38 MDT` restart, so the hot session is still draining old prompt state; the first fresh worker-generated continuation on the new runtime should show whether task 34 drops out of the actionable-draft bucket in production.
- 2026-03-27 19:54:35 MDT - Re-read the hottest remaining review-task efficiency family after the PM continuation fixes and confirmed the next deterministic waste was same-batch sibling `file.read` churn after the preferred deliverable target was already in the tool batch. The late placeholder follow-on guard only activates after `appendToolResults(...)` sets `placeholderTargetSeen`, so it could not stop the extra sibling reads in the same assistant batch that already read `templates/layout-10-conversational.html`.
- 2026-03-27 19:54:35 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so async task review batches now pre-block sibling `file.read` calls whenever the same batch already includes a `file.read` for the preferred deliverable target named in the review prompt. I kept the later placeholder follow-on guard in place for cross-turn retries, but added a new batch-level guard path plus reject-specific messaging so the review lane cannot pay same-turn sibling read misses before the preferred-target result lands.
- 2026-03-27 19:54:35 MDT - Added focused unit coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for the exact task-24 shape: the preferred deliverable read remains allowed, a sibling planning-file `file.read` in the same batch is blocked with `flow.review_decision reject` guidance, and the guard stays inactive when the batch does not include the preferred target.
- 2026-03-27 19:56:17 MDT - Verified the slice with `go test ./internal/turn -run 'Test(ShouldBlockTaskReviewPreferredDeliverableSiblingReadTool|ShouldBlockTaskPlaceholderDeliverableFollowOnTool|ShouldBlockTaskPlaceholderDeliverableFollowOnToolForReview|BuildTaskReviewActionPromptIncludesPreferredDeliverableTarget)$' -count=1`, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 19:56:17 MDT - Fresh live proof landed immediately on task-24 review session `5dc4b206-2fa7-477c-878a-912d8008a613`. The new post-restart turn `5ea4d212-0811-4bad-8edb-d8c09be6df0c` (messages `13-17`) went straight from a single preferred-target read of `templates/layout-10-conversational.html` to a reject decision, with no sibling `file.read not_found` results in that turn. The prior validation-loop churn in the same session (`73eab8e7-bbe6-4b7d-9d59-c848bfffcf19`) is now clearly the pre-fix shape, and the latest turn shows the cheaper post-fix review path in production.
- 2026-03-27 19:57:31 MDT - Committed the same-batch review sibling-read guard as `725f9251` (`Block same-batch review sibling reads`).
- 2026-03-27 20:14:18 MDT - Re-read the hottest remaining PM loop in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` and confirmed the waste had moved from broad tool selection to worker re-arming: minute by minute, `RequeueActiveProjectSessionsWithoutTurns(...)` was minting a fresh consumed `project_execution_continuation` for the same `completed_task_id` `5e0cf069-62d6-4514-ac7c-2295bb087cd2`, then the next PM turn immediately ended `validation_loop_blocked` on the same rediscovery guard again.
- 2026-03-27 20:14:18 MDT - Patched [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so worker-authored project continuations persist a `continuation_snapshot_fingerprint`, reuse an existing pending PM continuation only when both the latest completed task and snapshot fingerprint still match, and suppress a consumed continuation outright when the latest terminal turn for that same fingerprint already ended in the known `Project continuation rediscovery guard blocked only broad rereads...` path. While widening the continuation suite, I also fixed the adjacent bootstrap misroute so an active-bootstrap session with a stale `project_execution_continuation` and zero open tasks refreshes back to `project_bootstrap` instead of being retired as settled.
- 2026-03-27 20:14:18 MDT - Verified the worker slice with focused and widened integration coverage in [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go), rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 20:16:21 MDT - Live proof landed on the exact hot PM session. After the restart, the worker refreshed the old unfingerprinted continuation into new message `03fe591e-fb34-4102-b7fd-bcc40a9c603b` with fingerprint `17f9ed35f2a25ad1`; that fresh PM turn `e4da90de-41b0-458c-951e-78e6f2d08abf` still ended `validation_loop_blocked` at `20:15:14 MDT`, but the next worker requeue pass did not mint another continuation. Instead, message `03fe591e-fb34-4102-b7fd-bcc40a9c603b` was failed in place with `suppressed repeated identical project continuation after rediscovery-only validation block`, and the session was left with `0` pending/claimed `agent_turn` jobs.
- 2026-03-27 20:34:12 MDT - Re-read the hottest remaining first-turn review waste after the PM suppression slice and isolated a prompt-target derivation gap. Task 24’s first review prompt still omitted the preferred deliverable because the explicit-path extractor only recognized `Deliverable:` / `Output:` labels and missed `Save as templates/layout-10-conversational.html.`; task 11’s first review prompt omitted the preferred deliverable because a fresh review session could not reuse concrete deliverable paths already discovered in earlier sessions for the same task. Impact: the first review turn on those lanes still paid for broad `task.get` / `file.list` / git rediscovery, and only the retry prompt got narrow enough to reject cheaply.
- 2026-03-27 20:34:12 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so explicit deliverable extraction now recognizes `Save as ...` descriptions, strips trailing punctuation from extracted paths, and lets review/task target derivation fall back to concrete deliverable evidence from earlier sessions on the same task when the current session is still cold. Added focused unit coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for the `Save as ...` review prompt shape and an integration regression in [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go) proving a fresh review session inherits a deliverable target from prior same-task history. Verified with `go test ./internal/turn -run 'Test(BuildTaskReviewActionPromptIncludesPreferredDeliverableTarget|BuildTaskReviewActionPromptIncludesPreferredDeliverableTargetFromSaveAsDescription|ShouldBlockTaskReviewPreferredDeliverable(SiblingReadTool|FirstTool)|ShouldNotBlockTaskReviewPreferredDeliverableFirstToolForOrchestrationParent|ShouldBlockTaskPlaceholderDeliverableFollowOnTool|ShouldBlockTaskPlaceholderDeliverableFollowOnToolForReview)$' -count=1` and `go test -tags=integration ./internal/turn -run 'TestBuildTaskReviewActionPromptFallsBackToHistoricalTaskSessionDeliverableTargetIntegration$' -count=1`.
- 2026-03-27 20:34:31 MDT - Rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 20:35:31 MDT - Fresh post-restart live proof landed on both hot first-review families. Task 11 session `a81b0d92-ab02-4315-8ea9-d6afdc83e605` and task 24 session `a8597739-727d-4e64-8528-fadbc2acaacc` both received first review prompt sequence `3` with `Start with the preferred deliverable target ...` on the very first turn. In the same turns, the assistants immediately read the preferred targets (`content/technonymous/stop-preparing-your-kids-for-jobs.md` and `templates/layout-10-conversational.html`) and rejected from that direct evidence instead of spending a broad discovery turn first.
- 2026-03-27 20:54:25 MDT - Re-read the hot Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` after the first-review target fix and pinned the remaining continuation churn to worker recovery, not turn-engine retry. Live proof before the fix: PM turns `272-277` all had `retry_count=0`, messages `3159`, `3165`, and `3173` carried the same `completed_task_id` `5e0cf069-62d6-4514-ac7c-2295bb087cd2` plus identical fingerprint `17f9ed35f2a25ad1`, and the session still had two ancient pending `project_bootstrap` user messages at sequences `1` and `35` even though bootstrap had long been completed. That meant `RequeueActiveProjectSessionsWithoutTurns(...)` could keep waking on stale bootstrap roots and re-minting the same PM continuation after each `validation_loop_blocked` rediscovery stop.
- 2026-03-27 20:54:25 MDT - Patched both continuation seams. In [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go), worker-side continuation refresh now checks the latest matching `project_execution_continuation` fingerprint even when the prior message is already failed, suppresses a replay if that exact fingerprint most recently ended `validation_loop_blocked` on the PM rediscovery guard, and retires stale pending `project_bootstrap` messages once bootstrap is already complete so they stop acting as requeue roots. I kept the turn-engine-side suppression in [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) as a second guard for repeated generic project-continuation retries that rebuild the same prompt from the same completed task and snapshot. Verified with `go test -tags=integration ./internal/jobqueue -run 'TestJobWorker(EnsureProjectContinuationMessageSuppressesRepeatedConsumedRediscoveryBlockedContinuation|RequeueActiveProjectSessionsWithoutTurnsSuppressesRepeatedFailedRediscoveryBlockedContinuation)$' -count=1`, `go test -tags=integration ./internal/jobqueue -run 'TestJobWorker(RequeueActiveProjectSessionsWithoutTurns(SuppressesRepeatedFailedRediscoveryBlockedContinuation|PrefersExecutionKickoffOverNewerSupervisorRecovery|CreatesMissingTaskQueueKickoff|ForTaskQueueKickoff)|ClaimPendingAgentTurns(PrefersOldestDueTaskSessionAcrossSessions|PrefersNewestProjectContinuation)|PurgeStaleAgentTurnJobs(PrefersExecutionKickoffOverNewerSupervisorRecovery))$' -count=1`, `go test ./internal/turn -run 'Test(HandleCompletedProjectExecutionContinuationTurnRetriesGenericReplyWithFreshMessage|HandleCompletedProjectExecutionContinuationTurnSuppressesRepeatedRediscoveryBlockedRetry|BuildTaskReviewActionPromptIncludesPreferredDeliverableTarget|BuildTaskReviewActionPromptIncludesPreferredDeliverableTargetFromSaveAsDescription|ShouldBlockTaskReviewPreferredDeliverableFirstTool|ShouldBlockTaskReviewPreferredDeliverableSiblingReadTool)$' -count=1`, and `go test -tags=integration ./internal/turn -run 'TestBuildTaskReviewActionPromptFallsBackToHistoricalTaskSessionDeliverableTargetIntegration$' -count=1`.
- 2026-03-27 20:59:47 MDT - Rebuilt `./bin/ottercamp`, replaced tmux session `codex-e2e-20260324` cleanly on the new binary, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 20:59:47 MDT - Fresh live proof landed on the exact hot PM session. After the restart, one post-restart continuation message `3175` (`a7fac362-573c-4e74-a308-332a660164b5`) ran once and again ended `validation_loop_blocked` in turn `277` at `20:56:35 MDT`; after the next worker cycle, message `3175` was failed in place with `suppressed repeated identical project continuation after rediscovery-only validation block`, stale bootstrap roots `35` and `1` were both failed with `project bootstrap already complete; continuing project execution`, and the session held `0` pending `project_execution_continuation`, `0` pending `project_bootstrap`, and `0` pending/claimed `agent_turn` jobs. That is the new steady state: the PM loop is no longer rearmed by stale bootstrap roots once the repeated rediscovery stop has already fired on the same continuation fingerprint.
- 2026-03-27 21:22:56 MDT - Traced the next review-efficiency leak to blocked-task recovery rather than the review lanes themselves: Sam.blog tasks 11 and 24 were correctly blocking on `flow rejection max visits exceeded`, but supervisor recovery could still reconsider them because the durable checkpoint repair path treated that blocker as resumable whenever the deliverable file contained reviewer rejection text.
- 2026-03-27 21:22:56 MDT - Patched [`internal/task/service.go`](/Users/sam/dev/otter-camp/internal/task/service.go) and [`internal/task/recovery_resume.go`](/Users/sam/dev/otter-camp/internal/task/recovery_resume.go) so explicit runtime `flow_rejection_max_visits` / `flow.rejected max_visits_exceeded` evidence now short-circuits `ResumeValidationBlockedTask(...)` before checkpoint repair or legacy scaffold recovery can run; the old legacy flow-rejection scaffold repair path still remains available for manually blocked historical tasks that do not carry the runtime flow-rejection signals.
- 2026-03-27 21:22:56 MDT - Patched [`internal/controlplane/supervisor.go`](/Users/sam/dev/otter-camp/internal/controlplane/supervisor.go) so `detectResumableBlockedTasks(...)` skips blocked tasks whose latest blocked/flow-rejected events already mark explicit flow-rejection max-visits exhaustion, which removes repeated supervisor scans and active-execution abandonment attempts on terminal review-exhaustion tasks.
- 2026-03-27 21:37:08 MDT - Pulled a fresh live efficiency snapshot after the review max-visits fix and traced the next stranded-work family to worker recovery. Sam.blog tasks 16, 21, and 23 were all still `in_progress` with active `flow_node_execution` rows, latest `task_recovery_resume` turns completed as `validation_loop_blocked`, and zero pending or claimed `agent_turn` jobs. Root cause: worker recovery was treating any positive-byte `file.write` in a consumed recovery-resume turn as terminal success, even when the same turn had actually halted on validation churn.
- 2026-03-27 21:37:08 MDT - Patched [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so both active-execution requeue and stale-job purge only suppress a consumed `task_recovery_resume` when the completed turn both wrote bytes and finished cleanly with no `stop_reason`. Validation-loop-blocked recovery turns can still contain a positive-byte `file.write`, so they now stay eligible for fresh requeue instead of being stranded forever.
- 2026-03-27 21:37:08 MDT - Added focused worker integration regressions in [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go) for both sides of the seam: requeue now preserves `validation_loop_blocked` recovery-resume writes, and stale-job purge no longer dead-letters those retries. Verified with the focused four-test slice plus the adjacent active-execution/purge cluster, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-27 21:37:08 MDT - Fresh live proof landed immediately after restart. The previously stranded task-23 session `f9fbfadb-3aba-41ab-a914-08537f1b164e` gained a new pending `agent_turn` at `2026-03-27 21:36:39 MDT` for retry `1` on `task_recovery_resume` message `56d423f0-efd1-402e-bf32-22be85dad9e7`, and task 16 advanced out of its stranded recovery lane into a new active session instead of remaining parked with zero runnable work.
- 2026-03-27 21:22:56 MDT - Added focused integration coverage in [`internal/task/service_integration_test.go`](/Users/sam/dev/otter-camp/internal/task/service_integration_test.go) for the exact live polluted-deliverable case and in [`internal/controlplane/supervisor_integration_test.go`](/Users/sam/dev/otter-camp/internal/controlplane/supervisor_integration_test.go) for the supervisor-side “stay blocked / do not abandon execution” behavior, then reran those focused integration slices, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed health stayed `ok`.
- 2026-03-27 21:22:56 MDT - Live proof on the new runtime: after restart plus the 30-second supervisor grace window, Sam.blog tasks 11 (`7733f4c5-b39e-4ff9-9c42-c443099c142f`) and 24 (`8bf202d4-54c6-4fff-93f4-aa0f0a43df9c`) both remained `blocked` at their prior `updated_at` timestamps, and their `resume_validation_blocked_task` event counts stayed flat at `181` and `161` instead of incrementing again.
