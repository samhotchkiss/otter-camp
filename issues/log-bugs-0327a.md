# 0327a Bug Log

## 2026-03-29 14:08 MDT

- Bug: shared-file child recovery lanes were still allowed to loop on `file.read not_found` when the inherited parent file did not exist yet.
- Shape: the runtime already blocks those child lanes from replacing the whole shared file with `file.write`, but a missing inherited file still looked like a normal recovery discovery miss, so tasks `164-166` could keep reopening recovery turns on `planning/sambot-example-conversations.md`.
- Correct behavior: once a shared parent deliverable is still missing, the child lane should stop and hand work back to the parent / write-owning replacement path instead of continuing bounded recovery in a file that is not there.
## 2026-03-29 13:45 MDT

- Bug: malformed duplicate shared-file child tasks could still loop across turns after the runtime had already learned they were invalid.
- Shape: kickoff preflight would block a fresh duplicate child session, but if the lane first reached `validation_loop_blocked`, `enqueueTaskValidationBlockedContinuationPrompt(...)` still emitted another synthetic recovery prompt instead of terminating the malformed child.
- Live canary: SamBot task `155` under parent `154` (`planning/sambot-example-conversations.md`) kept generating recovery resumes despite matching the duplicate shared-file child classifier.
- 2026-03-29 13:11:45 MDT - Markdown-emphasized deliverable labels in task descriptions could still poison PM continuation deliverable hints. Fresh live evidence: draft task `154` (`Write SamBot Example Conversations (replacement)`) had description `**Deliverable:** \`planning/sambot-example-conversations.md\``, but the project continuation snapshot at `13:08:43 MDT` surfaced `deliverable_path=SamBot`. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only matched bare `Deliverable:` / `Output:` labels, so `explicitDeliverablePath(...)` missed markdown-emphasized labels and fell back to the leading title token extracted by `leadingVerbDeliverablePathPattern`. Impact: the later `file.read planning/sambot-example-conversations.md -> not_found` was treated as a generic missing artifact instead of the focused bounded-size parent’s own missing deliverable, which bypassed the already-written “keep bounded-size context when focused deliverable is missing” path.
- 2026-03-29 13:00:36 MDT - Even after same-turn rediscovery retries were cut off, bounded-size PM retries could still ignore the tool’s own decomposition hint and recreate one still-too-broad child on the next turn. Fresh live evidence: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` reached turns `15903934-a89d-45f7-8eed-4187be9c3b0e` and `8e86ea62-823d-4ba3-a778-4b4e189db58b`, each of which narrowed cleanly to `task.list(parent_task_id=146)` plus a spec reread, then attempted another single replacement child and hit `task exceeds bounded size policy` again. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) preserved only generic “split the parent” coaching across bounded-size PM retries and discarded the `suggested_decomposition.child_titles` already returned by the failing `task.update` / `task.create` tool result. Impact: the PM lane kept re-proposing one broad architecture-doc child instead of using the concrete multi-child split the runtime had already suggested.
- 2026-03-29 12:55:14 MDT - PM continuations could still waste an entire turn on serial blocked rediscovery retries even after the “pure blocked batch” guard was already in place. Fresh live evidence: Sam.blog PM turn `bd90604f-7b72-48d5-bef4-5f2977176245` on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` first hit blocked `file.read(planning/sambot-feature-spec.md)`, then blocked `task.list(parent_task_id=140)` against a named leaf task, then blocked broad `task.list(project_id=...)`, all in the same turn. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only ended the turn when a single blocked batch contained two or more rediscovery failures at once; it did not look at cumulative blocked rediscovery results already recorded earlier in the current PM turn. Impact: the PM lane could still burn repeated model retries inside one continuation turn instead of cutting over immediately after the second blocked reread signal.
- 2026-03-29 05:09:00 MDT - Bounded-size PM retries could still lose their decomposition contract after reading the exact missing deliverable path. Fresh live evidence: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` emitted the correct bounded-size retry `6518` for task `84`, but once the PM lane read `templates/template-08-replace.html` and got `not_found` (`6524-6525`), the next continuation `6526-6529` fell back into the generic missing-prerequisite queue path and immediately tried to queue task `84` again, re-triggering the same bounded-size stop. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) treated “missing focused deliverable” the same as “missing prerequisite artifact from another task,” even when the current prompt already proved the focused draft parent was too broad and needed decomposition instead.
- 2026-03-29 04:58:00 MDT - PM bounded-size supervisory stops could still leave the project session idle whenever the same turn also contained a successful mutating tool result. Fresh live evidence: Sam.blog PM turn `65888993-5fdb-45bc-8681-244bd949908f` ended with bounded-size stop `6517` for draft task `84`, but because the turn also successfully created or updated draft work before that stop, [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) hit `turnHasSuccessfulMutatingToolResult(...)` first and exited before the new bounded-size follow-on logic could run. Impact: the PM lane could correctly detect “this draft is still too broad” yet still drain idle instead of emitting the intended focused split retry.
- 2026-03-28 21:23:50 MDT - Worker-side repeated project-continuation suppression did not yet recognize the new PM/task-lane boundary stop family. Fresh live evidence: after the `3493` engine patch proved itself with Sam.blog PM boundary-stop system messages at sequences `5451` and `5454`, worker-generated continuation message `5455` still appeared immediately afterward with the same completed task (`75`) and same project-continuation shape. Root cause: [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) only treated rediscovery, active-replacement, and bounded-size `validation_loop_blocked` system messages as suppressible duplicates. Impact: even though the engine now knew the PM lane should leave the active task-owned lane alone, the worker could still reopen another identical PM continuation from the same snapshot fingerprint.

- 2026-03-28 21:18:17 MDT - Project continuations could still bounce on task-lane ownership errors after correctly identifying closeout-ready work. Fresh live evidence: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` reached assistant `5421`, recognized that the Technonymous recovery outputs existed and that the remaining step was to close blocked/draft tasks, then hit `task.update -> task_lane_owned_by_project_task_session` at tool result `5422`. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) treated that boundary error as a same-turn validation stop but did not emit a dedicated PM/task-lane boundary message and did not mark the completed continuation as handled when no other draft work was runnable. Impact: the PM lane could fall through to later project-completion logic and reopen another continuation even though the correct action was to leave the active `project_task` lane alone.

- 2026-03-28 20:13:29 MDT - The `non_substantive_content` recovery loop still lost the concrete target path and correction context before it reached the turn-level validation guard. Fresh live evidence: Sam.blog task `71` session `412b907c-9810-4f8c-bc3d-028cb00ab488` fetched the real article body at sequence `159`, then hit `file.write` `non_substantive_content` twice at `161` and `163`; the repeated-turn stop at `164` only surfaced the bare code, and the very next retry opened with `Let me use cli_execute with python3 to write the file...` at `166`. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) classified `non_substantive_content` from the raw error code alone and dropped both the target `path` argument and the richer tool message from native `file.write`. Impact: the runtime recognized the validation loop but still gave the next continuation too little context, so the lane pivoted into a worse tool choice instead of writing the deliverable body directly.

- 2026-03-28 20:08:17 MDT - Even after recovery-target sibling reads were blocked, the target file itself could still be a batch-inventory stub that native `file.read` treated as ordinary content. Fresh live evidence: post-restart Sam.blog task `71` session `412b907c-9810-4f8c-bc3d-028cb00ab488` read the preferred target at sequence `150`, and the returned body was `All 35 files exist. Posts 1-12 (0-indexed entries 0-11) are: ...` instead of real markdown or a `placeholder_deliverable` stop. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) only classified review-assessment/status prose that mentioned `.md` / `content/posts` explicitly, so batch inventory stubs listing slugs by numbered lines slipped through. Impact: execution lanes still spent one extra target reread before the new sibling-read guard redirected them back to the same file.

- 2026-03-28 20:02:12 MDT - Content-migration recovery still allowed execution lanes to roam the deliverable root after recovery had already named a concrete target. Fresh live evidence: Sam.blog task `71` session `456e188a-fda1-4832-9ac3-103bd72f1e77` kept listing `content/posts` (`file.list` at sequences `202` and `226`) and rereading markdowns under that root (`file.read` at `180`, `214`, and `228`) even though the recovery target remained `content/posts/stop-preparing-your-kids-for-jobs.md`. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) still let checkpointed content-migration tasks fall through to `taskAllowsPerItemDeliverableInspection(...)` / `taskAllowsBatchRecoveryRootInspection(...)`, and `allowRecoveryCheckpointArtifactInspectionWhenTrackedOutputsComplete(...)` also treated any path inside the deliverable root as allowed once tracked outputs existed on disk. Impact: reopened execution lanes could keep paying for root listings and sibling markdown rereads instead of staying pinned to the named target file.

- 2026-03-28 19:45:29 MDT - Placeholder deliverable rejection was still too narrowly gated for in-progress task execution lanes. Fresh live evidence: after the PM lane reopened task `71`, session `456e188a-fda1-4832-9ac3-103bd72f1e77` read `content/posts/stop-preparing-your-kids-for-jobs.md` at sequence `7`, and the tool result returned the exact self-assessment stub body (`Now I have all 12 existing files read. Let me assess their quality...`) as ordinary success even though the path was the real deliverable. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) only ran `rejectPlaceholderDeliverableRead(...)` for `execution_first` plan metadata or `work_status=review`; task `71` was `in_progress` with an explicit deliverable path from its description, so the placeholder classifier never ran. Impact: reopened execution lanes could still treat stub markdown as source material and keep re-scraping/reassessing instead of overwriting the placeholder directly.

- 2026-03-28 19:41:42 MDT - Project continuations could still treat terminally blocked review lanes as resumable review work when stale validation-guard metadata lingered on the task. Fresh live evidence: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` emitted project continuation `4840`, then assistant `4841` kept centering blocked review-family task `65`, hit rediscovery guards at `4842-4845`, and finally logged `[Project continuation found blocked review lane task 65 ... runtime resume is still blocked by flow_rejection_max_visits ...]` at `4846` before the project session went idle with no pending continuation/job. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) let `review_decision_required` validation-guard metadata override the newer blocked reason `flow rejection max visits exceeded`. Impact: PM snapshots and blocked-task selection kept steering the project session back toward a permanently non-resumable review lane instead of moving on to the next bounded repair step.

- 2026-03-28 19:29:08 MDT - Async task reviews could still treat AI self-assessment markdown stubs as real deliverables. Fresh live evidence: task `71` review session `8ee9e741-6b61-4348-9d16-c14420506971` read preferred target `content/posts/stop-preparing-your-kids-for-jobs.md`, and the file body started with `Now I have all 12 existing files read. Let me assess their quality...`; because native `file.read` returned ordinary success rather than `placeholder_deliverable`, the lane kept sampling sibling posts and the follow-on session `67348655-43d0-4330-9b55-d462d60d29e2` escalated into `web.fetch` of source articles. Impact: review burned same-turn sampling budget and cross-turn fetch budget on a stub markdown file instead of rejecting immediately from placeholder evidence.

- 2026-03-28 19:18:27 MDT - Blocked async task sessions could still strand their newest synthetic `task_recovery_resume` after the live execution had already been marked terminal. Fresh live evidence: Sam.blog task `70` session `30a79813-2880-4ae1-be7f-2dd76623f193` had `current_turn_id=NULL`, zero pending/claimed `agent_turn` jobs, latest unconsumed recovery-resume message `23b53aaf-5411-4571-9953-972f2e2531d6`, and latest `flow_node_execution` `b46596fb-c15e-402f-8fa0-fcdb13dabe4b` already `abandoned`; `RequeueActiveExecutionSessionsWithoutTurns(...)` only scanned `status='active'` executions, so the session never requeued onto the new binary. Impact: the checkpoint-first recovery/write-root hardening was deployed, but blocked recovery sessions could still go fully idle before they ever got the next corrected turn.

- 2026-03-28 15:33:49 MDT - Preferred-target review lanes could still reread the same deliverable from byte `0` after a full successful read in the same turn. Fresh live evidence: task `65` review session `74b1d8c8-5ad1-4697-9a99-2a668754ccb0` read `content/posts/yonderosa-and-the-art-of-having-less.md` successfully, then reread that same file again instead of using the existing evidence or taking one bounded adjacent check. Impact: once the target was proven readable, review still wasted same-turn budget on duplicate target reads and could block before reaching `flow.review_decision`.

- 2026-03-28 15:29:49 MDT - Root-only batch reviews could still turn one successful deliverable-root listing into “read the whole batch.” Fresh live evidence: Sam.blog task `65` review session `74b1d8c8-5ad1-4697-9a99-2a668754ccb0` listed `content/posts/`, observed `35` files, then immediately tried to read the first 12 post files in parallel and fell into `max_tool_calls` before issuing `flow.review_decision`. Impact: batch reviews without an authoritative named output set were still paying for full reread fans after the root listing had already proven presence/count.

- 2026-03-28 15:22:56 MDT - Async task turns were still paying for a third identical successful `file.write` before the runtime stopped same-turn churn. Fresh live evidence: Sam.blog task `38` session `fa67bafc-f60e-4eb4-9bde-daca10bb0bde`, turn `47511c0f-1869-4107-8e4e-6d1d72953aa2`, wrote `templates/template-07-dark-mode-developer.html` twice successfully at `byte_size=476` (`sequence 17` and `21`) and then continued narrating another mutation step instead of ending the turn. Impact: productive-but-duplicate task writes were still wasting one extra model/tool hop in the most common “rewrite the same deliverable again” family.

- 2026-03-28 15:18:00 MDT - PM continuations were still reopening malformed blocked review children. Sam.blog project session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` repeatedly resumed blocked review lane task `45` even though task `45` was already a malformed procedural child under task `44`. Impact: the project lane kept burning PM turns on a permanently invalid child review lane instead of moving on to real draft work or replacement tasks.

- 2026-03-28 15:07:00 MDT - Authoritative batch review lanes were still oversized even after capping the sibling count. Live task `64` review turns were reading the preferred target plus four sibling outputs at `4096` bytes each, then hitting `[Prompt input exceeded 20000-token guardrail - continuing in a new turn.]` before they could call `flow.review_decision`. Impact: review stopped trying to read the whole batch, but it still paid for repeated prompt-compression/retry loops because each allowed sibling sample was too large.

- 2026-03-28 14:05:00 MDT - Async malformed procedural-child preflight was too narrow for plain-English browser-navigation artifact tasks. Sam.blog task `45` (`Use browser tools to navigate to https://technonymous.org`) is a review-only procedural child under parent task `44`, but the classifier only matched `cli_execute`, `browser_...`, and a few other explicit tool markers. Impact: review lanes for this malformed child shape could still reach the model, spend turns on `task.get` / git inspection, and then fall out through `max_tool_calls` instead of blocking immediately.

- 2026-03-28 13:19:42 MDT - Task `64` review lanes were still oversampling authoritative batch outputs after the preferred-target read. Fresh live evidence: session `9e4efe39-c23a-43eb-af52-0be8a140a934` read `content/posts/practice-dont-own-things-you-have.md`, then kept reading sibling outputs from the same `content/posts` batch until `max_tool_calls`; session `5a0c5569-b5f0-4ac9-ac55-c73eea9aea73` had already burned `2.87M` tokens in the same family. Impact: bounded `max_bytes` helped, but review still paid for too many same-turn sibling spot checks because the runtime never told the lane when “enough sample evidence” had been gathered.

- 2026-03-28 12:58:34 MDT - Task `35` (`Scrape batch 1: first 5 technonymous.org posts to markdown`) was still escaping task-lane isolation through `cli.execute`. The engine correctly rooted shell execution in `/Users/sam/otter-data/task-worktrees/sam-blog-rebuild-restart-12/task-35`, but the model used absolute parent-workspace paths such as `/Users/sam/otter-data/workspaces/sam-blog-rebuild-restart-12/content/technonymous-index.json` and `/Users/sam/otter-data/workspaces/sam-blog-rebuild-restart-12/content/posts/...` inside the command string. Result: native file tools saw stale placeholder content in the task worktree while CLI reads saw merged parent-workspace content, creating a split-brain recovery loop.

- 2026-03-28 05:37:16 MDT - Legacy malformed child task lanes under explicit no-decompose parents could still spend full async task turns if they were reopened. Live Sam.blog shape: parent task `49` said `Do NOT decompose this task into subtasks. Execute it directly in a single session.`, yet child tasks `50-54` still existed from the older decomposition bug, including task `54` titled `import os, json`. Impact: if any of those child sessions were re-kicked or recovered again, they could still burn prompt assembly + model/tool budget even though the runtime already knew the parent contract made the child lane invalid.
- 2026-03-28 05:37:16 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so async task sessions now preflight malformed no-decompose children before prompt assembly: the lane emits one system correction, marks the child blocked, sets `validation_loop_blocked`, and completes the turn without a model call.
- 2026-03-28 05:37:16 MDT - Added focused regression coverage in [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go) proving the Sam.blog-shaped `task_queue_processor` kickoff for `import os, json` halts with `modelCalls=0`.
- 2026-03-28 05:37:16 MDT - Verification passed for the targeted unit + integration slice. One adjacent older bootstrap integration test is still red on a stale expectation (`missing_assignments` vs `first_wave_execution_missing`); that fixture drift predates this malformed-child preflight work and remains separate.
- 2026-03-28 05:37:16 MDT - Live note: at patch time, the known Sam.blog malformed child tasks already had `0` active sessions and `0` live `agent_turn` jobs, so the direct benefit is preventing the next reopen/recovery replay rather than changing a currently running lane.
- 2026-03-28 04:14:46 MDT - Passive blocked project tasks were still suppressing PM continuation wakeups after a later task completed. Fresh live proof: Sam.blog project session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` had draft tasks `34/39/43/44`, task `48` completed at `03:58:33 MDT`, `current_turn_id` was `NULL`, there were zero pending/claimed `agent_turn` jobs, and the newest `project_execution_continuation` message was still the old task-47 retry from `02:50 MDT`. Impact: the PM lane could go fully idle after a real task completion whenever the remaining project tree still contained passive `blocked` tasks.
- 2026-03-28 04:14:46 MDT - Worker recovery also had no path to heal that idle-project shape once the completion event had already been missed. It only requeued project sessions that already had a pending `project_execution_continuation` or `project_bootstrap` message, so an active project session with draft work but no pending continuation message stayed stalled indefinitely.
- 2026-03-28 04:14:46 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so completed-task PM wakeups ignore passive `blocked` tasks and only treat `queued` / `in_progress` / `review` tasks as active blocking work.
- 2026-03-28 04:14:46 MDT - Patched [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so startup/periodic recovery now synthesizes a missing `project_execution_continuation` for idle active project sessions with completed bootstrap, remaining draft work, no live task executions/sessions, and no pending continuation message/job.
- 2026-03-28 04:14:46 MDT - Added focused regression coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) and [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go), rebuilt/restarted the runtime, and confirmed health stayed green.
- 2026-03-28 04:14:46 MDT - Fresh live proof after deploy: startup recovery appended message `91c49623-dc8a-40a3-8158-cd63ee017ba3` with `completed_task_id=a0d4b707-3bb6-4577-bb5e-d1e4ba49b38a` (task `48`) at `04:13:53 MDT`, executed PM turn `984d9f13-c56d-4a9f-ad71-581dfa0b0c95`, and advanced the lane into the next real bounded-size stop for draft task `43` instead of leaving the session idle.
- 2026-03-28 01:23:16 MDT - Recovery-mode empty `cli_execute` still had a cross-turn loophole after the earlier ordinary-task guard. Live proof before the fix: Sam.blog task `42` session `cc3576a4-ec4a-4975-b823-23a5c6597946` hit `[Repeated identical successful file.write churn ... templates/template-08-minimalist-sticky.html ...]` at message `16`, then the very next recovery retry emitted another empty `cli_execute` and was still allowed to keep working from the same recovery target. Impact: once the runtime already knew the empty-command fallback was only replaying identical file content, the next recovery retry could still burn another turn before stopping.
- 2026-03-28 01:23:16 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so recovery-mode empty `cli.execute` halts immediately when the recovery checkpoint already records repeated successful identical `file.write` churn for the same target path.
- 2026-03-28 01:23:16 MDT - Added focused unit coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), reran the targeted recovery + child-lane test slice successfully, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `data.status=ok`.
- 2026-03-28 01:23:16 MDT - Fresh live proof is still pending because the hot template session had already finished before the deploy. The new guard is live and test-green, but the next natural recovery turn with this exact prior checkpoint shape has not fired yet on the new binary.
- 2026-03-28 01:16:56 MDT - Sam.blog task `40` (`Navigate to technonymous.org using browser tools`) was still overspending after the earlier child-lane prompt fix because the runtime allowed the discovery child lane to escalate from archive listing work into `/p/...` full article-body fetches. Live proof before the fix: session `bcbc53cd-99fa-476f-a50c-b4ba26dd0b7e` went `browser.navigate -> browser: not found`, then `web_fetch https://technonymous.org`, then `web_fetch https://technonymous.org/archive`, then nine `/p/...` detail-page fetches in the same turn before stopping at `max_tool_calls`.
- 2026-03-28 01:16:56 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so the sibling-responsibility guard now blocks discovery-lane detail-page fetches when a sibling child task already owns the write/output lane. Root/homepage and `/archive` listing fetches remain allowed; `/p/...` detail fetches are blocked unless the task brief explicitly asks for full content bodies.
- 2026-03-28 01:16:56 MDT - Added focused unit coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), reran the targeted turn-engine slice successfully, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `data.status=ok`.
- 2026-03-28 01:16:56 MDT - Fresh live proof is still pending traffic. The deployment is live and test-green, but task `40` and task `41` did not replay in the immediate post-restart watch window, so the remaining gap is just waiting for the next natural discovery child continuation on the new binary.
- 2026-03-28 00:40:17 MDT - The first PM follow-up after a dependency-miss stop could still reopen duplicate PM work even when replacement work for that missing artifact was already active. Live diagnosis before the fix: in Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, the fresh follow-up continuation at `3347-3352` tried to rediscover `OC-14` as the producer of `content/technonymous-index.json`, while the project tree already contained replacement task `39` and active child tasks `40/41` covering that same prerequisite. Impact: the runtime could correctly convert a dependency-miss stop into a follow-up continuation, but that continuation still wasted PM turns rediscovering producer ownership that the live task tree had already resolved.
- 2026-03-28 00:30:16 MDT - PM continuation dependency-miss stops were ending the expensive archive/homepage crawl loop, but they still left the project session idle instead of converting that exact missing-artifact evidence into one bounded follow-up continuation. Live proof before the fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` reached sequence `3334-3339`, read `content/technonymous-index.json -> not_found`, emitted `[Project continuation found that prerequisite artifact ...]`, and then had zero fresh continuation messages or pending/claimed `agent_turn` jobs. Impact: the runtime could correctly identify the missing prerequisite, but PM automation still stalled immediately afterward instead of taking the next orchestration step.
- 2026-03-27 22:08:07 MDT - Task-lane draft rejection was still allowing a specific troubleshooting prose family to count as a reusable deliverable draft: “the file currently contains the assistant’s prose text ... the `file.write` calls without `content` ... writing empty/chat text”. Live proof before the fix: task 37 sessions `08fe54d5-f4e2-4459-8053-a812fcae5be8` and `7c7666c7-640a-450a-8dde-b1aed1a3ac7a` repeatedly turned empty `cli_execute` retries into `file.write` executions that re-saved that narration at `byte_size=231`. Impact: the engine kept spending turns and tool calls re-writing the same troubleshooting prose into the deliverable instead of forcing a narrower correction or a direct reject path.
- 2026-03-27 21:54:20 MDT - `task_queue_processor` kickoff dedupe only inspected the oldest 200 chat messages in a session because [`internal/chat/service.go`](/Users/sam/dev/otter-camp/internal/chat/service.go) sorts ascending before applying `Limit`, while [`internal/controlplane/task_queue_processor.go`](/Users/sam/dev/otter-camp/internal/controlplane/task_queue_processor.go) assumed that window represented recent wakeups. Live proof before the fix: Sam.blog sessions `962ecd1e-35f1-425b-9581-2c65e28e5171`, `01979897-4e96-42c8-a975-d993e3f5dab7`, `8713d289-d4d6-49c2-96ab-3303ce20d9f4`, and `ca37b4e8-bc5a-4580-8c5a-ae099429d631` each accumulated two `task_queue_processor` user messages for the same `run_id` / `flow_node_execution_id` during `21:35-21:45 MDT`. Impact: long-lived task sessions kept paying for redundant kickoff writes and downstream claim churn even after the earlier singleflight narrowing.
- 2026-03-27 10:19:47 MDT - Operator workflow mismatch: [`ralph-loop-instructions.md`](/Users/sam/dev/otter-camp/ralph-loop-instructions.md) still requires staged issue directories (`issues/01-ready` through `issues/04-in-review`), but the live tree no longer has those directories. Impact: the prescribed pipeline gate cannot be executed literally against the current repo layout.
- 2026-03-27 10:41:26 MDT - Restart-readiness gate was blocked by stale integration coverage in [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go) and [`internal/controlplane/task_queue_processor_integration_test.go`](/Users/sam/dev/otter-camp/internal/controlplane/task_queue_processor_integration_test.go). Impact: `./build/verify-restart-readiness.sh` could not be trusted as the pre-run baseline until the tests were updated to current APIs and post-turn worker behavior.
- 2026-03-27 10:41:26 MDT - Fresh org-scope kickoff retries could still pay for another model round after a blocked duplicate `project.create` already proved the project identity. Impact: unnecessary kickoff churn and extra model spend before the Sam.blog run even starts.
- 2026-03-27 10:56:18 MDT - The TUI had no direct project archive/delete command even though the API and CLI already support project archive. Impact: the required “fresh start” cleanup had to go through a model-mediated Frank request, which is slower, more expensive, and vulnerable to provider backoff before testing even begins.
- 2026-03-27 10:56:18 MDT - The TUI continued to display `thinking...` for the cleanup request even after the runtime had already deferred that turn into a future retry job. Impact: the operator sees an apparently hung turn instead of accurate delayed-retry state and cannot tell whether the request is blocked, queued, or completed without external inspection.
- 2026-03-27 11:02:56 MDT - The first live `:archive-project all` run partially succeeded and then hit short-lived local API 429s because archive calls raced with normal TUI refresh traffic. Impact: “fresh start” cleanup could leave the org in a partially archived state unless the operator retries manually.
- 2026-03-27 11:11:52 MDT - The operator dashboard blocked section counted archived-project paused rows and archived-project inbox items as active blockers. Impact: a fully archived org still looked heavily blocked in the TUI (`+739 more`), which hides true zero-work state and wastes refresh/render effort on dead historical rows.
- 2026-03-27 11:32:06 MDT - Checklist-only project bootstrap recovery still keyed its broad-reread guard on resume prompt text instead of durable persisted bootstrap state. Impact: fresh Sam.blog restart sessions were able to call `project.list`, `task.list`, `project.get`, and `file.list` again at the zero-assignment staffing phase, burning turns and spawning duplicate restart projects instead of staffing the project.
- 2026-03-27 11:43:28 MDT - Multiline paste into the TUI chat composer was fragmented into one sent line plus many queued follow-on messages once the first line triggered a turn. Impact: a single project brief becomes a stack of queued org-chat messages, which is noisy for the operator and can waste extra model turns/tokens.
- 2026-03-27 12:14:02 MDT - `bootstrap.setup.persist` was over-trusting agent narration and marking later bootstrap checklist steps done before the underlying project state actually existed. Impact: fresh Sam.blog bootstrap sessions could show `staff-project`, `decompose-workstreams`, `validate-task-shape`, `attach-validate-flow-templates`, `select-first-wave`, or `record-frank-sign-off` as persisted while there were still zero real assignments, missing executable tasks, missing flow templates, or no explicit first-wave/sign-off evidence, which then caused archive/restart churn and wasted turns.
- 2026-03-27 12:38:13 MDT - Bootstrap staffing resumes could keep spending turns on `staffing.browse_profiles` / `staffing.get_profile` after an earlier turn had already produced a viable staffing catalog, instead of creating and assigning staff. Impact: fresh Sam.blog bootstrap sessions looped on advisory staffing discovery, never materialized PM/worker/reviewer assignments, and then failed/archive-restarted after paying for redundant profile reads.
- 2026-03-27 12:38:13 MDT - `bootstrap.setup.persist` still treated a single active non-starter assignment as enough to persist `staff-project` and later bootstrap steps. Impact: Sam.blog bootstrap could persist setup with only a PM assignment, create executable tasks without a real worker/reviewer roster, and then pause/fail later when kickoff validation discovered the project was structurally under-staffed.
- 2026-03-27 13:08:55 MDT - Bootstrap derived state could carry a stale completed `staffing_persisted` checkpoint forward even when the current live roster was only drafts or partial staffing, and later checkpoints could remain marked completed out of order after that stale carry-forward. Impact: Sam.blog restart sessions could resume at `task_tree_persisted` / later phases even though bootstrap still lacked a real PM/worker/reviewer roster, which steered the next turns into premature `bootstrap.setup.persist`, scaffold-only logic, and broader rereads instead of finishing staffing first.
- 2026-03-27 13:49:38 MDT - Org-scoped bootstrap repair turns could successfully call `bootstrap.setup.persist` after reviewer staffing was fixed, yet the target project still remained paused on the old reviewer-missing bootstrap failure because the org-scope external-mutation refresh path ignored `bootstrap.setup.persist`. Impact: repaired bootstrap projects stayed gated on stale pause/session state until some unrelated code path touched the project again.
- 2026-03-27 14:00:11 MDT - After the stale reviewer pause was cleared, bootstrap advanced to `first_wave_execution_missing`: first-wave child tasks had entered flow execution, but no runnable `agent_turn` jobs were created for their execution sessions. Impact: a project can pass staffing/task-tree/bootstrap persistence, then stall at the handoff into autonomous task execution even though the first wave appears launched.
- 2026-03-27 14:28:14 MDT - Root cause for the `first_wave_execution_missing` stall: the worker’s active-execution repair query only considered task sessions that already had a qualifying kickoff `chat_message`, so active first-wave execution sessions with zero kickoff messages were invisible to recovery and could never get their first `agent_turn` job. Impact: fresh first-wave task sessions could remain permanently idle even though bootstrap had already launched their flow execution.
- 2026-03-27 14:57:04 MDT - Second root cause in the same first-wave execution family: once a task session had both an older execution-owned kickoff (`task_queue_processor`) and a newer generic `supervisor recovery: resume task`, the worker consistently treated the newer supervisor message as the latest truth. It could purge the execution-owned job as a stale “project_task continuation”, claim the supervisor retry first, or refuse to requeue a fresh execution-bound kickoff because the pending supervisor job already occupied the session. Impact: first-wave tasks could still starve or loop on generic supervisor resumes even after the missing-kickoff fix created the correct execution-bound message.
- 2026-03-27 15:07:29 MDT - Ordinary cross-session `agent_turn` claim order still favored newer task-session work because [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) sorted non-project-continuation `agent_turn` rows by `run_after DESC` / `created_at DESC` in the global claimable set. Impact: older due async task sessions could be starved behind newer arrivals, obscuring the real oldest backlog and delaying Sam.blog first-wave progress even after task-session ownership bugs were fixed.
- 2026-03-27 15:30:12 MDT - Active-execution worker recovery could resurrect a consumed `task_recovery_resume` message even after its completed turn had already written the target file successfully. Impact: async task sessions could pay for another recovery turn on the same satisfied recovery prompt instead of moving on, which is pure model churn and keeps the lane artificially in recovery mode longer than necessary.
- 2026-03-27 15:43:33 MDT - Recovery durability verification for task-scoped `file.write` only searched project compatibility roots, not the task worktree where task-lane mutations actually land. Impact: a recovery turn could successfully write `templates/layout-04-magazine.html` into the task worktree, then immediately self-halt with “reported success but ... was not found on disk,” reopen recovery, and burn another turn on the same HTML file.
- 2026-03-27 15:57:02 MDT - Supervisor blocked-task recovery still auto-resumed `review_decision_required` review blocks. Live proof: task 13 was marked `blocked` by the turn engine at `15:47:16 MDT`, then supervisor changed it back to `review` at `15:48:30 MDT` with `recovery_action=resume_validation_blocked_task`. Impact: intentionally blocked review lanes can immediately reopen into more review turns, wasting queue slots and tokens while also risking abandonment of the active review execution the operator actually needs to finish the review decision.
- 2026-03-27 16:08:06 MDT - Ordinary async task turns still paid for malformed `cli.execute` tier2 runs after the engine had already issued an explicit missing-`command` correction. Impact: the same turn could spend multiple tool calls on `command is required` before the generic validation-loop guard stopped it, which showed up directly in the Sam.blog one-hour efficiency report as repeated `validation_loop_blocked` task churn.
- 2026-03-27 16:29:38 MDT - `_raw` `file.edit` arguments were not normalized at all, so task-turn repair logic could not recover `path` / `old_string` / `new_string` from live `file_edit` envelopes. Impact: HTML task lanes could keep dispatching pathful `file_edit` calls that omitted `new_string`, then fall into repeated same-turn validation churn instead of being corrected before dispatch.
- 2026-03-27 16:29:38 MDT - Validation classification treated any call envelope containing `_raw` as malformed even when normalization had recovered a complete call and the tool result succeeded. Impact: successful `_raw` `file.write` calls in task sessions could still be miscounted as validation failures, inflating `validation_loop_blocked` churn and obscuring the real remaining blocker family.
- 2026-03-27 16:35:19 MDT - Task-session tool routing can still drop the intended `file.write.content` payload and echo stale tool-error text back into the deliverable file. Live proof: task 21 (`templates/layout-07-split-screen.html`) kept reading back prior error prose after both `file.write` and `cli_execute` attempts on post-restart turns (`1c33e39d-22ad-4795-bd20-17d6ea48e25d`, `240c110a-a710-490f-a382-960d866ae9dc`). Impact: the lane burns retries on a false “tools are broken” narrative, blocks actual HTML progress, and likely drives more `validation_loop_blocked` / recovery churn than the raw-`file.edit` family itself.
- 2026-03-27 16:46:12 MDT - Task/recovery file-write repair helpers only matched `file.write`, not the live `file_write` alias that Anthropic task turns still emit in metadata. Impact: empty-content and wrong-path repair logic could silently miss in exactly the ordinary HTML task lanes we are using for the Sam.blog canary, letting malformed writes slip through to tier2 instead of being corrected pre-dispatch.
- 2026-03-27 16:46:12 MDT - Tool-routing/operator-repair troubleshooting prose was still considered a reusable draft body. Impact: when task 21 reread its own stale error text from the recovery artifact, the engine could treat that prose as “substantive” and write it back into `templates/layout-07-split-screen.html`, creating a self-reinforcing recovery loop instead of forcing a new concrete HTML draft.
- 2026-03-27 16:51:50 MDT - Content-migration checkpoint continuations returned before the normal task continuation action prompt was appended. Impact: after a large fetch turn compressed into a checkpointed follow-up, the next task turn could answer with generic chat (“What would you like to know?”) instead of resuming the migration work, wasting a full model turn immediately after an already-expensive context-compression boundary.
- 2026-03-27 16:57:43 MDT - Empty content-migration checkpoints returned no immediate-next-action guidance when a task branch had zero artifacts, scripts, and outputs. Impact: Sam.blog migration tasks 10 and 11 could keep paying for `file.list`, `git.status`, and `git.log` rediscovery against an empty task worktree instead of reading the upstream manifest/index deliverables already named in the task description and dependency history.
- 2026-03-27 17:07:55 MDT - The project-continuation rediscovery guard only activated when the prompt named actionable draft tasks, not when it only named already-active tasks. Live proof: project session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` reopened at `16:54-16:57 MDT` with prompts naming active tasks `30`, `25`, `24`, and `23`, then still spent turns on `session.list`, broad `task.list`, root `file.list`, `git.log`, and repeated `task.get` on those already-named tasks. Impact: the PM lane burned large `max_tool_calls` turns rediscovering status that was already present in the continuation prompt instead of taking a bounded project action or yielding.
- 2026-03-27 17:15:32 MDT - Synthetic engine-authored recovery-resume prompts (`metadata.source=task_recovery_resume`) were not recognized by `isRecoveryResumeMessage(...)`. Live proof: task 11 session `1aea2d41-cdfe-4df5-ac17-897c006e702f` completed turn `e3935d37-0388-43c5-81a9-b335b55e9514` at `17:09:10 MDT` with the generic assistant reply “I'm ready to help! What would you like to know?” immediately after a synthetic `Continue the active task recovery now...` prompt, but the engine did not enqueue the normal generic-recovery retry. Impact: a deterministic useless recovery response can silently consume a full async task turn, force the lane to depend on later worker/supervisor wakeups, and keep high-cost recovery sessions burning extra tokens instead of immediately retrying the bounded recovery prompt.
- 2026-03-27 17:38:33 MDT - Recovery turns with a concrete file-output target could still discard the specific first-correction failure reason (`cli.execute` missing `command` or `file.write` missing `content`) as soon as the same turn later hit the generic repeated-recovery-focus stop. Impact: the next resume only saw broad read-only churn instead of the sharper “send a real command/body next” instruction, which keeps deterministic malformed recovery loops more expensive than necessary.
- 2026-03-27 17:38:33 MDT - Two adjacent recovery integration fixtures are stale against the current runtime: `TestTurnEngineIntegrationRecoveryTurnPersistsCheckpointForRepeatedEmptyCLIExecuteWithFileContextEX321` and `TestTurnEngineIntegrationRecoveryTurnRewritesEmptyCLIExecuteToPersistedFileWrite` still expect the older no-rewrite/no-dispatch path, but the engine now legitimately synthesizes persisted-draft `file.write` work in those lanes. Impact: those tests are red for fixture drift, not because of the checkpoint-preservation changes in this slice.
- 2026-03-27 17:51:29 MDT - Review retry prompts had no specialization for “deliverables already found, only secondary planning artifacts missing.” Live proof: Sam.blog task 10 session `b09b5b7b-8e38-4a3b-a59d-7468ab8939e4` repeatedly listed `content/posts`, then reread missing `planning/prd-spec/oc-34-*.md` files on successive review retries until the three-turn blocked-review cap fired. Impact: review lanes can waste multiple turns on missing companion planning files even after current deliverables are already present and sufficient for bounded review.
- 2026-03-27 18:02:20 MDT - Project continuation snapshots still treated draft parent shells with active child execution as actionable remaining draft work. Live proof: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` spent turns on parent tasks `33` and `34` even though their child tasks `37/38` and `35/36` were already `in_progress` / `review`, because the continuation prompt surfaced those parents in the actionable-draft line and did not distinguish them from genuinely queueable drafts. Impact: the PM lane can waste repeated `max_tool_calls` turns rediscovering or trying to advance parent shells that should simply yield to already-running child task lanes.
- 2026-03-27 18:12:32 MDT - Even after the continuation prompt was patched to demote active-child draft parents, project continuation turns still rooted their next run on the summary system message rather than the synthetic user action prompt. Live proof: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` had user prompts at sequences `1667` and `1682` that clearly included the new guarded snapshot lines, but the following turns still allowed `session.list`, named active-task `task.get`, and `flow.get_execution` misuse that should have been blocked. Impact: prompt-level continuation hardening can silently fail at runtime because `rt.initialMessageText` never sees the synthetic action prompt content the guards depend on.
- 2026-03-27 18:25:38 MDT - A second same-turn handoff bug remained after the history-root fix: `appendContinuationSummaryAndAction(...)` appended the synthetic continuation action prompt before the model responded, but the live `turnRuntime` still kept `initialMessageText` / `initialMessageID` pointed at the older pre-summary trigger for the rest of that same turn. Live proof: fresh PM continuation turns still executed `task.get` on named active tasks and `session.list` / broad `task.list` inside the very turn that already contained the guarded synthetic user prompt. Impact: project-continuation guards that key off `rt.initialMessageText` silently miss same-turn rediscovery churn even though the model itself sees the corrected prompt.
- 2026-03-27 18:36:12 MDT - After the same-turn guard fix, the PM continuation lane still had two narrower leaks. First, broad recursive artifact-root reads like `file.list(path=content/posts, recursive=true)` and `file.list(path=templates, recursive=true)` were still allowed even though the project continuation prompt already named the active child tasks and explicitly told the PM lane not to rediscover their workspace state. Impact: the lane could still spend tool slots walking entire deliverable directories from the project lane instead of acting on the named tasks.
- 2026-03-27 18:36:12 MDT - Second, the continuation summary generator still admitted fenced pseudo-tool CLI transcripts such as ``task.get --task_id=...`` and ``session.list --scope_id=...``. Live proof: Sam.blog PM sequence `2239` stored exactly that pseudo-tool summary above the synthetic continuation prompt, and the following retry immediately replayed the same blocked `task.get` / `session.list` patterns. Impact: even after the prompt and guard fixes, unusable continuation summaries could keep priming the next PM turn to waste tool calls on already-blocked rediscovery actions.
- 2026-03-27 18:42:44 MDT - Async project continuation turns could still spend an extra same-turn assistant round after a blocked batch composed only of project-continuation rediscovery guard errors. Live proof before the fix: Sam.blog PM turn `42411199-0b87-4e75-9764-ad88a3d8e94f` (started `18:42:55 MDT`) took five blocked named-`task.get` results at sequences `2448-2452`, then still produced assistant sequence `2453` in the same turn instead of ending immediately. Impact: even after the PM rediscovery guards were firing, the lane still burned another model response on nothing but already-blocked discovery churn.
- 2026-03-27 18:52:55 MDT - Worker-synthesized `project_execution_continuation` prompts were still using an older hardcoded template with no project snapshot or anti-rediscovery lines. Live proof before the fix: fresh worker-generated prompts at sequences `2491` and `2514` in Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` contained only the generic “Inspect the current task tree...” text, so the following turns reopened with broad `task.list` and then re-read tasks `34/35/36/11/12/13/10` until `max_tool_calls`. Impact: even after the turn engine’s PM prompt hardening, worker-owned auto-continues could still bypass it completely and keep the PM lane paying for large rediscovery turns.
- 2026-03-27 18:57:10 MDT - The project-lane blocked-mutation stop only looked at `result.Error`, so it missed real tier1 bounded-size rejections that arrive as `output.error`. Live proof before the fix: Sam.blog PM turn `a338ef09-935a-4e4e-b3d3-a60bf0d95d63` reached `task.update -> task exceeds bounded size policy (estimated 50 minutes > 30 minute limit): split the work into smaller reviewable tasks before queueing` at sequence `2672`, but still fell through to `max_tool_calls` instead of ending immediately on the actual blocker. Impact: once the PM lane finally reaches the real decomposition blocker, it can still waste the tail of the turn budget on a turn that should instead stop and steer the next continuation directly into bounded task-splitting.
- 2026-03-27 19:04:46 MDT - The project-continuation snapshot rediscovery guard still allowed `flow.list_templates` / `flow_list_templates` even when the continuation prompt already named the active/draft task set and no actionable draft was blocked on `flow_template_id=missing`. Live proof before the fix: Sam.blog PM turn `a338ef09-935a-4e4e-b3d3-a60bf0d95d63` opened with `flow_list_templates` at sequence `2661` before broad `task.list`, named child rereads, and the later bounded-size rejection. Impact: the PM lane could still burn a template-catalog tool slot on broad rediscovery right before more specific guards or bounded-size blockers fired, adding avoidable project-turn cost without helping legitimate missing-template repairs.
- 2026-03-27 19:14:56 MDT - Even after the PM continuation prompt explicitly told the model not to reread the parent or child task records when the focus draft already had `child_tasks=2` and those child lanes were named above, the Sam.blog PM lane still repeated the same blocked `task.get` pattern across turns. Live proof after the prompt hardening: worker-generated continuation message `2753` in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` included the new anti-reread line, but the very next assistant step at `2754` still tried to re-check task 34’s children and then hit the same blocked `task.get` results at `2755`, `2757`, and `2758`. Impact: prompt-only steering is no longer sufficient for this task-34 parent/child rediscovery loop; the remaining waste now comes from repeated ignored guidance rather than missing prompt context.
- 2026-03-27 19:40:42 MDT - Project continuations were still classifying decomposed draft parents as “actionable drafts” unless one of their children was actively executing. Live proof before the fix: Sam.blog PM continuation message `2894` in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` still announced `1 remaining draft project task` for task 34 even though child tasks 35 and 36 already existed and were both `blocked`. Impact: the worker kept generating PM prompts that falsely presented a decomposed parent as the next runnable draft, which preserved the task-34 rediscovery loop and kept the queue spending turns on parent/child rereads instead of treating that work as already split.
- 2026-03-27 19:54:35 MDT - Review-task placeholder protection still landed too late for the hot task-24 family. Live proof before the fix: session `b2c5a138-e867-4537-b470-bdd2d6cf233e` emitted a single assistant tool batch containing the correct preferred-target `file.read` for `templates/layout-10-conversational.html` plus multiple sibling `file.read` calls that all came back `not_found`; because `placeholderTargetSeen` was only set in `appendToolResults(...)` after the whole batch completed, the existing placeholder follow-on guard could only affect later batches, not those same-turn sibling reads. Impact: review lanes could still spend several tool calls on avoidable missing-file reads even when the preferred deliverable already proved the task should be rejected immediately.
- 2026-03-27 20:14:18 MDT - Worker-side project continuation dedupe only compared pending message source plus `completed_task_id`. Live proof before the fix: PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` kept producing one fresh `project_execution_continuation` per minute for the same `completed_task_id` `5e0cf069-62d6-4514-ac7c-2295bb087cd2`, and every one of those turns ended `validation_loop_blocked` on the same `Project continuation rediscovery guard blocked only broad rereads...` stop. Impact: even after the PM turn engine was ending pure rediscovery batches early, the worker still re-armed the exact same no-progress continuation indefinitely, turning a bounded PM dead end into ongoing token churn.
- 2026-03-27 20:14:18 MDT - The same worker seam also misclassified active-bootstrap sessions with stale `project_execution_continuation` messages and zero open tasks as “settled project continuations.” Impact: instead of switching those sessions back to `project_bootstrap`, the worker could retire the stale continuation and leave the bootstrap lane without a fresh kickoff.
- 2026-03-27 20:34:12 MDT - First-turn review prompts only derived a preferred deliverable target from `Deliverable:` / `Output:` labels or current-session evidence. Live proof before the fix: task 24’s first review prompt omitted `templates/layout-10-conversational.html` because the description used `Save as ...`, and task 11’s first review prompt omitted `content/technonymous/stop-preparing-your-kids-for-jobs.md` because the fresh review session did not reuse prior same-task deliverable evidence. Impact: the first review turn could still waste tool calls on broad rediscovery and only narrow down on retry.
- 2026-03-27 20:54:25 MDT - The repeated Sam.blog PM continuation loop had one more hidden worker trigger even after fingerprinted `project_execution_continuation` suppression: stale pending `project_bootstrap` user messages were still left behind in completed-bootstrap project sessions, and `RequeueActiveProjectSessionsWithoutTurns(...)` would wake on those ancient bootstrap roots and synthesize a fresh `project_execution_continuation` for the same completed-task fingerprint after every rediscovery-only `validation_loop_blocked` PM turn. Live proof before the fix: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` still had pending bootstrap messages at sequences `1` and `35`, while turns `273-276` repeatedly created/fired messages `3153`, `3159`, `3165`, and `3173` for the same fingerprint `17f9ed35f2a25ad1` with `retry_count=0`. Impact: PM continuation churn could persist indefinitely even after a same-fingerprint continuation had already been suppressed once, because stale bootstrap roots kept rearming the loop from the side.
- 2026-03-27 21:22:56 MDT - Explicit runtime `flow rejection max visits exceeded` review blocks could still take the legacy durable-checkpoint resume path whenever the target deliverable contained reviewer rejection text, because `ResumeValidationBlockedTask(...)` did not hard-stop on the newer `flow_rejection_max_visits` / `flow.rejected max_visits_exceeded` event signals before checkpoint repair and legacy scaffold heuristics ran. Impact: terminal review-exhaustion tasks could reopen after supervisor recovery and re-enter review churn even though the runtime had already concluded the reject path was exhausted.
- 2026-03-27 21:22:56 MDT - Supervisor blocked-task detection also lacked a filter for explicit flow-rejection max-visits evidence, so it kept reconsidering those already-terminal review tasks every scan window. Impact: even after the service-level resume path is corrected, the supervisor would still waste cycles loading and evaluating tasks that should be permanently excluded from blocked-task recovery.
- 2026-03-27 21:37:08 MDT - Worker recovery was misclassifying consumed `task_recovery_resume` turns as terminal success whenever they contained any positive-byte `file.write`, even if the same turn completed with `stop_reason='validation_loop_blocked'`. Live impact: Sam.blog tasks 16, 21, and 23 sat `in_progress` with active executions, latest recovery-resume turns halted on validation churn, and no pending or claimed `agent_turn` jobs because both stale-job purge and active-execution requeue treated those turns as already recovered.
- 2026-03-27 22:17:12 MDT - Task recovery still allowed same-task `planning/...` `file.read` calls even after the engine-authored recovery prompt explicitly said a substantive durable draft was already present and the next action should be the concrete write. Live proof before the fix stayed task-37 recovery sessions `1a31300a-7a58-4f65-98bf-8cf473c905fa` and `a80aa505-f06a-4e2c-a959-c482d7a54dd7`, which reopened with `planning/prd-spec/oc-34-prd.md` and `planning/prd-spec/oc-34-acceptance-criteria.md` rereads instead of writing `templates/template-07-dark-mode-developer.html`. Impact: recovery lanes with an already-embedded draft could still spend expensive turns on same-task planning rediscovery that the prompt had already ruled out.
- 2026-03-27 22:24:13 MDT - The first durable-draft recovery read-scope patch was not sufficient by itself because synthetic recovery prompts were not becoming the runtime’s active prompt text. Live proof: after the `22:21 MDT` restart, task-37 recovery session `d675c45c-95d1-4503-aa96-f152236c7725` still emitted `file.read not_found` on `planning/prd-spec/oc-34-prd.md` and `planning/prd-spec/oc-34-acceptance-criteria.md` at `22:18:54-22:19:19 MDT`, while the appended `task_recovery_resume` prompt messages in the same session definitely contained both durable-draft guard phrases. Impact: same-turn recovery tool guards that rely on `rt.initialMessageText` could stay blind to the synthetic recovery prompt and fail open even after the prompt content itself was corrected.
- 2026-03-27 22:40:15 MDT - Supervisor blocked-task recovery was still auto-resuming durable recovery-checkpoint tasks whose blocker text explicitly required manual repair. Live proof before the fix: Sam.blog task 21 (`74d1dd51-479b-4b55-bfec-088bf6d72f3e`) cycled `blocked -> queued -> in_progress` at `22:27`, `22:29`, and `22:31 MDT` with the same `recovery halted after recovered file.write ... re-queue only after resolving that failure` blocker, and each supervisor pass abandoned the active execution first. Impact: malformed recovery writes could loop indefinitely and the supervisor was actively worsening the damage by tearing down flow execution state on every scan.
- 2026-03-27 22:47:14 MDT - Async project prompt compression could still fall through to a model-authored continuation summary even when the active project session already had a direct user steering request that should have been preserved deterministically. Live proof before the fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` stored sequence `3184` as a weak summary beginning `I need to clarify the status of blocked HTML template tasks` and `Since I don't have direct access to project history in this conversation...`, even though sequence `3181` already carried the real user request to recover the blocked HTML template tasks. Impact: the next PM turn rooted on that bad summary and immediately spent its budget rereading stale named task ids that the rediscovery guards already block.
- 2026-03-27 22:59:48 MDT - Reviewer rejection prose could still be treated as a reusable template deliverable draft because the draft classifier only recognized reviewer-assessment pollution on `Work/...` and `Test/...` paths, not on template/content deliverables. Live proof before the fix: the task-37 worktree file `templates/template-07-dark-mode-developer.html` contained `The file read returned \`mismatched_deliverable_context\`... I will reject the review immediately using this evidence.`, and the following review session `3678ca0a-fc50-43f0-bdd7-6b4f5e786be7` read that file back as `mismatched_deliverable_context` instead of seeing real HTML. Impact: review-stop commentary can be written back into a deliverable, then later be mistaken for substantive task content, which amplifies both recovery churn and review exhaustion.
- 2026-03-27 23:16:40 MDT - No-contract async review prompts still allowed first-turn `planning/...` reads even when the prompt explicitly said `Do not invent companion planning-artifact requirements ... review the actual deliverable files ... only`. Live proof before the fix: task 38 review session `03e9e50a-e822-4e33-9cda-28b518324cd9` and task 10 review session `b09b5b7b-8e38-4a3b-a59d-7468ab8939e4` both spent all three retries on repeated `file.read not_found` against missing planning artifacts instead of inspecting deliverables and calling `flow.review_decision`. Impact: no-contract review lanes could still burn multiple turns on invented companion planning requirements even though the prompt already ruled them out.
- 2026-03-27 23:24:11 MDT - Scrape/import review prompts still failed to name a concrete deliverable root even when the task description already did. Live proof before the fix: task 10 session `b09b5b7b-8e38-4a3b-a59d-7468ab8939e4` used the generic review prompt without any `content/posts` target, despite the task description explicitly saying the output is committed under `/content/posts/`, and the resulting review turns opened with `task.get`, `git.log`, and missing dependency/planning artifact reads instead of inspecting the delivered markdown set directly. Impact: review lanes for directory-style outputs could not take a bounded first step, so they kept rediscovering metadata and dependencies before ever touching the actual output root.
- 2026-03-27 23:35:54 MDT - PM continuation snapshots still named blocked tasks without surfacing why they were blocked. Live proof before the fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` reached assistant turn `3208`, which said `I need to understand why tasks 35, 36, 37, and 38 are all blocked`, then immediately burned four blocked named-`task.get` calls (`3209-3212`) to rediscover reasons already available from `project_task_event`. Impact: the PM lane could spend several extra tool calls per continuation turn on blocked-task reason rediscovery before any real unblock, decompose, or queue action started.
- 2026-03-27 23:43:18 MDT - Blocker summaries alone were not quite enough for PM continuations: once the session could see why tasks 35, 36, 37, and 38 were blocked, it still had no explicit action class for those blockers. Live proof before the follow-on fix: the fresh post-summary PM turn said tasks `35/36/38` needed recovery and task `37` was terminal, but still spent the turn on broad `file.list(templates)`, `file.list(content)`, `file.search`, and planning-artifact reads to rediscover what kind of follow-on work to create. Impact: the PM lane had crossed from “missing blocker context” into “missing blocker action policy,” so it could still burn a full turn after correctly identifying the blocker family.
- 2026-03-28 00:04:15 MDT - PM dependency hints were still missing when the producer task described its output in natural language instead of strict label form. Live proof before the fix: Sam.blog task `14` already said `Output a JSON index at content/technonymous-index.json`, but the fresh PM continuation prompt for tasks `35/36` only showed `deliverable_root=content/posts` and no `depends_on_path`, so the lane still reopened with blocked root browsing instead of reading the exact missing prerequisite artifact first. Impact: the PM continuation had enough context to know the output root but not enough to name the concrete dependency file, so it continued paying for preventable rediscovery.
- 2026-03-28 00:09:25 MDT - PM write-boundary blocks were not leaving a durable correction trail. Live proof before the fix: after the new dependency hint drove PM continuation turn `bb841e94-1964-4e38-9d9c-cd0b988acc09` to the exact missing prerequisite, the lane tried to `file.write` `content/technonymous-index.json` from the project session and got `task_execution_required` at message `3278`, but the turn still completed with no `stop_reason` and no system message telling the next continuation to queue the owning task instead. Impact: the runtime recognized the boundary error but failed to turn it into a machine-visible validation stop, so follow-on PM turns could keep drifting back into the same project-session write mistake.
- 2026-03-28 00:13:47 MDT - Even after the dependency path was explicit, PM continuations still tried to execute the missing prerequisite work themselves after the first `file.read` miss. Live proof before the fix: on Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, turns `3290-3328` repeatedly read `content/technonymous-index.json -> not_found` and then burned the rest of the turn on homepage/archive discovery instead of immediately queuing or advancing the replacement task that should produce the index. Impact: one missing prerequisite artifact could still consume full PM turns on external browsing even though the dependency path was already known and the project lane is not supposed to execute that deliverable directly.
- 2026-03-28 00:48:37 MDT - The next narrower seam was worker-side, not turn-engine-side. After the dependency-miss slice, the turn engine started appending the stronger PM stop `[Project continuation found that prerequisite artifact \`content/technonymous-index.json\` already has active replacement work in the tree: ...]`, but the worker still treated that session as a candidate for replay. The clean live proof is session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` at messages `3420-3422`: the active-replacement stop landed at `3421`, and a fresh synthetic `project_execution_continuation` was immediately rearmed at `3422`.
- 2026-03-28 00:48:37 MDT - Patched [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so repeated-project-continuation suppression now matches both families:
  - the older rediscovery-only stop
  - the newer active-replacement stop that starts with `[Project continuation found that prerequisite artifact \`...\`` and contains `already has active replacement work in the tree:`
- 2026-03-28 00:48:37 MDT - Added focused integration coverage in [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go) for both worker entry points that could revive the session (`ensureProjectContinuationMessageDecision(...)` and `RequeueActiveProjectSessionsWithoutTurns(...)`), reran the targeted integration slice successfully, rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`.
- 2026-03-28 00:48:37 MDT - Post-deploy live state is clean even though I did not get a second identical active-replacement replay window: the Sam.blog PM session now has `0` pending `project_execution_continuation` messages and `0` pending/claimed `agent_turn` jobs instead of being kept alive by worker recovery. That is enough to keep the system moving forward without replaying the same PM dependency-miss family.
- 2026-03-28 01:07:11 MDT - The next hot cost moved fully into decomposed child task sessions. In Sam.blog, task `40` session `bcbc53cd-99fa-476f-a50c-b4ba26dd0b7e` and task `41` session `1e7d7f89-27b7-4170-b585-7f7dadcd9868` were resuming from generic task continuation roots even though the real contract lived on parent replacement task `39`. Live proof before the fix:
  - task `40` resumed with only the generic fallback, hit `browser.navigate -> browser: not found`, then pivoted into repeated full-post fetches and burned `105` invocations / `1.64M` total tokens in the last-hour report before ending at `max_tool_calls`
  - task `41` resumed with the same generic fallback, re-read sibling task/project context, then started browsing technonymous.org directly from the write lane even though sibling task `40` already owned navigation/discovery work
- 2026-03-28 01:07:11 MDT - Patched [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so decomposed child-task continuations now inherit a compact snapshot of the current task, parent contract, and sibling ownership, and so write-focused child lanes block `browser.navigate` / `web.fetch` when a sibling child lane already owns discovery/navigation responsibility.
- 2026-03-28 01:07:11 MDT - Widened explicit deliverable-path extraction to catch natural `write|create|produce ... to PATH` phrasing, which is what let task `41` start surfacing `deliverable_path=content/technonymous-index.json` directly from its own brief instead of looking context-free.
- 2026-03-28 01:07:11 MDT - Added focused turn-engine unit coverage for the new snapshot builder, prompt guidance, and sibling-responsibility blocker; reran the targeted slice successfully; rebuilt `./bin/ottercamp`; restarted tmux `codex-e2e-20260324`; and confirmed `./bin/ottercamp health --output json` returned `data.status=ok`.
- 2026-03-28 01:07:11 MDT - Fresh live proof is still pending because those two hot task sessions did not replay in the immediate post-restart window. The deploy state is still useful: the fix is test-green and live, and the remaining gap is only waiting for the next natural task-40/task-41 continuation on the new binary.
- 2026-03-28 01:35:37 MDT - Blocked decomposed child lanes could still leave stale active async sessions behind after their owned execution was already `abandoned`. Live proof before the fix: Sam.blog task `40` session `bcbc53cd-99fa-476f-a50c-b4ba26dd0b7e` and task `41` session `1e7d7f89-27b7-4170-b585-7f7dadcd9868` both remained `chat_session.status='active'` with `current_turn_id=NULL`, no pending/claimed `agent_turn` jobs, and their only owned `flow_node_execution` rows already `abandoned`. Impact: those sessions were dead from a work-execution perspective, but they still inflated active async state and future cleanup scans as if something live remained.
- 2026-03-28 01:41:43 MDT - The same stale-session leak also existed for blocked tasks whose owned execution had ended `rejected` instead of `abandoned`. Live proof before the fix: after the first cleanup slice, Sam.blog still had `11` active async task sessions (`11`, `16`, `17`, `18`, `19`, `20`, `22`, `23`, `24`, `30`, `37`) with `current_turn_id=NULL`, zero pending/claimed `agent_turn` jobs, and only `flow_node_execution.status='rejected'` ownership remaining. Impact: review-exhausted / flow-rejected blocked lanes stayed visible as active async work even though they were equally terminal from the worker’s perspective.
- 2026-03-28 02:04:12 MDT - Async project continuation compression still had a stale synthetic-prompt suppression bug. Live proof before the fix: after a bounded human PM directive landed in Sam.blog session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, the runtime appended `[Continuation summary] Active project request: ... restore content/technonymous-index.json ...` at sequence `3475` but then emitted generic assistant reply `3476` (`I'm ready to help. What's your question or request about the Sam.blog Rebuild project?`) because orphan pending synthetic `project_continuation_resume` rows with `turn_id=NULL` blocked the fresh action prompt from being appended to the compressed turn. Impact: PM sessions with stale synthetic residue could silently fall back into generic chat even when the continuation summary and active request were both correct.
- 2026-03-28 02:04:12 MDT - After the continuation prompt was restored, the next PM inefficiency narrowed to off-target task focus rather than missing context. Live proof before the second fix: the same session appended a correct dependency-focused continuation prompt at `3497` naming task `41` (`content/technonymous-index.json`) plus parent replacement task `39`, but the PM lane still created unrelated top-level template task `43` and then treated it as the next focus at `3514-3521`. Impact: once an unrelated actionable draft exists in the project, generic first-draft focus can still pull the PM lane off the explicit active request and burn more bounded-size churn on the wrong workstream.
- 2026-03-28 02:10:42 MDT - Even after the continuation prompt was correctly focused on the technonymous index path, the PM lane could still reopen external crawl work from the project session. Live proof before the next fix: PM turn `bff83bf0-442e-4576-81f7-9738029b6870` stayed on `content/technonymous-index.json` but still spent the turn on `web.fetch` for the Substack landing page, archive page, and sitemap (`3528`, `3530`, `3531`) before hitting the existing project-session write boundary. Impact: the PM lane was no longer drifting to unrelated drafts, but it could still pay for external fetch churn instead of queueing or advancing the already-named replacement execution lane.
- 2026-03-28 02:17:44 MDT - Post-turn PM continuation repair still let unrelated runnable drafts preempt a confirmed missing-dependency stop. Live proof before the fix: on Sam.blog PM turn `d1aaf03e-6d39-4ddf-a471-d85cd181c4b1`, the runtime already reached `file.read(content/technonymous-index.json) -> not_found` and appended the correct missing-dependency stop at `3545`, but completion handling still fell through to unrelated draft task `43` and appended its bounded-size failure at `3546`. Impact: even after the PM lane proved exactly which prerequisite artifact was missing, the repair path could still burn project-session attention on unrelated draft work instead of advancing the matching replacement producer.
- 2026-03-28 02:34:00 MDT - PM missing-dependency recovery still treated blocked producer tasks as if they were active replacement work. Live proof before the fix: on Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, blocked producer task `41` owned `content/technonymous-index.json` but was already `blocked`; the fresh continuation at `3551-3558` only moved forward because the bounded directive overrode that stale assumption. Impact: blocked producer tasks could suppress the exact follow-up replacement work the PM lane needed to create.
- 2026-03-28 02:34:00 MDT - Queue decomposition was still treating procedural sections as child-task deliverables. Live proof before the fix: Sam.blog replacement task `44` persisted decomposition metadata whose `source_description` contained `## Steps` and `## Important`, and those lines became malformed child tasks `45-48` (`Use browser tools...`, `Use browser_navigate...`, `Use cli_execute...`, `Do NOT produce planning artifacts...`). Impact: a single concrete deliverable task could explode into instruction-step child tasks, reopening unnecessary async sessions and token churn.
- 2026-03-28 02:56:00 MDT - PM continuation suppression still missed the bounded-size draft loop. Live proof before the fix: Sam.blog session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` kept replaying identical `project_execution_continuation` turns for completed task `47` after `task.update(43)` hit the same bounded-size rejection. The worker already suppressed rediscovery-only and active-replacement validation blocks, but it did not treat bounded-size PM stops the same way, and a newer identical pending message could even shadow the older consumed blocked turn. Impact: the PM lane stayed token-idle but operationally noisy, with the same task-43 continuation wave rearming instead of draining.
- 2026-03-28 03:06:00 MDT - Async task review still had an unbounded preferred-deliverable read loop. Live proof before the fix: Sam.blog task `48` session `f1b27039-5b3b-4a57-aaf4-025443cdc612` repeatedly called plain `file.read(content/technonymous-index.json)`, received the full `35340` byte JSON payload, then immediately hit the `20000` token prompt guardrail and continued into another compressed review turn. Impact: a single large deliverable file could trap review lanes in reread/compress/retry churn without producing new evidence or a `flow.review_decision`.
- 2026-03-28 03:16:36 MDT - Discovery-only decomposed child lanes could still mutate the shared deliverable even when a sibling write child already owned that artifact. Live proof before the fix: blocked task `46` session `5073a26f-0352-49b5-80c8-536f7531fbbe` hit `file.read(content/technonymous-index.json) -> mismatched_deliverable_context`, but the same turn still retried `file.write(content/technonymous-index.json)` and then burned a repeated identical successful file-write churn stop instead of yielding back to the sibling write lane. Impact: malformed discovery children could spend full turns rewriting the final artifact they were never supposed to own.
- 2026-03-28 03:35:43 MDT - The first preferred-deliverable review read cap fixed the worst full-file blowup, but task `48` still had a narrower reread loop because `file.read` only read from byte `0`. Live proof before the follow-up fix: fresh task-48 review session `1bb99d1b-d24c-4716-b9d4-3fd3197144ac` was now capped to `max_bytes=8192`, but messages `398`, `401`, `404`, `406`, `415`, and `417` kept rereading the same first chunk of `content/technonymous-index.json`, then hit `[Prompt input exceeded 20000-token guardrail - continuing in a new turn.]` and compressed again. Impact: the system stopped paying for full 35 KB reads, but it could still burn multiple review turns and continuation summaries on the same first 8 KB because the lane had no efficient way to inspect the tail.
- 2026-03-28 03:47:00 MDT - The first tail-window slice still missed same-turn follow-up review reads. Fresh post-restart task-48 rows on session `1bb99d1b-d24c-4716-b9d4-3fd3197144ac` showed `offset_bytes=0` rereads even after earlier truncated head reads were already present and the assistant was explicitly asking to “read the tail.” Impact: the API surface was correct, but the rewrite could still miss the immediate follow-up read inside the hot review turn and pay for another first-8KB reread.
- 2026-03-28 04:28:22 MDT - Single concrete file deliverables could still be misclassified as bounded-size PM work when their descriptions carried long requirement blocks. Live proof before the fix: Sam.blog PM turn `984d9f13-c56d-4a9f-ad71-581dfa0b0c95` hit `task.update(43) -> task exceeds bounded size policy (estimated 65 minutes > 60 minute limit)` even though task `43` names exactly one deliverable file, `templates/template-08-replace.html`, has no decomposition metadata, and is meant to execute as one bounded template implementation task rather than another orchestration parent. Impact: the PM lane could only halt on task `43` instead of queueing it, which preserved a false bounded-size stop family and delayed real template execution.
- 2026-03-28 04:47:33 MDT - Project-continuation replay suppression was still blind to deploy boundaries. Live proof before the fix: after the first uncommitted runtime restart, Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` emitted fresh continuation `1c10ae13-c5e1-4c9d-bed0-12a03fa73801` with `repo_version=3384`, but once that turn failed on the task-43 bounded-size stop the same session went idle again with no new continuation created while the binary stayed on the same repo version. Impact: code-only deploys that materially changed project-continuation behavior could still leave an otherwise recoverable PM session parked until some unrelated state changed the fingerprint.
- 2026-03-28 04:47:33 MDT - The task-43 bounded-size false positive came from naive substring matching inside broad-scope heuristics, not from the single-file sizing path itself. Reproduced in tests with the exact live task wording: `looksLikeSingleConcreteFileDeliverable=false`, extracted path `templates/template-08-replace.html`, and `broadScope=true` solely because `professional personal hub` accidentally matched the broad-scope signal `persona`. Impact: narrow single-file template tasks could be misestimated at `65` minutes and blocked from queueing anywhere that uses the shared bounded-size estimator, including native `task.update` from the PM lane.
- 2026-03-28 05:00:19 MDT - Queue decomposition still missed explicit anti-decomposition instructions when the brief used variant procedural headings and fenced code. Live proof before the fix: Sam.blog replacement task `49` stored `source_description` containing `## Exact Steps`, `## Critical Rules`, a fenced `python3 -c` block, and the explicit line `Do NOT decompose this task into subtasks. Execute it directly in a single session.`, yet it still decomposed into child tasks `50-54`, including procedural steps and code-snippet child `54: import os, json`. Impact: one concrete crawler task could reopen the same async churn pattern the earlier procedural-section fixes were meant to prevent, only now through heading-variant and code-block leakage.
- 2026-03-28 05:14:11 MDT - PM continuation snapshots still treated malformed legacy children under explicit no-decompose parents as real active child work. Live proof before the fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` kept naming child tasks `54` (`import os, json`) and `52` in continuation turns `fc42d3e4-cfbf-49e8-862f-9d4c351fb794` and `244dd119-a2fa-41e7-896b-484b1f587c05`, then immediately hit the existing named-task rediscovery guard instead of focusing back on parent task `49` or the real replacement draft `44`. Impact: even after the no-decompose parser fix stopped creating new malformed children, pre-fix child artifacts could still waste PM turns by hiding the true bounded parent behind bogus `child_tasks` / `active_child_tasks` state.
- 2026-03-28 05:56:35 MDT - Task-review preferred-root extraction still broke when the same task brief named both an input artifact and an output root. Live proof before the final fix: Sam.blog task `56`’s description says `listed in content/technonymous-index.json ... under content/posts/`, but fresh post-restart continuation summary `399` still said `Start with the preferred deliverable target content/technonymous-index.json` and the lane immediately re-read the index file. Root cause: `preferredTaskDeliverableRoot(...)` only checked the first `in/under content/...` match, stopped on the dependency artifact, and never reached the actual output root. Impact: historical deliverable reuse and review prompting kept treating an input artifact as the review target, reopening task-56 review churn on the wrong file family.
- 2026-03-28 06:17:00 MDT - Review-retry state refresh still lagged behind the actual synthetic prompt. Live proof before the fix: on task `56` session `76fd7c7c-d988-463e-871e-5e8876a43f3e`, retry turn `a826ec5e-bf3d-408b-8ee4-67e1782f1b0f` appended a fresh root-aware review prompt at message `51` and the assistant immediately switched to `content/posts` at message `52`, but the next guard error at message `53` still claimed the prompt named preferred deliverable target `content/technonymous-index.json`. Root cause: the retry append paths were writing fresh `task_review_action` user messages without updating `rt.initialMessageID` / `rt.initialMessageText`, so same-turn review guards kept consulting the stale continuation summary or prior prompt. Impact: even after the prompt builder was fixed, review lanes could still burn the same wrong-target guard loop until the entire turn/session restarted.
- 2026-03-28 06:24:00 MDT - Duplicate-successful-`file.write` early stops were still too vague across normal async task retries. Live proof before the fix: task `56` session `589c5986-b952-4d00-a1d9-65cf9730a59d` ended turn `1142bfe3-3454-4744-944b-f30dec774165` with `[Repeated identical successful file.write churn in this turn (2/3) ... take a narrower step]` after three identical successful rewrites of `content/posts/stop-preparing-your-kids-for-jobs.md`, but the immediate retry turn `9f4845ea-7a34-4181-9f01-3b73355f3de0` still opened with `The file_write tool isn't accepting content in this session context`. Root cause: the turn-stop system message did not tell the next continuation that the prior write already succeeded or that it must not start by rewriting the same path again. Impact: ordinary task retries could pivot into tool-troubleshooting prose and reopen the same duplicate-write loop even after the validator had already identified the exact written file.
- 2026-03-28 06:47:14 MDT - Project-continuation rediscovery guards still allowed one narrow but recurring PM waste path: `task.list(parent_task_id=...)` on active leaf tasks that the continuation prompt had already proved had no children. Live proof before the fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` named active task `56` as the live leaf lane, then still spent a tool call on `task.list(parent_task_id=task56)` before broader blocked rediscovery calls and another `validation_loop_blocked` stop. Impact: PM turns could keep burning one extra tool slot and one extra reasoning step on child inspection that could never reveal new work.
- 2026-03-28 07:07:48 MDT - Completed task output still was not reliably promoting into the parent project workspace. Root cause: the live worker wired both merge and push jobs to `delivery.UnavailableGitService{}`, so even when tasks completed their flow the delivery workers had no real git implementation to merge task branches back into the bound workspace.
- 2026-03-28 07:07:48 MDT - The delivery path also failed on the exact Sam.blog workspace shape even after a real git adapter was added. Live diagnosis before the fix: the active environment was `workspace | gated | repo_path=/Users/sam/otter-data/workspaces/sam-blog-rebuild-restart-12 | target_branch=main` with no default remote, but [`internal/delivery/merge_worker.go`](/Users/sam/dev/otter-camp/internal/delivery/merge_worker.go) always looked up `project_remote` after merging. Impact: a merge could succeed in git and then still roll back its DB state because remote lookup failed, leaving the parent workspace updated but the merge queue entry unresolved.
- 2026-03-28 07:52:00 MDT - Autonomous task completion still did not enqueue merge work even after the worker had a real git implementation. Live diagnosis before the fix: Sam.blog tasks `9,14,15,25,26,27,28,29,31,32,42,47,48,50,53` were already `done` with non-empty task branches like `task/9`, but every one still had `merge_entries = 0`. Root cause: runtime-owned flow completion never called `EnqueueForMerge(...)`, and `task.EnqueueForMerge(...)` itself only inserted `merge_queue_entry` rows without ever enqueuing the initial `merge_execute` job. Impact: completed task output could remain stranded in task worktrees forever even though the delivery workers were now capable of merging.
- 2026-03-28 07:29:14 MDT - After merge jobs finally started running, the parent workspace still failed the first real promotions. Live proof before the fix: the Sam.blog workspace at `/Users/sam/otter-data/workspaces/sam-blog-rebuild-restart-12` showed `No commits yet on main`, while `merge_execute` jobs for task branches like `task/14`, `task/15`, `task/31`, `task/32`, `task/47`, and `task/53` failed with `fatal: Non-fast-forward commit does not make sense into an empty head`. Follow-up inspection showed the task branches were also unrelated root histories with no merge base. Impact: even with merge queue entries and runnable jobs in place, the first promotion into a fresh parent workspace and any later merge across unrelated task histories would still dead-letter.
- 2026-03-28 07:29:14 MDT - Queued merge entries could also strand after their `merge_execute` jobs exhausted retries. Live proof before the fix: `merge_queue_entry` rows for the same Sam.blog tasks stayed `status=queued` while the corresponding `job_queue` rows were already `dead_letter`, and worker startup cleanup only repaired missing queue entries, not missing runnable merge jobs. Impact: once a merge job hit a transient runtime bug, the queue entry could remain permanently queued with no pending work to retry it after a deploy.
- 2026-03-28 07:44:13 MDT - Even after parent-workspace promotion started working, the first seeded project workspace could still retain stale untracked residue from its pre-merge runtime state. Live proof before the fix: `/Users/sam/otter-data/workspaces/sam-blog-rebuild-restart-12` still had junk like `.ottercamp/` and `planning/prd-spec/`, and a direct cleanup pass also removed stray untracked `scripts/` and `content/posts/` paths. Root cause: `deliverySeedTargetBranchFromSource(...)` seeded the unborn target branch with `git checkout -f -B target source`, but never cleaned untracked files afterward. Impact: the canonical parent workspace could look cluttered and misleading even after successful merge promotion, which made hands-on review and file-location correctness harder to trust.
- 2026-03-28 08:07:14 MDT - PM continuation snapshots still misclassified one important Sam.blog state: draft parents whose only child lanes were already terminally blocked. Live proof before the fix: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` showed parent tasks like `55` and `39` only in the `Draft parent tasks already have child work` bucket even though all visible children were blocked with `resume_policy=terminal_keep_blocked`, so the PM lane kept oscillating between “I need a fresh replacement task” and blocked broad rediscovery (`task.list`, `file.list content/posts`, `flow.list_templates`). Root cause: snapshot child activity only tracked `childTaskCount` and `activeChildTaskCount`, so blocked terminal child lanes looked the same as healthy child work. Impact: PM turns had no clear valid next move and kept spending tokens rediscovering context instead of directly creating the next bounded replacement child task.
- 2026-03-28 08:19:25 MDT - Replacement-child creation fixed the PM rediscovery loop, but the next Sam.blog inefficiency moved into queue decomposition itself. Live proof before the fix: PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` correctly created replacement task `58` under parent `55`, but task `58` then persisted decomposition metadata that treated numbered procedure text as child deliverables, producing `59` = `Use web_fetch to retrieve the page content as plain text` and `60` = `Use cli_execute with shell scripting or iterate in the agent loop. Process all 35 posts.` Root cause: `taskdecomp` did not recognize counted `fetch/scrape all N ...` titles as enumerated batch work, so it fell back to raw `Use ...` step lines in the description and treated those tool instructions as executable child tasks. Impact: a real scrape batch task turned back into low-value procedural child lanes, reopening PM/task churn instead of creating meaningful bounded execution slices.
- 2026-03-28 08:27:45 MDT - Even after fixing future decomposition, the already-created procedural child lanes were still live and noisy. Live proof before the fix: pre-deploy Sam.blog task `59` remained `in_progress` with title/description `Use web_fetch to retrieve the page content as plain text`, and task `60` remained `in_progress` with title/description `Use cli_execute with shell scripting or iterate in the agent loop. Process all 35 posts.`; task `60` also already carried `agent_turn_validation_guard.failure_code=path_required`. Impact: the new decomposition logic would help the next replacement task, but these existing malformed children could still keep PM snapshots focused on the wrong lanes and burn more async task retries before a fresh replacement-child pass ever happened.
- 2026-03-28 08:47:10 MDT - After the malformed-child preflight landed, PM continuations still had one focused-child mutation leak. Live proof before the fix: on Sam.blog session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, message `3929` correctly identified draft task `58` as the next bounded step, but `task.update(work_status=queued)` at `3930` left task `58` in `draft`, and the same turn escalated into ancestor promotion attempts on tasks `55` and `34` (`3933-3936`) before halting on the old bounded-size stop. Root cause: continuation snapshots ignored malformed child artifacts strongly enough to stop rediscovery, but still presented their parent as an ordinary runnable draft, and there was no project-lane guard blocking ancestor promotion once the focused child queue attempt proved to be a stale no-op. Impact: PM turns could keep paying for `58 -> 55 -> 34` mutation churn instead of creating the smallest fresh replacement child under the already-named focus task.
- 2026-03-28 08:57:20 MDT - Fresh bounded scrape child tasks were still being misclassified by the task-lane sibling-responsibility heuristic. Live proof before the fix: Sam.blog task `61` session `cea885e7-0b88-4e0d-a251-8a5f8f029da7` repeatedly blocked `web.fetch` at messages `6-17` with `task execution should stay inside the write-focused child lane`, then later blocked `file.write` at `39-40` with `task execution should keep task 61 ... in the discovery/listing lane`. Root cause: `shouldBlockTaskExecutionSiblingResponsibilityTool(...)` treated a mixed fetch-and-write batch task as a pure write-focused child and also let malformed procedural sibling `59` plus sibling batch tasks `62` / `63` distort the sibling-role split. Impact: the first real bounded child lane under replacement task `58` burned retries on contradictory sibling-lane errors instead of simply fetching and writing its assigned `1-12` post batch.
- 2026-03-28 09:14:57 MDT - Content-migration checkpoints and review targeting were still leaking parent-workspace state into live batch tasks even after the task-61 sibling-lane fix. Live proof before the fix: Sam.blog task `63` carried `content_migration_checkpoint.outputs` for first-batch files like `content/posts/hello-world.md` and `content/posts/practice-listen-to-albums.md` even though its own branch delta was the last-11 batch; review sessions `5f5b5eee-5a03-410c-a84e-ab1eefe5720d` and `15b90c22-a025-4078-ad4f-358f4667ef8c` then kept naming `content/technonymous-index.json` as the preferred deliverable target and blocking reads of the actual markdown outputs with `recovery_target_focus_required`. Root cause: the checkpoint builder first scanned the parent project workspace instead of the task worktree, and then a status-only worktree filter still missed task-owned outputs when they were already committed on the task branch. `sessionTaskDeliverablePath(...)` also preferred the stale recovery checkpoint target over real migrated outputs. Impact: execution/review lanes could keep re-anchoring on the input manifest instead of the migrated post files, wasting retries and carrying the wrong deliverable state across turns.
- 2026-03-28 09:26:36 MDT - Content-migration checkpoint filtering still dropped real task outputs even after the task-worktree / branch-delta slice. Live proof before the fix: fresh task-63 continuation/checkpoint messages `47`, `95`, and `124` on session `08b56f02-a02e-4329-bd15-cb24eacef05d` all still said `no migrated output files are on disk yet`, while the task worktree already had `content/posts/why-privacy-matters-part-2-what-to.md`, `content/posts/how-to-cook-a-steak-and-why-it-matters.md`, `content/posts/the-water-is-nearing-a-boil.md`, and `content/posts/practice-listen-to-albums.md` on disk with earlier mtimes. The persisted checkpoint file itself showed `## Outputs (none)`. Root cause: the engine still built migration state by full-scanning the worktree and then post-filtering the result, which left one more mismatch between tracked path selection and output classification. Impact: continuation turns kept restarting from “write the first output” instead of continuing from the outputs already written in the task branch.
- 2026-03-28 09:34:40 MDT - Batch content-migration review still over-expanded after the first correct output read. Fresh post-fix live proof: task-63 review session `075cf859-5b23-4077-8f0f-5f25236d36c0` correctly shifted its preferred deliverable target to `content/posts/practice-eliminating-streaming-video.md` and read that file first, but then messages `229-237` broadened into index/list inspection and hit `Repeated read-only discovery churn across 3 consecutive max-tool-call turns`. Impact: the checkpoint/output-target bug is fixed, but batch review still burns turns after the first representative deliverable read because it falls back into “enumerate the whole batch” discovery instead of moving directly to a bounded approval/rejection decision.
- 2026-03-28 09:50:43 MDT - The first batch-review follow-on prompt change still was not sufficient on its own because the native recovery-target validator kept treating blocked review retries as ordinary recovery lanes. Live proof before the fix: task-62 session `7e33dd11-d9a0-4799-9f4a-002e454b835e` already carried a synthetic `Review only... Start with the preferred deliverable root \`content/posts\`` prompt, but messages `311-312` at `09:46:28 MDT` still rejected both `file.read content/technonymous-index.json` and `file.list content/posts` with `recovery_target_focus_required`. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) only relaxed recovery-target reread blocks when `project_task.work_status = review`; once the runtime had blocked the task after repeated review retries, the same session kept the review prompt but no longer qualified for the native review-lane exception. Impact: batch review retries could continue bouncing on stale single-file recovery targets even after the turn engine correctly shifted the prompt to the deliverable root.
- 2026-03-28 10:12:30 MDT - Same-session review continuations still had one deeper reroute bug after the first native deliverable-root fix. Live proof before the fix: task-62 session `7e33dd11-d9a0-4799-9f4a-002e454b835e` was already on `current_flow_node_id=4dbe4e97-e438-431b-aa6e-80fc5eef1418` (`NodeType=review`), but max-tool-call continuation turn `47785d80-09ab-4887-baec-b3511c4bb7cb` and its model invocation still ran under worker agent `acd65fcc-0a14-4492-89ea-f7e286628166`. Root cause: fresh turn dispatch used `resolveSessionAgentForSession(...)`, but same-session `continueTurn(...)` cloned `rt.agent.ID` directly and never re-resolved the next agent. Impact: blocked review retries could keep reopening under the worker actor, which in turn preserved stale review-target assumptions and re-triggered `review actor must differ` family failures even after the task itself had moved to a review node.
- 2026-03-28 10:39:45 MDT - Review approval still treated ambient sibling output noise as if it were task-owned dirty work. Live proof before the fix: task-62 session `7e33dd11-d9a0-4799-9f4a-002e454b835e` listed `25` untracked files that were clearly sibling outputs/checkpoints (`oc-61`, `oc-63`, extra `content/posts/...` files outside task-62’s own batch), yet `flow.review_decision approve` kept failing `review_approval_requires_clean_workspace` and eventually pushed the model into a raw `git add -A && git commit` workaround. Root cause: review approval dirtiness used whole-worktree git status with no notion of current-task ownership, and the review approval commit path staged the entire worktree instead of issuing an empty review commit. Impact: batch review lanes could get trapped on cleanability checks and even absorb sibling artifacts into task-local commits instead of closing review cleanly.
- 2026-03-28 10:47:54 MDT - The first PM blocked-review auto-resume slice still picked the wrong child lane when multiple blocked review tasks existed in the project. Live proof before the follow-up fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, turn `377`, had already named blocked review tasks `61`, `62`, and `63` in messages `4116-4121`, yet system message `4126` resumed unrelated older blocked review task `10`. Root cause: `nextResumableBlockedProjectTask(...)` scanned all blocked review tasks in project order and did not prefer the task IDs already named in the active continuation prompt. Impact: the PM lane could wake stale unrelated review work and leave the actual hot blocked child tasks untouched, preserving PM churn even though the new auto-resume path existed.
- 2026-03-28 11:20:11 MDT - Batch review retries still had one narrower efficiency loop even after the preferred-root/checkpoint-output fixes. Live proof before the new fix: task `62` review session `59c169af-fd13-4022-9d75-a7b2568d449d` kept relisting `content/posts`, re-hitting the outside-root guard on `content/technonymous-index.json` / `.ottercamp/checkpoints/oc-62-content-migration.md`, and then re-reading the same successful checkpoint outputs (`mister-rogers-and-the-forgotten-art.md`, `jenny-can-i-have-my-privacy-back.md`, `the-next-thing-will-be-better.md`, `the-arithmetic-of-dying.md`) across turns `46-49`. Root cause: review retry prompts refreshed the preferred root and checkpoint-output summary, but they did not carry forward which exact outputs had already been inspected, and there was no guard stopping the next retry from relisting the same root and rereading the same files from byte `0`. Impact: review lanes could keep spending whole retry turns on duplicate evidence collection instead of turning existing output reads into `flow.review_decision`.
- 2026-03-28 11:31:00 MDT - Supervisor recovery still reopened terminal malformed-child lanes if they carried stale validation-guard metadata. Live proof before the fix: Sam.blog task `59` (`Use web_fetch to retrieve the page content as plain text`) was reopening every minute with fresh task sessions at `11:18`, `11:20`, `11:21`, `11:22`, `11:23`, `11:24`, `11:25`, `11:26`, and `11:27` MDT, and each one immediately ended with the malformed-child preflight halt message. Root cause: `classifyTaskResumeDecision(...)` prioritized any blocked validation guard like `command_is_required` before checking whether the latest blocked reason was terminal malformed-child / no-decompose evidence. Impact: supervisor recovery kept creating new `flow_node_execution` visits and `task_queue_processor` kickoffs for a lane the runtime had already proven should never resume.
- 2026-03-28 11:46:00 MDT - Batch review prompts still left one root-membership rediscovery gap even after the preferred-root and checkpoint-output guard fixes. Live proof before the new slice: task `62` review session `4737d210-2039-40aa-bf6f-db9e55421454` reopened with `file.list(content/posts)` at sequence `216`, then assistant sequence `217` said `13 files present` and tried to distinguish OC-62 vs OC-61 membership with `git.status` and `content/technonymous-index.json`. Root cause: review prompts only used the persisted checkpoint metadata, which still exposed just the latest 8 task-owned outputs, and the prompt did not mark that recovered output set as authoritative for task ownership. Impact: review lanes could keep paying for `file.list(content/posts)` and root-membership narration even though dependency rereads were already blocked.
- 2026-03-28 11:51:43 MDT - Batch review retries still had one narrower repo-state churn seam even after the authoritative checkpoint-output prompt landed. Live proof before the new guard: the same hot task-62 review session `4737d210-2039-40aa-bf6f-db9e55421454` still carried `git.status` as part of its recurring read-only churn family, and system message `295` ended the lane with `Repeated read-only discovery churn across 3 consecutive max-tool-call turns using file.read, git.status ...` even though the prompt already named the task-owned output set under `content/posts` as authoritative. Root cause: the turn engine blocked `file.list` / dependency rereads once the checkpoint output set was authoritative, but it still had no parallel guard against repo-state inspection tools (`git.status`, `git.diff`, `git.log`) in that same review mode. Impact: review retries could keep burning tool budget on repo-state narration instead of either reading the named outputs directly or turning existing evidence into `flow.review_decision`.
- 2026-03-28 11:59:40 MDT - The next active waste family moved back to task execution, and it was a batch-write targeting bug. Live proof before the fix: Sam.blog task `62` (`Fetch posts 13–24 ... save as markdown under content/posts/`) had `deliverable_root=content/posts`, but session `d5a6404a-f2fe-43d3-8059-a3f07a3c5774` still redirected every `file.write` to the first checkpoint output `content/posts/mister-rogers-and-the-forgotten-art.md`. In the same turn, messages `97-101` all wrote that one path even though the assistant was trying to create different files like `content/posts/discomfort-is-growth.md`, and system message `102` halted with `Repeated redirected file.write churn ... latest requested content/posts/discomfort-is-growth.md`. Root cause: `handleTaskFileWriteWrongPath(...)` enforced a single `sessionTaskDeliverablePath(...)` target even for multi-output tasks whose real contract is “many files under a preferred deliverable root.” Impact: batch content-migration tasks could thrash on off-target rewrites and never emit the intended set of files inside `content/posts/`.
- 2026-03-28 12:02:50 MDT - A closely related batch-task bug was still leaving dependency artifacts as the durable recovery target even after the write-root fix. Live proof before the normalization patch: Sam.blog task `63` (`Fetch posts 25–35 ... save as markdown under content/posts/`) still had `recovery_file_write_checkpoint.target_path=content/technonymous-index.json` in task metadata even though the same task also carried a `content_migration_checkpoint.outputs` set under `content/posts/`. In session `be5dc554-8bcb-4fc5-982d-2f1851e99a57`, tool result `71` showed `deliverable_path=content/technonymous-index.json` with `recovery_target_focus_required` after the model tried to inspect `content/posts/how-to-cook-a-steak-and-why-it-matters.md`. Root cause: `normalizeRecoveryCheckpointTargetForTask(...)` accepted any explicit-looking checkpoint path and never cleared recovery targets that sit outside the preferred deliverable root for multi-output batch tasks. Impact: review or recovery turns could keep getting dragged back to `content/technonymous-index.json` even after the lane had already produced the actual deliverables under `content/posts/`.
- 2026-03-28 12:11:30 MDT - Recovery resume turns still had one deeper file-target override even after the batch-root and outside-root checkpoint fixes. Live proof before the final fix: Sam.blog task `61` recovery session `741ebe25-8571-4274-af6b-b6fa5c7ff40d` clearly named `Target file: content/posts/stop-preparing-your-kids-for-jobs.md` in system messages `44`, `50`, and `56`, and the synthetic recovery prompt repeated `Treat content/posts/stop-preparing-your-kids-for-jobs.md as the target file for this recovery turn`. But the assistant kept emitting `file.write` to `.ottercamp/checkpoints/oc-61-content-migration.md`, and the runtime kept rewriting that to sibling output `content/posts/you-havent-had-to-wonder-about-anything.md` in tool results `42`, `49`, `53`, and `55`. Root cause: `handleRecoveryRejectedFileWriteContent(...)` canonicalized recovery file writes through `recoverySynthesizedFileWriteTargetPath(...)`, and that helper still preferred `sessionTaskDeliverablePath(...)` over the explicit recovery target carried by the active recovery turn. Impact: even explicitly targeted recovery resumes could keep writing the wrong sibling deliverable and churn on fake “file_write redirect” failures instead of repairing the intended file.
- 2026-03-28 12:26:20 MDT - Task-12 review target selection still had one same-root dependency-artifact loophole after the earlier outside-root fixes. Fresh live proof before the final clamp: task `12` review session `45746345-e9ae-4f38-94db-40b5dc790e7e` generated prompt message `577` with `Start with the preferred deliverable target content/technonymous/post-index.json`, even though the task contract says `Save each post as a markdown file with YAML frontmatter under content/technonymous/`. The turn then read `post-index.json`, blocked on `task.get` / `file.list`, and spent more review budget trying to reach the actual markdown outputs through that dependency artifact. Root cause: review-target reuse still accepted same-root historical/session artifacts like `content/technonymous/post-index.json`, and `reviewPromptDeliverableTarget(...)` needed an explicit deliverable-contract gate instead of trusting any target-like path inside the preferred root. Impact: markdown-batch review lanes could keep centering on a dependency index and then re-trigger guard churn before ever inspecting or judging the real deliverables.
- 2026-03-28 12:33:23 MDT - Dirty-workspace review retries still had an avoidable read-only churn hole. Fresh evidence before the fix: task `10` review session `ccfb79a9-45f8-4938-9e3b-2fa680c4eef5` hit turns `53` and `54` with retry prompts that already said `workspace is still dirty` and `reissue flow.review_decision immediately with decision=reject`, but the lane still reread the same `content/posts/...` deliverables and spent another max-tool-call turn. Fresh replacement session `d901ca6f-d4b5-4ded-8f31-3da57de44b54` reopened with the same reject-only dirty-workspace prompt family at messages `201`, `203`, and `215`. Root cause: the turn engine rewrote mistaken `approve` retries to `reject`, but it did not block non-decision tools once that reject-only dirty-workspace retry prompt was active. Impact: review retries could still burn full turns on `file.read` / `git.status` after the runtime had already made the only acceptable next action explicit.
- 2026-03-28 12:39:24 MDT - Even after the first dirty-workspace guard landed, the review lane still paid for one blocked-tool hop before it could get back to a decision. Fresh evidence before the follow-up fix: task-10 session `d901ca6f-d4b5-4ded-8f31-3da57de44b54` already had assistant `217` saying it would inspect dirty workspace state and then reject, but the turn still emitted blocked tool result `218` first. The replacement session `1139b4a5-49e7-41e7-87a6-cf7bbd27b724` repeated the same shape after dirty-approval failure `33`, with repo-state probes at `35/37/39` and blocked review-lane `cli.execute` at `41`. Root cause: the runtime blocked the non-decision tools but still waited for a second model/tool cycle instead of synthesizing the only acceptable `flow.review_decision reject` immediately from that dirty-workspace retry state. Impact: dirty-workspace review retries became cheaper, but still not as cheap as they could be, because every bad retry still paid for one blocked tool hop.
- 2026-03-28 12:56:50 MDT - Native recovery-target enforcement for batch execution tasks was still letting stale session history override task metadata. Live proof before the fix: task `61` session `3844d628-4139-4770-8745-f1208e19c1d3` had tool results `9` and `10` returning `recovery_target_focus_required` with `deliverable_path=content/technonymous-index.json` when the lane tried to read `content/posts/stop-preparing-your-kids-for-jobs.md` and `content/posts/you-havent-had-to-wonder-about-anything.md`, even though task metadata already carried `content_migration_checkpoint.outputs` under `content/posts` plus `recovery_file_write_checkpoint.target_path=content/posts/stop-preparing-your-kids-for-jobs.md`. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) derived recovery focus from `sessionRecoveryState(...)` and raw explicit-path heuristics before honoring metadata-derived batch outputs, so an early dependency-artifact read polluted both reread and mismatched-deliverable validation. Impact: batch recovery lanes could still get bounced back to `content/technonymous-index.json` and pay repeated validation-loop turns even after the engine had already narrowed the task onto `content/posts/...`.
- 2026-03-28 13:14:32 MDT - Task-63 recovery resume synthesis could still overwrite a real `content/posts/...` deliverable with checkpoint/status prose even after recovery-target selection was fixed. Live proof before the new guard: task `63` session `64e6354b-93c8-4222-b73c-96de0876b201`, turn `2a93ae85-a8c1-43b8-a2ca-cf2c762c3fea`, used the correct target path `content/posts/every-convenience-is-a-small-step.md`, but assistant/tool messages `147-150` wrote the recovery system message’s `Continuation summary draft:` block (`All 11 files already exist in content/posts/... according to the checkpoint ... I'll spot-check several`) into that file twice. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) did not classify content-migration checkpoint/status summaries as invalid reusable drafts, and persisted recovery-draft reuse could return previously accepted target/artifact/summary drafts without revalidating them against the latest reject heuristics. Impact: once target-path selection improved, the recovery lane could corrupt a real deliverable with checkpoint narration instead of just churning on the wrong checkpoint file.
- 2026-03-28 13:24:18 MDT - After the checkpoint-summary fix, the same task-63 recovery family still had one adjacent non-substantive draft shape: self-reflective recovery notes already written into the target file. Live proof before the follow-on fix: session `64e6354b-93c8-4222-b73c-96de0876b201` recovery prompt `198` embedded target-file text beginning `I keep making the critical error of writing the continuation summary text ...`, assistant `200` fetched the real article, and tool result `203` still rewrote `content/posts/every-convenience-is-a-small-step.md` with that 415-byte note instead of the fetched article body. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) still treated that self-reflective recovery-step narration as a usable durable draft because the reject heuristics only recognized checkpoint summaries, not “I need to read the index / fetch each URL / write the actual article markdown” notes already persisted in the target file. Impact: even after the checkpoint-summary branch was blocked, recovery resumes could still loop by rewriting a different kind of non-substantive status note into the real deliverable path.
- 2026-03-28 13:33:09 MDT - Multi-output content-migration execution lanes were still being forced into shell workarounds by the native recovery reread guard. Live proof before the fix: task `64` session `13c7bcb5-0b90-44b0-a694-a883624d1819` hit `recovery_target_focus_required` on `file.list(content/posts)` at tool result `42` even though the task contract is “for each ... save as markdown under content/posts/,” then spent more budget on `cli.execute` directory listings/readbacks (`44`, `46`, `48`) and eventually escalated to a Python bulk-writer plan at assistant `72`. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) only allowed deliverable-root inspection inside recovery reread guards for review lanes, not for execution lanes that legitimately need sibling inspection inside the same multi-output deliverable root. Impact: batch execution tasks could not use the native file tools for safe same-root inspection and instead burned extra turns on shell-based rediscovery or Python writer detours.
- 2026-03-28 13:43:29 MDT - Content-migration execution tasks could still burn expensive post-validation tails after the real content work was already done because checkpoint-only dirty workspaces were indistinguishable from real task-owned dirt at `git.commit` time. Live proof before the fix: task `10` session `8017151b-7eb2-4e20-88f9-851b3ad80cb9` showed `cli.execute` results `49` and `51` proving the only dirty path was `.ottercamp/checkpoints/oc-10-content-migration.md`, then assistant messages `50`, `52`, `54`, `56`, and `58` kept re-verifying already-written posts and planning a final `git.commit` anyway. Root cause: [`internal/tools/native/mutation_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/mutation_tools.go) only emitted the generic `task_git_commit_blocked`, and [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only short-circuited blocked `git.commit` after a same-turn deliverable mutation. Impact: content-migration task sessions could spend extra turns auditing completed markdown files just to try to clean up a runtime-owned checkpoint timestamp.
- 2026-03-28 14:05:12 MDT - Batch review prompting still had an output-set recovery hole when the task brief named one preferred file via recovery state but the full batch membership only lived in the task description. Live proof before the fix: task `64` review session `5a0c5569-b5f0-4ac9-ac55-c73eea9aea73` kept reopening with prompts that said `Start with the preferred deliverable target content/posts/practice-one-screen-at-a-time.md`, then spent full turns on sibling-read guard errors after the first successful read instead of moving into authoritative batch review. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only emitted authoritative checkpoint-output guidance when it already had a multi-output set from checkpoint metadata, and it was not extracting the explicit `content/posts/*.md` outputs enumerated in the task brief itself. Impact: multi-output review lanes could stay trapped in one-file-first prompt loops even though the task brief already listed the real batch outputs.
- 2026-03-28 14:12:42 MDT - Synthetic review/recovery prompts still had a deploy-freshness hole. Live proof before the fix: task-64 session `5a0c5569-b5f0-4ac9-ac55-c73eea9aea73` kept consuming stale pending `task_review_action` prompts from `14:02 MDT` across restarts, so new prompt logic could not take effect until those older pending prompts drained. Root cause: synthetic `task_review_action` / `task_recovery_resume` prompts did not stamp `repo_version`, and `shouldAppendSyntheticUserPrompt(...)` treated any pending same-source prompt as a blocker even when it came from an older binary. Impact: deploys could leave hot task-review and recovery lanes replaying stale instructions for multiple more turns, delaying or hiding freshly shipped hardening fixes.
- 2026-03-28 14:12:42 MDT - After the fresh authoritative prompts landed, one narrower review-guard bug remained. Live proof before the follow-on fix: task-64 session `5a0c5569-b5f0-4ac9-ac55-c73eea9aea73` generated fresh output-set-aware prompts at messages `926`, `942`, and `943`, but still hit same-root sibling `file.read` blocks at `1203-1208` after a successful preferred-target read at `1201`. Root cause: the same-batch sibling-read guard had already been relaxed, but the older preferred-target first-tool guard still blocked later same-turn same-root `file.read` calls even after the preferred target had already been read successfully. Impact: the runtime could carry the right authoritative prompt and still reopen the same review churn because one older guard path never learned about the successful preferred-target read.
- 2026-03-28 14:27:36 MDT - After the sibling-read guards were finally relaxed, task-64 batch review still had one remaining efficiency hole: authoritative same-root spot-checks could succeed, but they were still large enough and numerous enough to trigger `max_tool_calls` plus `Prompt input exceeded 20000-token guardrail` continuations. Live proof before the new cap: session `5a0c5569-b5f0-4ac9-ac55-c73eea9aea73` produced successful same-root `content/posts/*.md` reads at sequences `1535-1547`, then still ended on system message `1548` after another discovery-only continuation wave. Root cause: the preferred-target read rewrite only bounded the main target/tail path at `8192`, and authoritative sibling output reads had no parallel cap or prompt guidance. Impact: batch review lanes could get past the old guard walls and still pay multi-turn prompt-growth churn while sampling the same authoritative output set.
- 2026-03-28 15:50:40 MDT - Task-65 review retries still had two narrow but expensive gaps after the earlier preferred-target hardening. Live proof before the current slice: review session `c4b04c3b-328b-4189-8294-0815b283453b` reopened with target-only prompts for `content/posts/yonderosa-and-the-art-of-having-less.md` even though the task’s batch outputs already existed under `content/posts/`, and messages `168-174` showed the assistant eventually stating `this is sufficient evidence to reject` after the preferred-target miss but still not emitting `flow.review_decision`. Root cause: `reviewPromptCheckpointOutputPaths(...)` bailed out before task-worktree recovery when checkpoint metadata contained zero outputs, and `explicitReviewDecisionFromText(...)` did not recognize the live rejection wording. Impact: task-65 review could stay trapped on one-file-first retries and then pay an extra turn even after it had already articulated enough evidence to reject.
- 2026-03-28 16:03:47 MDT - Sam.blog PM continuations still lacked a precise way to recognize that a just-completed scrape batch superseded older blocked same-batch lanes. Fresh live proof before the fix: PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` hit continuation prompt `4560` immediately after task `67` (`Fetch posts 25–35 ...`) completed, but the turn still fell back into broad `task.list` rediscovery at `4562-4563` and another `validation_loop_blocked` stop at `4564`. Earlier in the same session, assistant `4545` also treated the content-migration tree as a generic four-task assessment and drifted through `content/posts` rediscovery despite already having named batch tasks. Root cause: project continuation task refs only carried shared hints like `deliverable_root=content/posts` and `depends_on_path=content/technonymous-index.json`, so the prompt could not tell the PM lane that older blocked/replacement-eligible `25-35` work was already covered by the latest completed batch. Impact: PM continuations could keep reopening replacement-planning or rediscovery work for batches that had just completed instead of treating those older lanes as superseded and moving on to the next unmet batch.
- 2026-03-28 16:05:46 MDT - The new batch-range prompt eliminated one PM ambiguity, but the next live mistake was even narrower: the model started calling `task.get` with the active project id. Fresh evidence before the follow-on fix: post-deploy Sam.blog continuation prompt `4575` correctly included `That completed task covers batch_range=13-24` and `batch_range=...` hints on the named tasks, but assistant `4576` still emitted `task_get(task_id=a6dbd331-7205-42d9-b0df-10105d5b5330)`, which is the project id, not a task id; tool result `4577` came back `not_found`, and the same turn then fell straight back into blocked `file.list content/posts` and `task.list(project_id=...)` rediscovery at `4578-4581`. Root cause: project continuation prompts still surfaced `Active project id: ...` without explicitly warning that this UUID is not a task record, and the rediscovery guard only blocked `task.get` for named task ids, not for the project id itself. Impact: even with better batch-level PM guidance, the first tool hop could still waste a real `task.get` on the wrong UUID and then reopen the broader rediscovery loop.
- 2026-03-28 16:08:18 MDT - After the `3443` follow-up, the runtime guard was present but the live PM prompt still lacked the new project-id warning. Fresh evidence before the alignment fix: worker-authored continuation prompt `4582` on Sam.blog session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` still showed `Active project id: a6dbd331-...` immediately followed by the active-task snapshot, with no `The active project id above is not a task_id` sentence, because that continuation was built from [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go), not the engine prompt builder. Impact: the runtime could block `task.get(project_id)` if it happened again, but worker-authored PM continuations were still missing the explicit prompt-level hint that should prevent the mistake in the first place.
- 2026-03-28 16:09:58 MDT - After the worker prompt alignment, the next PM waste remained a completed-batch verification loop. Fresh evidence before the guard: worker-authored continuation prompt `4587` on Sam.blog session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` correctly carried `That completed task covers batch_range=13-24`, `batch_range=...` task refs, the project-id warning, and the duplicate-replacement guard, but assistant `4588` still opened with `file.list content/posts` plus `file.read content/technonymous-index.json`; `file.list` was blocked at `4589`, yet the index reread still succeeded at `4590` and the assistant continued from that redundant verification. Root cause: project-continuation snapshot guards still blocked broad artifact browsing, but they did not block rereading the shared dependency artifact once the prompt had already established that the latest completed task covered that exact batch. Impact: PM continuations could still spend a file-read/tool slot confirming a finished batch against the shared index instead of moving directly to the next unresolved batch or blocker.
- 2026-03-28 16:32:11 MDT - Even after the completed-batch dependency-read fix, PM continuations still had a narrower same-family blind spot: they could see the latest completed batch, but they still did not surface older completed sibling batches or persist review-resume intent strongly enough to suppress replacement work. Fresh evidence before the new slice: prompt `4600` on Sam.blog session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` still only named blocked tasks `65/63/62/61`, so assistant `4605` tried to create fresh replacement tasks for both `1-12` and `25-35`, and both `task.create` calls hit bounded-size rejection at `4606-4607`. Root cause: PM continuation snapshots skipped `done` tasks entirely, so the prompt never surfaced that task `67` had already completed `batch_range=25-35`, and review-resume intent depended too much on stale blocked-reason strings instead of persisted validation-guard state. Impact: PM continuations could still waste turns planning or creating redundant replacement work for already-completed sibling batches.
- 2026-03-28 16:33:23 MDT - After completed-batch coverage landed in the prompt, one deeper engine mismatch remained: PM prompts now surfaced task `65` as `resume_review_decision`, but the project-continuation auto-resume path still ignored that lane whenever its persisted validation guard was `count>0, blocked=false`. Fresh evidence before the engine follow-on fix: prompt `4608` clearly named task `65` with `resume_policy=resume_review_decision`, yet the resulting PM continuation kept circling through `task.get`, dependency/root rereads, and later replacement creation instead of automatically resuming the exact review lane once the PM turn completed. Root cause: the engine-side review-resume selector only treated persisted validation guards as review-resumable when `blocked=true`. Impact: PM continuations could carry the right review-resume prompt state and still fail to hand off into the actual blocked review task, extending PM verification churn.
- 2026-03-28 16:33:23 MDT - Fresh Sam.blog task `68` exposed a separate recovery-target poisoning bug inside batch markdown execution lanes. Live evidence before the fix: task `68` (`Fetch posts 1–12 from content/technonymous-index.json and save as markdown in content/posts/`) immediately read `content/technonymous-index.json`, then every later `file.list(content/posts)` / `file.read(content/posts/*.md)` attempt came back with `deliverable_path=content/technonymous-index.json` plus `recovery_target_focus_required` (session `bb7c535f-6c45-4a3d-9140-25301b895f69`, sequences `7-10`, `48`, `69`, `97`, `118`, `141`, `148`, `152`, `168-169`). Root cause: both the native tool executor and the turn engine were willing to treat a recent `file.read` path as a recovery target even for `project_task` sessions whose deliverable contract clearly says “markdown files under content/posts/.” Impact: the first dependency read poisoned the entire recovery lane, reopening index-file focus churn and eventually blocking the task on a fake target.
- 2026-03-28 17:09:00 MDT - Active Sam.blog PM recovery still had one worker-side backlog bug after the earlier resume-selection patch. Fresh live evidence: project session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` was `active` with no `current_turn_id`, no pending/claimed `agent_turn` jobs, and `176` pending `project_continuation_resume` user messages; `174` of those rows already had completed turns attached, including newest resume `4692` with retries `0-2` all ending `validation_loop_blocked`. Root cause: `RequeueActiveProjectSessionsWithoutTurns(...)` would select/requeue `project_continuation_resume`, but consumed resume messages were never retired or refreshed into a deterministic `project_execution_continuation`, and `retireSettledProjectContinuationMessage(...)` still ignored `project_continuation_resume`. Impact: PM sessions could look idle while actually carrying an ever-growing stack of stale pending synthetic resume messages, which obscured the real live continuation state and kept the worker revisiting the wrong anchor.
- 2026-03-28 17:21:48 MDT - Sam.blog PM focused-draft guarding still had a stale-child occupancy bug after the resume cleanup. Fresh live evidence before the fix: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` emitted the focused-draft guard at messages `4735-4737`, naming cancelled task `69` as “the next bounded step,” and the assistant immediately replied `Task 69 was already cancelled. The guard is stale.` at message `4739` before falling back into specific file verification reads. Root cause: project-continuation child activity still counted terminal `done`/`cancelled` child tasks as live child occupancy, so a cancelled child could keep its parent out of the fresh actionable-draft snapshot, and `shouldBlockProjectContinuationFocusedDraftMutationTool(...)` trusted the prompt’s `FocusTaskLine` without revalidating the live task tree. Impact: PM continuations could block on dead child lanes, hide the real parent draft from the snapshot, and burn more turns on stale focus corrections instead of advancing the current bounded work.
- 2026-03-28 17:35:24 MDT - After the cancelled-child fix landed, the next PM continuation failure moved into the turn-completed consumer. Fresh live evidence before the fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` had already advanced far enough to focus task `55`, but `turn-engine.turn-completed` kept replaying event seq `380850` because task `65` was selected as `resume_review_decision`, `ResumeValidationBlockedTask(...)` returned `tasksvc.TaskResumeBlockedStateError{BlockerClass: flow_rejection_max_visits, BlockerReason: flow rejection max visits exceeded}`, and the handler returned that error instead of continuing. Impact: the event consumer retried the same completion event, the PM lane could not move on to the next bounded task, and the runtime burned operator attention on a resumability state that should have been treated as terminal for that lane.
- 2026-03-28 17:43:08 MDT - Completed-task merge promotion still had a dirty-parent-workspace failure. Fresh live evidence before the fix: worker startup kept enqueueing a missing merge job for merge queue entry `804136ec-5dfe-411f-b397-fe9d5f12b007`, and merge job `502f68fe-72d6-4c20-acab-0df8e9b03658` dead-lettered after three attempts because `git merge --no-ff --no-edit task/64` refused to run on dirty parent workspace [`/Users/sam/otter-data/workspaces/sam-blog-rebuild-restart-12`](/Users/sam/otter-data/workspaces/sam-blog-rebuild-restart-12). Root cause: [`internal/delivery/workspace_git_service.go`](/Users/sam/dev/otter-camp/internal/delivery/workspace_git_service.go) merged directly into the bound repo path without resetting/cleaning OtterCamp-managed parent workspaces first. Impact: completed task branches could not be promoted into the canonical workspace, so hands-on project output could lag behind completed task state and accumulate dead-letter merge retries.
- 2026-03-28 18:02:00 MDT - Async task continuation after content-migration checkpointing was still wasting a model call and degrading prompt quality. Fresh live evidence before the fix: task `70` session `ed9c1fc8-3bd4-4dcc-becd-e19440287b67` wrote multiple markdown outputs, persisted checkpoint file [`oc-70-content-migration.md`](/Users/sam/otter-data/task-worktrees/sam-blog-rebuild-restart-12/task-70/.ottercamp/checkpoints/oc-70-content-migration.md), then hit `[Prompt input exceeded 32000-token guardrail - continuing in a new turn.]` and `[Context compressed - continuing in a new turn.]`, but the next continuation still emitted `[Continuation summary] Continuation summary unavailable.` Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only tried `taskActiveRequestContinuationSummary(...)` and `taskExecutionContinuationSummary(...)` before falling through to a synthetic `continuation_summary` model request, and it never reused the already-persisted content-migration checkpoint metadata for async `project_task` sessions. Impact: large migration tasks paid for an extra model invocation exactly when context was already strained, and the resulting continuation prompt was weaker than the checkpoint data already on disk.
- 2026-03-28 18:28:00 MDT - Dirty-workspace review approval still had a same-turn leakage path after the earlier retry-prompt hardening. Fresh live evidence before the fix: task `70` review session `8ee30a7b-6e81-4b6a-8932-d49a8093c15a` successfully inspected the checkpoint-owned markdown outputs, called `flow.review_decision approve`, received `review_approval_requires_clean_workspace`, and then spent the rest of that same turn on blocked `git.status` / `git.diff` probes instead of stopping immediately. Root cause: the turn engine only hardened the follow-up retry prompt and reject-only retry turn; it did not stop the original tier2 batch after the dirty approval failure tool result landed. Impact: every dirty review approval failure still paid at least one or two unnecessary repo-inspection hops before the retry path could take over.
- 2026-03-28 18:36:00 MDT - Task execution continuation summaries were still vulnerable to dirty review prose leaking in from older sessions on the same task. Fresh live evidence before the fix: task `70` execution session `30a79813-2880-4ae1-be7f-2dd76623f193` hit `max_tool_calls`, then appended `[Continuation summary] The runtime is indicating uncommitted changes exist but the review sandbox is restricting me from inspecting them via git tools ...` even though the turn was not a review lane. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) reuses historical assistant finals from prior sessions on the same task, and the draft-rejection filter still treated dirty review workspace prose as acceptable continuation draft text. Impact: execution continuations could inherit review-lane narration, steering the next turn into the wrong context and wasting more recovery/verification calls.
- 2026-03-28 18:13:53 MDT - Task-70 recovery still had one deeper file-body fabrication bug after the dirty-review continuation fixes. Fresh live evidence before the fix: recovery-resume messages on session `30a79813-2880-4ae1-be7f-2dd76623f193` carried `Continuation summary draft` content that was just the task brief scaffold (`# Fetch posts 1-12 ... ## Objective ... ## Validation Criteria ... ## Evidence Expectations ...`), and the next turns wrote that synthetic scaffold into `content/posts/...` instead of article markdown. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) still allowed `inferredTaskDeliverableDraft(...)` to fabricate markdown starters for source-backed content-migration tasks, even though those tasks require fetching real page bodies from `content/technonymous-index.json` / `web_fetch` and cannot be reconstructed from the brief alone. Impact: recovery retries could keep re-writing fake article scaffolds, burn extra `file.write` / `cli.execute` corrections, and never converge on the actual post body.
- 2026-03-28 18:20:00 MDT - The first no-fake-fallback patch was necessary but not sufficient. Fresh post-deploy live evidence: task-70 session `30a79813-2880-4ae1-be7f-2dd76623f193`, recovery-resume message `285`, still carried `Continuation summary draft` with the same scaffold text even after `3463` was live. Root cause: the scaffold was also entering recovery through historical/system continuation-summary content, and [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) still treated that source-backed task scaffold as a valid reusable draft because the reject filter only knew about checkpoint summaries and recovery notes. Impact: task-70 could still restart from a fake article scaffold on the next retry even after the inferred fallback helper was disabled.
- 2026-03-28 18:23:08 MDT - After `3464` removed the fake continuation scaffold, the next remaining task-70 issue moved into the action prompt itself. Fresh live evidence before the fix: recovery-resume system messages `341`, `347`, `354`, `360`, and `366` on session `30a79813-2880-4ae1-be7f-2dd76623f193` already omitted the non-substantive continuation summary, but the paired user prompt at `367` still said `take direct recovery action from the durable drafts above`, and the assistant immediately fell back into `Let me check...` plus repeated `282`-byte placeholder rewrites at `375-378`. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) always phrased the recovery action prompt as if a reusable draft existed, even when all target/artifact/summary drafts were empty or rejected. Impact: no-draft recovery lanes could still behave as if a durable body was available and keep rewriting stubs or rechecking context instead of sourcing the real file body.
- 2026-03-28 18:33:41 MDT - Task-70 recovery still had one native focus-selection bug after the no-draft prompt hardening. Fresh live evidence before the fix: session `30a79813-2880-4ae1-be7f-2dd76623f193` had already written `content/posts/i-cant-picture-my-kids.md`, but later recovery retries still behaved as if that first output were the only valid deliverable target and kept returning `recovery_target_focus_required` instead of advancing to the next missing post. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) always chose the first `content_migration_checkpoint.outputs[]` path and never looked at the actual task workspace to see which checkpoint outputs already existed. Impact: batched content-migration recovery could stay pinned to an already-written file, which wasted retries and blocked progress toward the next missing output in `content/posts/`.
- 2026-03-28 18:39:24 MDT - After the checkpoint-output rotation landed, task-70 exposed one tighter heuristic gap in the same native layer. Fresh live evidence before the next fix: execution session `30a79813-2880-4ae1-be7f-2dd76623f193` still got `recovery_target_focus_required` on `file.list content/posts` even though the task clearly said to `write each post as a separate .md file under content/posts/`. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) only recognized markdown deliverables when the description literally said `markdown file(s)` or `save as markdown`, and it missed the equally authoritative `.md file` phrasing used by the live replacement task. Impact: the execution-lane batch root/sibling inspection allowance never activated for this task family, so the runtime kept blocking bounded `content/posts` inspection and forced more recovery churn.
- 2026-03-28 18:45:15 MDT - After the `.md file` wording fix, the last task-70 native blocker was a checkpoint-completeness case. Fresh live evidence before the next patch: session `30a79813-2880-4ae1-be7f-2dd76623f193` still returned `recovery_target_focus_required` for both `content/posts` and `content/technonymous-index.json` even though the task checkpoint already had multiple substantive outputs on disk plus `content/technonymous-index.json` in `checkpoint.artifacts`. Root cause: [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) had no narrow allowance for checkpoint-owned artifact/root inspection once the tracked batch outputs were already complete, so recovery stayed pinned to the last target file and blocked the exact bounded inputs needed to continue the batch. Impact: batched content-migration recovery could still thrash between repeated rewrites of one post and blocked rereads of the source index instead of moving on to the remaining posts.
- 2026-03-28 18:54:29 MDT - The next task-70 inefficiency moved up one layer into recovery prompt shaping. Fresh live evidence before this slice: even after the native checkpoint-owned `content/posts` / `content/technonymous-index.json` allowances landed, task-70 recovery still started from `content/posts/i-cant-picture-my-kids.md` as if it were the primary durable draft. That file already existed as one substantive batch output, but the recovery resume state/action prompt still foregrounded it, which encouraged another rewrite of the same file before the model ever used the checkpoint-owned batch context. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) treated the current target-file draft as equally authoritative for multi-output content-migration recovery, even when the checkpoint already proved the batch had substantive outputs plus owned source artifacts. Impact: task-70 could still spend recovery retries rewriting one completed post instead of using bounded checkpoint context to identify the next remaining output.
- 2026-03-28 19:03:38 MDT - After the checkpoint-context prompt fix, task-70 exposed the next narrower write-path bug. Fresh live evidence before this slice: the post-`3472` recovery turn on session `30a79813-2880-4ae1-be7f-2dd76623f193` finally started from `content/posts` + `content/technonymous-index.json`, but tool result `779` still rewrote an early batch write back onto `content/posts/i-cant-picture-my-kids.md` before the turn fetched the seven thin posts. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) still canonicalized recovery-turn `file.write` paths to a single explicit recovery target even when the same turn had already shifted into authoritative multi-output checkpoint context. Impact: batched content-migration recovery could still waste writes on the already-complete target post and slow or block the remaining files in `content/posts/`.
- 2026-03-28 20:28:28 MDT - PM continuation handling still had a narrow idle-session bug after the rediscovery guard slices. Fresh live evidence: Sam.blog project session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` was active with no current turn and no pending jobs after continuations `4865`, `4870`, and `4875` each hit `[Project continuation rediscovery guard blocked only broad rereads...]`, even though the continuation prompt already named draft parent task `44` as the exact replacement-child focus. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only had follow-up handling for generic PM replies, missing-dependency stops, blocked-review resumes, or runnable draft leaves. If the next bounded work was a staffed draft parent whose children were all replaceable blocked lanes, the completion handler returned without queuing a sharper continuation, and worker-side identical-continuation suppression then drained the PM session into idle. Impact: PM continuations could still dead-end after rediscovery-only stops instead of taking the one sharper turn needed to create the fresh replacement child.
- 2026-03-28 20:41:05 MDT - PM replacement-child continuations still had one same-turn waste path after the idle-session fix. Fresh live evidence: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` created replacement child task `72` at messages `4888-4889` and queued it at `4890-4891`, but the same PM turn kept spending extra tool calls on post-handoff verification until it eventually hit `max_tool_calls`. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) stopped early for blocked project mutations, not for successful project-continuation handoffs. Impact: even after the PM lane took the correct bounded action, it could still burn an extra continuation batch re-checking work that had already been handed to the child task lane.
- 2026-03-28 21:03:00 MDT - PM continuation snapshots still misclassified one important Sam.blog parent-closeout state after the replacement-child and handoff fixes. Fresh live evidence: prompt messages `5151-5152` on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` still showed parent task `44` under `Draft parent tasks need fresh replacement child work ... malformed_child_tasks=3`, even though child task `75` was already `done` as `Verify content/technonymous-index.json delivered — close parent OC-44` and sibling proofs `72` / `74` were also `done`. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only tracked open child lanes vs malformed child lanes; it had no closeout-ready state for parents whose deliverable had already been proven by a completed child. Impact: the PM lane could keep trying to create another replacement child under a parent whose only remaining real action was to advance/close the parent itself.
- 2026-03-28 21:21:00 MDT - The first closeout-ready PM prompt still carried one contradictory instruction after the parent-closeout fix landed. Fresh live evidence: prompt `5334` on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` correctly labeled task `44` as `completed_closeout_child_tasks=1` and told the lane to advance/close the parent directly, but the same focus guidance block still appended `Because that focus parent only has malformed or stale child artifact lanes ... create the smallest fresh replacement child task under it now instead.` Impact: the PM lane kept bouncing into broad artifact verification (`content/posts`, `content/technonymous-index.json`) and could still drift back toward replacement-child behavior even though the new snapshot had already proven the parent closeout state.
- 2026-03-28 21:44:10 MDT - Worker-generated `project_execution_continuation` prompts were still one generation behind the engine closeout-ready snapshot. Fresh live evidence: on Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, worker prompt `5594` still classified task `44` as replacement-child work and only surfaced `depends_on_path=content/technonymous-index.json`, while engine prompt `5622` for the same task tree showed task `44` as actionable closeout work with `deliverable_path=content/technonymous-index.json` and `completed_closeout_child_tasks=1`. Root cause: [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) still used older child-activity accounting and deliverable-hint extraction: it did not count draft children as open work, did not track completed closeout children, and only read deliverable hints from the raw task description instead of `metadata.decomposition.source_description`. Impact: whenever the worker synthesized the next PM continuation, the session could regress from the new closeout-ready engine prompt back into replacement-child / broad-artifact verification churn.
- 2026-03-28 21:46:13 MDT - After the worker snapshot fix, the next PM closeout blocker moved into parent completion semantics. Fresh live evidence: the first corrected worker prompt `5749` told the PM lane to close task `44` directly, assistant `5750` followed that interpretation, and tool results `5751-5752` then failed on `parent task requires child verification and passed integration before completion` plus `task_lane_owned_by_project_task_session`. Impact: even with the prompt fixed, the PM lane still cannot actually settle a closeout-ready parent when stale child/task-lane state makes the parent look incomplete or still owned elsewhere.
- 2026-03-28 21:56:04 MDT - Root cause of the task-44 closeout failure was in the shared parent-completion rule, not the PM prompt. [`internal/taskorchestration/metadata.go`](/Users/sam/dev/otter-camp/internal/taskorchestration/metadata.go) still treated blocked malformed child artifact lanes as executable work, so stale procedural children `45/46` kept `ValidateCompletion(...)` from allowing task `44` to finish even after the parent had completed child proof, a passed integration check, and a satisfied outcome assessment. Impact: PM/worker snapshots could correctly identify a parent as closeout-ready, but the runtime still rejected the closeout until those stale blocked child artifacts were ignored or manually repaired.
- 2026-03-28 22:18:00 MDT - The next PM churn family was not only worker “same snapshot” duplication. Fresh Sam.blog evidence on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` showed the continuation loop reopening off the same completed task `76` even after the runtime had already handed work to child lanes, because the child-lane state change itself kept mutating the PM snapshot fingerprint. Pre-fix shapes:
  - `5814` ended with `[Project continuation successfully handed off the focused replacement child work ...]`, but `5815` reopened the PM lane against the same completed task `76`
  - `5830`, `5841`, and `5847` each ended with `[Project continuation resumed blocked review lane task 78 ... flow.review_decision ...]`, yet `5831`, `5836`, and `5842` still reopened PM turns against the same completed task `76`
  - the continuation fingerprints kept changing (`5807=fae98fd9b949a73a`, `5815=58778a09acad903d`, `5821=5be963aa5aa9bba7`, `5831=b84871f7651c7b54`, `5836=b7ccaf19201b0d32`, `5842=a6c8ff5bf8733749`), so worker suppression keyed only on “same completed task + same snapshot fingerprint” could never catch the family
  - impact: the PM lane kept burning rediscovery-blocked turns after successful replacement-child handoff or blocked-review-lane resume, even though the project lane should have just waited for the child task lane to continue
- 2026-03-28 22:29:45 MDT - One narrower duplicate-continuation gap still remained after the worker-side fix. Root cause: [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only suppressed repeated `project_execution_continuation` retries on exact matching continuation fingerprints plus validation-loop stop reasons. If the runtime had already reached a child-lane wait state from the same completed task in a prior PM turn, and then the child-lane state change mutated the current snapshot fingerprint, engine-origin retries could still rearm another PM continuation even though the worker had already learned not to. Impact: identical same-completed-task PM wait states could still reappear through the turn engine’s retry path after deploys or generic/missing-dependency retries, even with the worker fix in place.
- 2026-03-28 22:36:52 MDT - After the same-completed-task suppression landed, the remaining PM inefficiency moved inside a single live turn. Fresh Sam.blog evidence stayed visible in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` at messages `5842-5847`: the prompt already named blocked task `78` with `resume_policy=resume_review_decision`, but assistant `5843` still emitted `task.get` / `task.list`, tool results `5844-5845` came back with the project-continuation rediscovery guards, and only then did the runtime append the correct review-lane resume message `5847`. Root cause: blocked PM rediscovery batches were always routed through the generic blocked-call path first, and the runtime-owned review-lane resume only happened later in `handleCompletedProjectExecutionContinuationTurn(...)`. Impact: even the correct PM lane could still pay one full blocked tool-result batch before the runtime performed the obvious bounded action it already knew how to take.
- 2026-03-28 22:47:12 MDT - Closed and archived chat sessions could retain orphan pending synthetic prompts indefinitely. Fresh live proof before the fix: task `78` (`scope_id=75a5606f-3577-4be8-bbf6-337fe3a68286`) had seven async `project_task` sessions already `status=closed`, no pending/claimed `agent_turn` jobs, and terminal `flow_node_execution` rows only, but the closed sessions still held pending user messages sourced from `task_queue_processor` and `task_review_action`. Root cause: session-close paths cancelled non-terminal turns but did not fail non-terminal `chat_message` rows, and worker cleanup only repaired `current_turn_id` / turn state, not orphan pending prompts on already-closed sessions. Impact: dead synthetic prompts could accumulate forever under closed sessions, distort queue/session inspection, and make the system look like there was resumable work when the underlying execution had already ended.
- 2026-03-29 05:18:16 MDT - PM continuation rediscovery guards still left one hole for exact task-owned deliverables already named in the continuation prompt. Fresh pre-fix proof on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: user continuation prompt `6605` already named task `88` as `work_status=in_progress deliverable_path=planning/sambot-feature-spec.md` and task `85` as `work_status=in_progress deliverable_path=templates/template-08-replace.html`, but turns `617-619` still emitted `file.read` on those same paths (`6608-6609` are the visible `not_found` results) before the runtime fell back into the generic missing-dependency / active-replacement stop chain. Root cause: `shouldBlockProjectContinuationSnapshotRediscoveryTool(...)` blocked broad task-tree rediscovery and completed-batch dependency rereads, but it did not yet treat prompt-listed live deliverable paths as PM-lane boundary violations. Impact: the PM lane could still waste a turn inspecting deliverables already owned by active child task sessions instead of stopping immediately on task-lane ownership.
- 2026-03-28 23:08:38 MDT - Blocked resumable review children were still being counted as “active child work” in project-continuation snapshots. Fresh live proof before the fix: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` repeatedly logged `requeued active project sessions missing continuation count=1` while staying idle (`current_turn_id=<nil>`, `0` pending/claimed `agent_turn` jobs, no fresh continuation messages after `22:47 MDT`) even though child task `78` was already back in `blocked`, not `review`. Root cause: the snapshot builders populated `ChildActiveDraftLine` from `childTaskCount > 0` instead of `activeChildTaskCount > 0`, so blocked review-decision lanes looked like live child occupancy. Impact: PM recovery suppressed the exact follow-up continuation needed to re-resume the blocked review lane, leaving the project idle until manual/runtime restart churn intervened.
- 2026-03-28 23:10:18 MDT - Root-authoritative review sample caps still had a same-batch overshoot hole after the first checkpoint-output-set fix. Fresh live proof before the second patch: task `78` review session `dd526b27-be5d-4759-ad3c-78064ff7bd7b` on `repo_version=3507` blocked `file.list content/posts` correctly, then assistant `85` still queued three post spot-check reads after the two named checkpoint outputs, and messages `86-88` all executed before the turn halted again at `89-91` without `flow.review_decision`. Root cause: the root-first sample-cap guard only compared `reviewCheckpointOutputSiblingReads` from completed prior results, so it could not stop a single assistant batch from queueing multiple new sibling reads past the sample cap. Impact: root-only review lanes could still waste the turn budget on a last extra batch of spot checks even after the checkpoint output set was already authoritative.
- 2026-03-28 23:19:50 MDT - Worker startup stale-job purge still assumed every historical `tool_result` row held valid JSON. Fresh live proof before the fix: right after the `3508` deploy, tmux worker pane `codex-e2e-20260324:0.1` logged `stale agent_turn purge failed ... purge stale agent_turn jobs (successful recovery resumes): ERROR: invalid input syntax for type json (SQLSTATE 22P02)`. Root cause: both the startup purge path and the active-session recovery helper cast `tool_result.content::jsonb` directly while scanning for completed recovery-resume `file.write` evidence, but some legacy `tool_result` rows are plain-text noise rather than JSON objects. Impact: the worker emitted a runtime error on every purge cycle for that path and could skip stale recovery-resume cleanup until restart/manual intervention.
- 2026-03-28 23:34:00 MDT - Review approvals that hit `blocked to in_progress requires direct human operator continuation` were still falling into the generic “review turn completed without calling flow.review_decision” retry family. Fresh live proof before the fix: task `78` review session `265cfc47-fd50-413f-b24f-5e775df41ba4` recorded a real `flow.review_decision approve` bounce, but retries `20-51` kept generating operator-facing prose and reopening the same review lane because the runtime never treated that blocked approval as terminal review work, and the worker snapshot path still let stale `review_decision_required` guard state override the newer blocked reason. Impact: the review lane burned repeated retries after the approval was already recorded, and PM continuations could keep surfacing that lane as `resume_review_decision` instead of a human/operator dependency.
- 2026-03-28 23:45:53 MDT - Multi-output content-migration recovery checkpoints still accepted stray single-file targets as long as they were somewhere under the preferred deliverable root. Fresh task-78 proof: metadata already named checkpoint-owned outputs `content/posts/README.md` and `content/posts/VERIFICATION.md`, but `recovery_file_write_checkpoint.target_path` drifted to `content/posts/happy-new-yeartechnonymous-isnt-dead.md`. Impact: close-out recovery could oscillate between the wrong sampled post and the real checkpoint-owned outputs, which wasted retries and weakened the task’s durable recovery state.
- 2026-03-28 23:53:32 MDT - `task_git_commit_blocked` immediate-stop logic still missed same-turn mutations when the write and the blocked `git.commit` happened in separate assistant/tool batches. Fresh task-78 proof: the turn edited `content/posts/README.md`, then hit `git.commit -> task_git_commit_blocked`, but still kept going into another verification step and another live model invocation. Impact: task lanes could still spend extra tool/model budget after the runtime had already proven “deliverable mutated, commit is runtime-owned, stop now.”
- 2026-03-29 00:13:58 MDT - Review lanes still treated checkpoint-owned close-out outputs as normal readable evidence even when those files had been polluted by review-session narration or explicit approval prose. Fresh task-78 proof on review session `4e2fe5e3-cdc4-4f63-8c14-511cae1ac459`: `content/posts/README.md` contained `The workspace is blocking further reads...` plus `The deliverables satisfy the task's acceptance criteria. Approving.`, while `content/posts/VERIFICATION.md` held the real import summary/README body. Impact: the review lane reread and reasoned about a self-referential meta file instead of getting a direct invalid-deliverable signal, so it spent more turns on sibling spot-checks and retry churn before the task finally rejected back to work.
- 2026-03-29 00:27:53 MDT - Reopened work after review rejection was still losing concrete repair evidence and regressing to generic recovery scaffolds. Fresh task-78 proof on work session `2565c314-693a-4e54-8c60-7a4f06ae1a25`: the prior review blocker already said `content/posts/README.md` held review-summary text while `content/posts/VERIFICATION.md` held the substantive summary, but the recovery resume state did not surface that evidence. Instead it treated an inferred task-brief scaffold as a durable draft, told the lane to reuse it, and the very next model cycle fell back into `recovery_target_focus_required` reads against `content/posts` and `.ottercamp/checkpoints/oc-78-content-migration.md`. Impact: post-rejection work lanes could still waste turns rediscovering a defect the runtime had already recorded.
- 2026-03-29 00:27:53 MDT - A second checkpoint-owned review-placeholder variant was still escaping the invalid-deliverable classifier. Fresh task-78 proof on review session `337fe47d-a163-4b9f-97dd-4089fbbaed47`: `content/posts/README.md` returned a combined review blob saying both checkpoint outputs were “rejected placeholders,” that the task’s acceptance criteria could not be satisfied, and `Rejecting this review.` Because that blob was returned as normal file content, the review lane kept rereading, spot-checking, and narrating instead of immediately treating `README.md` as invalid review evidence. Impact: review turns could still burn tool budget on a second family of meta-summary content even after the first review-summary classifier landed.
- 2026-03-29 00:46:40 MDT - PM closeout-ready parents were still being misclassified as replacement-child work whenever the only remaining open child lanes were terminally blocked stale children. Fresh Sam.blog proof on project session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: after closeout child task `79` finished, the snapshot still kept parent task `34` in the replacement bucket, so the PM lane bounced between “create the smallest fresh replacement child” and rediscovery guards instead of closing task `34`. Root cause: both the turn-engine and worker snapshot helpers required `childTaskCount == 0` for closeout readiness, even when `completed_closeout_child_tasks=1` was already present and every remaining open child was `resume_policy=terminal_keep_blocked`. Impact: PM continuations kept reopening a workstream that task `79` had already proven complete.
- 2026-03-29 00:51:53 MDT - Prompt guidance alone still allowed duplicate closeout child creation under a now-closeout-ready PM focus parent. Fresh Sam.blog proof on the first post-`3517` PM replay: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` consumed stale continuation prompt `5935`, then created task `80` (`Close out OC-34: verify content/posts/ contains all scraped posts and mark parent complete`) even though task `79` had already completed the same closeout handoff. Root cause: the runtime had fixed the snapshot classification for new prompts, but there was still no direct `task.create` guard that revalidated current closeout-ready parent state at mutation time. Impact: already-queued pre-fix PM continuations could still create redundant replacement children for the same completed batch before the new snapshot text had a chance to take effect.
- 2026-03-29 00:57:21 MDT - The first `3518` duplicate-child guard was still too narrow for the real Sam.blog state. Fresh proof on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: after task `80` completed, the next PM replay still created task `81`, because the runtime only treated “completed closeout child proof + all remaining children are replaceable blocked” as closeout-ready. Live child task `71` was `resume_policy=manual_recovery_repair`, so parent task `34` failed that narrower check even though tasks `79` and `80` already proved the scrape/import deliverable was complete. Impact: any stale blocked child lane outside the narrow `replaceable_blocked_child_tasks` subset could still keep a proven-done parent in replacement-child churn.
- 2026-03-29 01:08:41 MDT - Review-target inference was still treating parameterized output contracts as concrete files. Fresh task-81 proof on review session `903bdd67-5384-430e-8fa0-a992cc7c5648`: retry prompt `305/306` insisted on reading literal `content/posts/{slug}.md`, then blocked `file.list`, sibling reads, and even `git.log` until the lane somehow inspected that impossible placeholder path. Root cause: parameterized explicit deliverable paths and content-migration checkpoint outputs like `content/posts/{slug}.md` were still eligible as preferred deliverable targets. Impact: review retries for batch markdown tasks could burn multiple turns on impossible placeholder-file guards instead of falling back to the output root or a concrete historical deliverable.
- 2026-03-29 01:15:27 MDT - Async task continuations were still accepting model-authored summaries that had already turned into runtime-owned handoff plans. Fresh task-81 proof on work session `92a98bde-c9b3-4f32-b9ce-f71e75b3bff7`: message `130` stored `[Continuation summary] The posts are all committed... Let me clean up by staging the deletion of the placeholder and committing:`, and the next turn immediately replayed `git.status`, `git.commit`, and `flow_owned_done_blocked` churn. Root cause: `continuationSummaryLooksUnavailable(...)` rejected pseudo-tool transcripts, but it still admitted first-person runtime-owned plans like “let me commit the current state” and “let me update the task status.” Impact: compressed async task turns could still start from a summary that primed the model to spend its budget on runtime-owned commit/flow handoff steps instead of continuing the deliverable work.
- 2026-03-29 01:15:27 MDT - `task_git_commit_blocked` immediate-stop handling was still too narrow for checkpoint-only task mutations. Fresh task-81 proof on session `eb7c666e-00f3-4b53-862d-3eba852cecfd`: the turn first ran `cli.execute` that wrote the checkpoint (`stdout_inline=wrote checkpoint`), then hit `git.commit -> task_git_commit_blocked`, but instead of stopping immediately it went on to `task.update` / `flow_owned_done_blocked` and then assistant prose about advancing the flow. Root cause: `handleToolValidationResults(...)` only treated successful `file.write` / `file.edit` results as same-turn mutations for the commit-handoff stop, so checkpoint updates done through `cli.execute` were invisible to that guard. Impact: task lanes that had already mutated runtime-owned checkpoint state could still burn an extra blocked commit/status-update handoff cycle before the runtime stopped the turn.
- 2026-03-29 01:22:35 MDT - Task-81 recovery still had one narrower poisoned-draft family even after the continuation-summary sanitizer landed. Fresh live proof on new session `2175bbde-f279-4b13-9c23-e418626de537` showed the target file [`content/posts/stop-preparing-your-kids-for-jobs.md`](/Users/sam/otter-data/task-worktrees/sam-blog-rebuild-restart-12/task-81/content/posts/stop-preparing-your-kids-for-jobs.md) literally contained `The posts are all committed... Let me clean up by staging ... and committing:`. On the new binary, recovery turns then kept writing that same 231-byte handoff prose back onto the markdown file (`file.write` tool results `6`, `11`, and `17`) and later reread it raw as if it were valid task evidence (`file.read` tool result `80`). Root cause: the runtime-owned commit-handoff prose family was still not recognized by `recoveryFileWriteDraftRejectReason(...)`, native `file.write` non-substantive-content checks, or native `file.read` placeholder-deliverable classification. Impact: recovery lanes could overwrite a real markdown post with runtime handoff prose, then treat that poisoned file as reusable draft/evidence and burn more retries before fetching the actual content.
- 2026-03-29 01:32:41 MDT - The first `3525` replay proved that the exact commit-handoff paragraph was gone, but task-81 still had a second poisoned-markdown variant. Fresh live proof on the same session `2175bbde-f279-4b13-9c23-e418626de537`: the file now contained `So stop-preparing-your-kids-for-jobs.md is the only real post file missing valid frontmatter. I need to fetch the actual post and write it. Also {slug}.md is a bad placeholder file I should remove.` and `file.write` tool results `118` / earlier `101-112` still succeeded with that status prose. Root cause: the narrower `3525` filter only matched runtime-owned commit/flow handoff language; it still missed content-migration status narration about missing frontmatter, placeholder cleanup, and needing to fetch the actual post. Impact: recovery turns could still poison the markdown file with operational status notes even after the specific commit-handoff summary family was blocked.
- 2026-03-29 01:48:48 MDT - Task-81 still has a third poisoned-deliverable variant on the live `3526` tree: the task worktree file [`content/posts/stop-preparing-your-kids-for-jobs.md`](/Users/sam/otter-data/task-worktrees/sam-blog-rebuild-restart-12/task-81/content/posts/stop-preparing-your-kids-for-jobs.md) now contains review/meta commentary (`preferred target`, `mismatched deliverable`, `fabricated "review summary"`, `not YAML frontmatter + markdown post body`, `meta-commentary about the review process itself`) instead of article markdown. Root cause: the existing runtime/placeholder filters were still too phrase-specific, and `recoveryFileOutputContext(...)` could still surface rejected fallback drafts as reusable context once they were on disk. Impact: task lanes can still write or reread self-referential review prose at a real deliverable path, then reuse that poisoned file as recovery context instead of replacing it with the actual markdown body.
- 2026-03-29 01:58:32 MDT - Post-`3527` task-81 recovery still had a live directory-discovery bypass even after the poisoned-draft slice. Fresh proof on session `2175bbde-f279-4b13-9c23-e418626de537`: with recovery prompt `242-243` already naming checkpoint-owned root/artifact context and a concrete `content/posts/...` target, assistant message `244` emitted `cli_execute(command="ls content/posts/")`, tool result `245` returned the full directory listing, and assistant `246` started another verification/count loop. The same session also kept rereading `content/technonymous-index.json` (`225`, `239`) under the same prompt family. Root cause: `shouldBlockTaskRecoveryReadScopeTool(...)` only guarded `file.read`, and its same-task fallback depended on task-number path hints that do not exist for `content/posts/...` targets. Impact: recovery turns can still side-step the target-focus guard with read-only shell discovery and sibling input rereads even when the prompt already says to continue from checkpoint-owned context.
- 2026-03-29 02:18:40 MDT - Even after the `3528` recovery read-scope clamp, task-81 content-migration recovery still had a post-fetch discovery hole. Fresh live proof on session `2175bbde-f279-4b13-9c23-e418626de537`: assistant `372` successfully fetched the exact article body via `web_fetch https://technonymous.org/stop-preparing-your-kids-for-jobs`, but the next model step still emitted an empty `file.write` and the runtime had no stateful guard saying “source content is already in hand, stop rediscovering and write now.” Root cause: recovery enforcement tracked local rereads, but it did not persist the fact that the exact external source body had already been fetched in-turn for a `content/posts/...` target. Impact: recovery turns could keep spending extra tool/model budget on post-fetch discovery or malformed follow-ons instead of cleanly forcing a write-or-blocker step.
- 2026-03-29 02:18:40 MDT - Active PM sessions that lose their pending continuation on a rediscovery-blocked turn were still effectively invisible until a repo-version change, and that was easy to misread from the logs. Fresh Sam.blog proof: after restart, worker pane `codex-e2e-20260324:0.1` logged `requeued active project sessions missing continuation on startup count=1`, but session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` still had no pending `project_execution_continuation`, no pending/claimed `agent_turn` job, and its latest failed continuation already carried `repo_version=3528`. Root cause: worker suppression for “same completed task + same snapshot” did not consider `metadata.repo_version`, so a new binary could not refresh a rediscovery-blocked continuation unless some other snapshot field changed. Impact: deploys could leave PM lanes idle behind an older failed continuation even though the code path meant to recover them was running.
- 2026-03-29 02:35:55 MDT - Sam.blog PM continuation snapshots still missed one live parent-closeout signal even after the repo-version refresh reopened the session. Fresh live proof before the matcher fix: task `79` (`Close out scrape-import workstream: verify all 35 posts in content/posts/ and mark parent OC-34 complete`) and task `80` (`Close out OC-34: verify content/posts/ contains all scraped posts and mark parent complete`) had both finished, but the PM session previously kept parent task `34` out of `Actionable draft tasks already in the tree` and instead surfaced only blocked child relics. Root cause: the closeout-child detector in both [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) and [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) required one of a narrow set of legacy closeout phrases before it would use the existing `OC-<n>` / `parent <n>` fallback, so titles that said `close out OC-34 ... mark parent complete` never incremented `completed_closeout_child_tasks`. Impact: the PM lane hid the real closeout-ready parent work, reopened blocked child churn, and delayed the planning/closeout turn that should have started from task `34`.
- 2026-03-29 02:39:04 MDT - Even after the PM snapshot started surfacing task `34`, the closeout-ready prompt still left one expensive contract gap. Fresh proof on turn `9cf90141-f798-4cc0-bb4b-8bb7fd411b62`: the PM lane tried to close parent task `34`, hit `parent task requires child verification and passed integration before completion`, then kept going into blocked child-cancellation work and finally bounced on `task_lane_owned_by_project_task_session`. Root cause: the closeout-ready prompt told the PM lane to “advance or close the parent” but never told it the exact `parent_orchestration` metadata it must record first, and the blocked-mutation early-stop path did not treat `ErrParentCompletionRequirements` as terminal guidance. Impact: each PM retry could still burn extra tool calls rediscovering the parent-closeout contract and poking blocked child lanes after the runtime had already said what was missing.
- 2026-03-29 03:16:11 MDT - PM continuation repair still treated stale duplicate closeout drafts as actionable remaining work. Fresh pre-fix Sam.blog proof: project `a6dbd331-7205-42d9-b0df-10105d5b5330` still had exactly three drafts in SQL (`39`, `55`, `58`) even though equivalent deliverable families were already fully closed out elsewhere (`44/75` for `content/technonymous-index.json`, `34/79/80` plus the earlier scrape/import replacements for `content/posts`). Session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` had no `current_turn_id`, no pending/claimed `agent_turn` jobs, and only failed continuation messages, yet worker startup still reported `requeued active project sessions missing continuation count=1`. Root cause: PM snapshot/draft counting only trusted deliverable identity extracted from descriptions or existing parent metadata, so duplicates like task `39` never got a deliverable path from their title text and could not be marked superseded by the already-closed done families. Impact: PM continuation recovery could keep waking or appearing to wake the project lane because of dead duplicate drafts instead of only when genuinely actionable blocked work remained.
- 2026-03-29 03:31:29 MDT - `RequeueActiveProjectSessionsWithoutTurns(...)` still overstated PM recovery after a suppressed/no-op continuation decision. Fresh pre-fix `3534` proof on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: latest user message `67cc224a-267a-46d3-9aa1-51b609515efa` was already `failed|project_execution_continuation`, there were `0` pending/claimed `agent_turn` jobs, yet the worker still logged `requeued active project sessions without turns count=1`. Root cause: the worker incremented `repaired` even when `ensureProjectContinuationMessageDecision(...)` suppressed the continuation or returned `uuid.Nil`, and the stale `project_bootstrap` path could still fall through toward dispatch with no replacement message. Impact: ghost repair counts made drained PM sessions look active again and obscured whether the worker had actually queued any real continuation work.
- 2026-03-29 03:46:41 MDT - Executable-task preflight still allowed pure procedural instruction artifacts to become runnable work. The old Sam.blog backlog already showed the concrete family: tasks like `Use browser tools to navigate to https://technonymous.org`, `Use browser_extract_text to get the page content and identify post links`, and `Use cli_execute with shell scripting or iterate in the agent loop` could be drafted or queued if they had a flow template and did not exceed the bounded-size splitter. Root cause: queue-time validation enforced flow-template presence, bounded size, and decomposition/orchestration rules, but it never required a deliverable-focused executable contract. Impact: tool-procedure artifacts could still leak into execution, burn turns, and then need downstream PM/reviewer cleanup after the fact.
- 2026-03-29 04:00:00 MDT - PM rediscovery-only validation stops still had one dead-end supervisory failure mode. Fresh pre-fix proof on `repo_version=3536`: session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` could hit `[Project continuation rediscovery guard blocked only broad rereads ...]`, finish the turn cleanly, and then sit drained with no narrower PM follow-on despite the snapshot already naming the next blocked task family. Root cause: [`handleCompletedProjectExecutionContinuationTurn(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only turned that stop into another action for very specific families (missing dependency or replacement-child work); otherwise it returned without synthesizing a narrower supervisory retry. Impact: PM stop conditions were good enough to prevent repeated broad rereads, but not good enough to turn that stop into one bounded next move, so project sessions could still go idle after the first clean guard hit.
- 2026-03-29 04:25:47 MDT - Focused PM rediscovery retries still spent one wasted supervisory turn when the named blocked task already owned execution through `current_flow_node_id`. Fresh pre-fix `3539` proof: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` created a focused retry around task `71`, but the retry only ended on `task_lane_owned_by_project_task_session`. I first checked for active `project_task` chat sessions and found only closed rows, which looked like a mismatch. The actual runtime ownership signal in the hot case was `task.current_flow_node_id`, matching the same task-lane boundary used by native mutation blocking. Impact: the PM lane paid one extra continuation turn even though the task lane already owned the work and the project session could have stopped immediately.
- 2026-03-29 04:43:27 MDT - The first live-lane shortcut was still too broad. Fresh SQL on Sam.blog showed blocked tasks like `81`, `78`, `77`, `71`, and `56` retaining non-null `current_flow_node_id` values while their async `project_task` chat sessions were closed and their matching `flow_node_execution` rows were already terminal. Root cause: PM and native mutation guards treated `current_flow_node_id` alone as proof of an active task lane. Impact: PM recovery can be suppressed behind dead task lanes that no longer own runnable work.
- 2026-03-29 04:43:27 MDT - Project continuation summaries could still regress to stale PM requests even when the current turn was triggered by a fresh `project_execution_continuation`. Fresh Sam.blog proof: continuation message `6159` named tasks `71` and `81`, but max-tool-calls summary `6176` reverted to the older `OC-45 through OC-48 ... content/technonymous-index.json` storyline. Root cause: the continuation-summary path classified `project_execution_continuation` messages as history-rooted resume prompts, then looked backward for an older request anchor. Impact: PM resumes can reopen stale recovery families after a clean bounded continuation turn, even when the current trigger already contains the right focus.
- 2026-03-29 05:46:48 MDT - Task-85 template recovery still had one same-batch waste hop even after the direct-write clamp landed. Fresh live proof on session `324f566b-751b-4d09-915f-f821abbfd37a`: turns `43-44` already blocked the first reread with `recovery_direct_write_required` (`336` / `296`), but the same assistant batch still spent another blocked `file.list templates` result (`337` / `297`) before ending on repeated focus stop `338` / `298`. Root cause: direct-write-only mode only blocked `file.read` / read-only `cli.execute` and had no pure blocked-batch stop path in `dispatchTools(...)`, so a recovery assistant could still burn a second blocked rediscovery call after the runtime had already said “write the file body or one blocker sentence.” Impact: hot template-recovery retries were cheaper than before, but still not fully bounded; each bad batch could pay two blocked tool results instead of one terminal stop.
- 2026-03-29 06:00:41 MDT - Synthetic recovery/continuation prompts from older runtime generations could survive deploys and keep draining after restart. Fresh live proof on task `85` session `324f566b-751b-4d09-915f-f821abbfd37a`: after the `3549` restart, the session still had multiple pending synthetic recovery prompts with `metadata.repo_version=3548`, and the latest turns were consuming those old prompts instead of proving the new direct-write-only stop. Root cause: the turn engine already knew not to append a new synthetic prompt behind a pending older-version synthetic prompt, but worker cleanup did not retire stale pending synthetic prompts or their dispatch jobs after a repo-version change. Impact: deploy-only fixes could look “not live yet” for longer than necessary because hot sessions kept replaying older synthetic prompts instead of switching cleanly onto the new runtime generation.
- 2026-03-29 06:09:09 MDT - Repeated-resume halting for empty recovery `file.write` calls was still missing the correction wording that actually survives on hot sessions. Fresh task-85 proof on session `324f566b-751b-4d09-915f-f821abbfd37a`: turns `55-60` each began with checkpoint failure reason `file.write for templates/template-08-replace.html was emitted without \`content\`; the next retry must provide the full file body instead of another empty file.write call`, but the next resumed turn still granted one more bounded correction (`417`, `425`, `433`, `441`, `449`) before the direct-write-only stop fired again. Root cause: `RecoveryFileWriteFailureIsMissingContent(...)` only matched `retried without content`, so the correction wording from the prior turn did not count as a prior missing-content failure. Impact: the direct-write recovery family stayed bounded within each turn but never escalated across turns, which is why task `85` still dominated last-hour token burn after the cheaper stop landed.
- 2026-03-29 06:24:31 MDT - After the classifier widening, task `85` still looped because the runtime had two different checkpoint sources for the same recovery family. Fresh live proof on session `324f566b-751b-4d09-915f-f821abbfd37a`: message `457` already surfaced `Checkpoint failure reason: file.write for templates/template-08-replace.html was emitted without \`content\` ...` inside the synthetic recovery resume state, but the same resumed turn still appended message `460` with another bounded correction instead of halting. Root cause: prompt shaping reads from `recoveryFileWriteCheckpointCandidate(...)`, which can recover checkpoint state from the initial synthetic resume message metadata, while the actual halt helpers (`recoveryCheckpointShowsMissingContent`, `recoveryCheckpointShowsMissingCommand`, repeated-successful-write checks, and the failure-reason strengthening helpers) were still reading only `currentRecoveryFileWriteCheckpoint(...)`, which ignores that resume-message metadata path. Impact: synthetic recovery turns could literally display the checkpoint they should obey, then still spend one more retry hop because the stop path was consulting a narrower checkpoint source than the prompt.
- 2026-03-29 06:33:18 MDT - That synthetic-resume checkpoint-source mismatch is now closed in production. Fresh post-`3554` proof on the same task-85 session shows message `492` still surfacing the missing-content checkpoint from resume state, but turn `66` goes straight to terminal halt message `495` with no intervening bounded correction. The remaining task-85 blocker is now the underlying absence of a durable file body for `templates/template-08-replace.html`, not another same-family anti-loop failure in the recovery controller.
- 2026-03-29 06:46:02 MDT - Recovery checkpoint reconciliation for decomposed child tasks was still too local to the child task itself. Fresh pre-fix evidence: closed Sam.blog task `88` (`Personality & Tone: How SamBot should reflect Sam's voice ...`) had recovery session `2df643c5-662a-435b-830d-d28060edca86` with prompt `58` surfacing checkpoint target `content/posts/mister-rogers-and-the-forgotten-art.md`, even though the child belongs to SamBot spec parent `86` whose contract is `planning/sambot-feature-spec.md`. The assistant explicitly noticed the mismatch at message `60`, but the runtime still treated the foreign checkpoint as resumable state and blocked on it again at `62`. Root cause: `recoveryCheckpointClearlyBelongsToDifferentTask(...)` only compared the checkpoint against the child task’s own weak local contract and did not validate it against the explicit deliverable path/root owned by the decomposition parent. Impact: decomposed planning/spec children can inherit foreign recovery checkpoints from other task families, reopen the wrong file lane, and pay for pointless recovery turns even when the model itself can see the target is unrelated.
- 2026-03-29 06:39 MDT - Explicit deliverable contract parsing was still too weak for the new SamBot replacement children. Fresh pre-fix proof:
  - task `96` session `28629e98-87c2-46ce-8aca-376ee696e483` repeatedly received `recovery_target_focus_required` for `planning/sambot-conversations.md` even though the task title/description explicitly say `Produce planning/sambot-feature-spec.md ...`
  - task `99` review session `70ec722b-f73f-468a-aac8-135074c981df` kept starting from `content/posts` and emitting root-first review guards even though the explicit deliverable is `planning/sambot-feature-spec.md`
  Root causes:
  - `explicitDeliverablePath(...)` only recognized `write/create/produce ... at|to PATH`, so it missed direct-verb forms like `Produce planning/sambot-feature-spec.md ...`
  - `preferredTaskDeliverableRoot(...)` still inferred `content/posts` from reference text even when the same task already had an explicit single-file deliverable
  - `deliverableTargetMatchesTaskContract(...)` did not require exact-path matching when an explicit deliverable existed, so unrelated markdown files still qualified as task-owned recovery targets
  Impact: explicit single-file planning/spec tasks could be redirected into sibling planning artifacts or review reference roots, reopening the wrong lane and burning new validation-loop turns immediately after PM decomposition succeeded.
- 2026-03-29 06:42 MDT - Even after that parser fix landed, task-scoped synthetic retries still lacked deploy invalidation when their prompt metadata came from `syntheticContinuationActionMessageMetadataWithCarryForward(...)`. Fresh evidence from the same SamBot family: active successor sessions `cc97cbdd-4a4b-490b-9dbb-01b63a5f2c17` (task `96`) and `cb850e6f-01b8-45ca-8d3c-5265c1453794` (task `99`) kept replaying pre-restart prompt shapes after `3557` because their carry-forward synthetic user messages had `source` but no `repo_version`. Root cause: the carry-forward helper preserved run attribution but never stamped the current repo version, so worker stale-synthetic cleanup could not recognize those prompts as superseded after deploy. Impact: task review / task recovery retries could keep replaying older guidance across deploys, delaying live confirmation of the newly-shipped fix and burning extra turns on already-known bad prompt state.
- 2026-03-29 07:13:41 MDT - Decomposed SamBot child tasks were still split across two inconsistent deliverable-contract implementations after the earlier engine fix. Fresh task-103 proof:
  - turn-engine review prompts named `planning/sambot-feature-spec.md`
  - but native file-tool recovery guards still surfaced `deliverable_path=Append` from the raw child checkpoint (`0190` messages `5`, `56`, `159`)
  Root causes:
  - the native parser in [`internal/tools/native/mutation_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/mutation_tools.go) still only recognized the older `Deliverable:` form and still accepted bare capitalized action words as path-like targets
  - the native recovery focus path in [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) still resolved child-local explicit/recovery state without inheriting the decomposition parent’s explicit deliverable path
  Impact: even after the engine moved the task to `planning/sambot-feature-spec.md`, the tool layer could still bounce the same session back to fake target `Append`, reintroducing the wrong-lane churn inside live file reads.
- 2026-03-29 07:13:41 MDT - Task-review preferred-target accounting was also too eager about what counts as a successful first read. Fresh task-103 proof on review session `0190f0dc-9c58-4f5c-8d32-05292c38dc75`:
  - the first `file.read` returned only `{"error":"recovery_target_focus_required","path":"planning/sambot-feature-spec.md","deliverable_path":"Append"}`
  - the very next `file.read` on `planning/sambot-feature-spec.md` was blocked as `preferred deliverable target ... was already fully inspected in this turn`
  Root cause: [`recordTaskReviewPreferredDeliverableReadResult(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) marked the preferred target as “already inspected” for any matching `file.read` tool result, even when that payload carried only validation errors and no successful file body. Impact: review lanes could dead-end into repeat-read guards without ever getting one real deliverable read, then churn through git/task rediscovery until the empty-output blocker halted the task.
- 2026-03-29 07:23 MDT - Even after the preferred-target read started hitting the correct file, placeholder evidence could still strand review turns without a decision. Fresh post-fix proof on task `103` review session `ec062d3c-08d5-475d-967b-03726b7a2f65`:
  - message `7` successfully read the correct target `planning/sambot-feature-spec.md`
  - that returned real scaffold/placeholder body content instead of the earlier `deliverable_path=Append` validation error
  - the lane still closed blocked without emitting `flow.review_decision reject`
  Root cause: [`maybeSynthesizeTaskReviewDecisionToolCalls(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only synthesized review decisions from explicit assistant phrasing or the dirty-workspace retry branch; it did not consume already-known preferred-target reject evidence from the current turn. Impact: review lanes could pay for the correct target read, narrate findings, and still end `blocked`, reopening the same review family instead of closing the node with an immediate reject decision.
- 2026-03-29 07:31 MDT - Append-only single-file execution tasks could still spin into runtime-owned completion churn after already finishing the concrete file edit. Fresh live evidence:
  - task `111` session `fb9e5552-e69f-43a3-ad6d-1a1347685f96` did `file.edit -> planning/sambot-feature-spec.md`, reread the full file, then hit `git.commit -> task_git_commit_blocked` and `task.update -> flow_owned_done_blocked`
  - task `109` session `b832a073-a30d-4e16-8b80-81698696c8d2` repeated the same pattern across multiple retries
  Root cause: [`explicitExecutionDeliverableWriteCompleted(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) and [`shouldStopAfterExecutionDeliverableWrite(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only recognized terminal deliverable completion for `file.write`, not explicit-target `file.edit`. Impact: append tasks that correctly modify the single owned file could keep rereading, planning `git.commit`, and trying forbidden `task.update done` calls instead of ending immediately and letting runtime flow advancement take over.
- 2026-03-29 07:38 MDT - Runtime-owned completion-summary placeholder prose was still slipping through both native deliverable guards and recovery draft selection. Fresh pre-fix proof on task `111` session `1a638004-6599-46ad-abe4-96acb1c6f76a`:
  - `file.read planning/sambot-feature-spec.md` returned the raw `1868`-byte body `The deliverable is complete. The runtime will advance the flow. Here's a summary of what was delivered ...`
  - the assistant explicitly identified that file as placeholder/meta-summary content, but the runtime treated it like ordinary deliverable text rather than `placeholder_deliverable`
  - the lane then drifted into `file.read planning -> recovery_target_focus_required` and later rewrote a `988`-byte summary derivative back into the target file
  Root cause: the placeholder detectors covered narrated write placeholders, review memos, content-migration scaffolds, and runtime-owned commit handoff prose, but not the newer `runtime will advance the flow / summary of what was delivered` family. Impact: hot recovery lanes could reread and even rewrite runtime-owned completion summaries as if they were deliverable drafts, reopening the same target-focus churn instead of forcing a concrete overwrite.
- 2026-03-29 07:47 MDT - After the `3566` deploy, the active SamBot lane immediately exposed the next narrower classifier gap. New task `111` execution session `6cd4921c-0690-4054-aab6-e681db237511` no longer reopened the raw runtime-owned completion-summary prose; instead, its first target read returned a different `988`-byte reviewer-summary placeholder beginning `The evidence is clear. The file at planning/sambot-feature-spec.md ...`, and the lane then tried blocked `task.list` / `memory.query` rediscovery. Impact: the just-fixed completion-summary family is no longer the active live blocker, but there is still one adjacent review-summary placeholder family to classify so these SamBot recovery turns stop before broad-context churn.
- 2026-03-29 07:57 MDT - The next recursive reviewer-summary family is partially fixed but not fully live-proven as a native placeholder classification yet. Fresh `3568` proof:
  - task `111` execution session `72a06536-9316-4cf2-9b7d-fd412307af85` still read a `493`-byte body beginning `The file content is clearly not a valid deliverable ... self-referential summary ...`
  - the succeeding review session `c95f1f65-1a31-44e1-b833-124a2c6911af` then rejected and blocked correctly, so the family is now bounded
  - but the read itself still returned content instead of `placeholder_deliverable`, so the native deliverable-classifier effect is still only test-proven for this specific paraphrase family
- 2026-03-29 08:20 MDT - Replacement review task `129` exposed a separate path-token bug in native recovery targeting. Fresh pre-fix evidence on session `cb25ec19-d20e-4250-8f47-1e49bd64cd08`:
  - the review prompt correctly named preferred target `planning/sambot-feature-spec.md`
  - direct `file.read` still returned `recovery_target_focus_required`
  - the tool payload showed `deliverable_path=\"`planning/sambot-feature-spec.md`\"`, with wrapper backticks preserved in the stored target
  Root cause: shared path normalization preserved markdown/quote wrappers, so runtime comparisons treated `` `planning/sambot-feature-spec.md` `` and `planning/sambot-feature-spec.md` as different paths. Impact: review lanes could self-block on their own preferred deliverable target even when the contract was otherwise correct.
- 2026-03-29 08:33 MDT - Task `109` exposed stale content-migration checkpoint bleed-through on a single-file planning/spec lane. Fresh live evidence on session `4ba95ee9-123f-4b9d-9fcf-37a73b84fe00`:
  - the lane repeatedly reread large sections of `planning/sambot-feature-spec.md`, hit prompt-input overflow/compression, then reread again
  - task `109` metadata still carried `content_migration_checkpoint` outputs under `content/posts/...`, even though its current contract is `planning/sambot-feature-spec.md`
  Root cause: checkpoint summary / preferred-output helpers only checked for checkpoint presence, not whether the task was still a markdown-batch migration task. Impact: stale checkpoint metadata could inject the wrong continuation summary and wrong preferred-output hints into explicit single-file review/recovery lanes, increasing prompt size and steering them back toward irrelevant batch-output context.
- 2026-03-29 08:39 MDT - After the checkpoint-baggage fix, task `109` still showed a deterministic large-read review loop on the explicit single-file target itself. Fresh post-fix evidence on session `4ba95ee9-123f-4b9d-9fcf-37a73b84fe00`:
  - new retries at `08:31:31` and `08:32:19` dropped the stale `[Content migration checkpoint]` banner
  - the lane still did `head 8192 -> tail 8192 -> middle 8192` on `planning/sambot-feature-spec.md`, then hit the review prompt-size guard again
  Root cause: once head and tail had already been sampled, the preferred-target review rewrite path still allowed a third full-size same-file reread instead of narrowing to the unresolved middle gap. Impact: append-style single-file review lanes could keep burning ~24 KB of repeated target reads per turn even after the stale checkpoint context was removed.
- 2026-03-29 08:42 MDT - The stale checkpoint banner came back on task `109` even after the first gate fix because explicit deliverable detection was still missing the exact live SamBot wording. Fresh proof:
  - `3573` retry `08:39:28` proved the middle-gap reread bound was active
  - but continuation `08:39:47` still injected `[Content migration checkpoint] ...` on the same task
  Root cause: both engine and native explicit-deliverable parsing only inspected `Description` / `decomposition.source_description`. Task `109`'s description starts with `Read planning/sambot-feature-spec.md ...`, while the real explicit deliverable appears in the title. Impact: title-led single-file append tasks could still be misclassified as generic markdown-batch work, letting stale content-migration checkpoint state re-enter continuation and recovery prompts.
- 2026-03-29 08:50 MDT - Review-lane mutation blocking was still tied too tightly to `project_task.work_status`. Fresh task-109 evidence on session `4ba95ee9-123f-4b9d-9fcf-37a73b84fe00`:
  - `08:47:08` runtime halted the review lane after repeated empty outputs and marked the task blocked
  - `08:47:16` and `08:47:22` the same review-scoped session still wrote `planning/sambot-feature-spec.md`
  Root cause: native [`rejectReviewTaskMutation(...)`](/Users/sam/dev/otter-camp/internal/tools/native/mutation_tools.go) / [`rejectReviewTaskCLIExecute(...)`](/Users/sam/dev/otter-camp/internal/tools/native/mutation_tools.go) only checked `work_status == review`. When runtime moved the task to `blocked`, the session still had a live `Review only. ... flow.review_decision ...` prompt but no longer tripped the native review guard. Impact: failed review lanes could mutate the deliverable after the runtime had already decided the lane should only inspect and issue `flow.review_decision`, reopening corruption/churn instead of settling.
- 2026-03-29 08:57 MDT - After the native guard fix, the next task-109 failure mode moved up into the turn engine. Fresh `3578` evidence on successor review session `5f28b1d3-7738-43e4-9eb6-402fa3b835f4`:
  - the lane stayed read-only and did one direct `file.read`
  - the assistant final explicitly said the file was a `clear mismatched deliverable` and described the placeholder/spec mismatch
  - the session still closed without `flow.review_decision`, and task `109` fell back to `blocked`
  Root cause: [`maybeSynthesizeTaskReviewDecisionToolCalls(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) still short-circuited on `task.work_status != review`, even when the active prompt was clearly a review-resume lane. At the same time, [`inferredReviewRejectFromText(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) did not treat the now-common `mismatched deliverable` phrasing as strong reject evidence. Impact: blocked review resumes could inspect the right file, explain exactly why it should be rejected, and still miss the required `flow.review_decision reject`, reopening the lane as another blocked review family.
- 2026-03-29 09:12 MDT - Append-style explicit deliverable parsing had a candidate-order bug that misclassified slash-heavy section titles as repo paths. Fresh proof:
  - [`TestShouldStopAfterExecutionDeliverableWriteStopsForExplicitFileEdit`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) failed for the realistic append wording `Append "UI/UX Design" section to planning/sambot-feature-spec.md — covering ...`
  - the simpler helper case still passed, which narrowed the regression to the title/description candidate ordering rather than the stop path itself
  Root cause: both engine and native parsers tried the loose leading-verb fallback on each candidate immediately. For a title like `Append UI/UX Design section`, the fallback saw `UI/UX`, treated the slash as a file-path hint, and returned before the parser ever reached the later description candidate containing the real target `planning/sambot-feature-spec.md`. Impact: append/review lanes could miss the real single-file target on exactly the SamBot spec tasks we are hardening, which weakens completion-stop behavior, preferred-target review prompts, and native mutation boundaries.
- 2026-03-29 09:21 MDT - Task `133` exposed a retry-settlement bug after successful deliverable mutation. Fresh live evidence on session `d513ff3e-bc46-4a35-8a20-0e2b1b0a1133`:
  - `09:06:00` the lane successfully edited `planning/sambot-feature-spec.md`
  - `09:06:09` `git.commit` was blocked correctly as runtime-owned, and the engine emitted the early-stop message
  - every later retry turn then emitted only assistant prose like `The deliverable is complete. The runtime will advance the flow.` with no new tools, reaching at least retry `28`
  Root cause: task-resume completion handling only looked for durable writes in the current completed turn. Once the session moved into assistant-only retry turns, the runtime could no longer “see” the already-written deliverable and therefore requeued placeholder completion summaries instead of committing/advancing the flow from the earlier write. Impact: completed task lanes can burn many empty retry turns after already writing the deliverable, delaying review and wasting tokens even though the runtime has enough evidence to settle immediately.
- 2026-03-29 09:25 MDT - Task `127` exposed stale execution-owner drift in worker recovery. Fresh live evidence on review session `00dec24e-1f16-4ea6-a5c9-f3a640e566a3`:
  - the session remained `active` with leaked `current_turn_id=1d08f452-69a5-491a-aa87-fa1207a9afd4`
  - that turn had four completed model invocations, no run ownership, no pending/claimed `agent_turn` job, and a terminal halt message already written
  - the active `flow_node_execution` still pointed at older completed `live_turn_id=f60d5eb6-c5d7-4c60-9044-1d84eed77ffc`
  Root cause: project-task stale-turn recovery preferred `flow_node_execution.metadata.live_turn_id` even after that pointer had fallen behind the session’s newer leaked `current_turn_id`. Impact: leaked review/execution turns can remain stuck forever with no job ownership because the worker keeps evaluating the wrong completed turn instead of the actual in-progress leak that needs recovery.
- 2026-03-29 09:42 MDT - The remaining live PM seam was a native mutation-boundary hole on planning deliverables. Fresh evidence on project continuation turn `242f846d-a862-4cb1-83d6-c6773707712e` in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`:
  - the PM lane read `planning/sambot-feature-spec.md`
  - then it directly edited that file from the project session
  - then it attempted `cli.execute` from the same PM lane
  Root cause: native project-session deliverable blocking exempted all `planning/` paths, and `cli.execute` had no equivalent project-session guard at all. Impact: the PM lane could still do task-owned spec mutations and shell work against live deliverables even after executable task lanes existed, bypassing the intended bounded project-management contract.
- 2026-03-29 09:48 MDT - Once PM-lane direct writes were blocked, the next live continuation still spun on named-task rediscovery after already reading the explicit deliverable. Fresh `3583` evidence on turn `f5a846a4-f224-46a7-b840-359ff02e1bc0`:
  - first tool action was a direct `file.read` of `planning/sambot-feature-spec.md`
  - the assistant then concluded the needed sections already existed
  - despite that, it fell back into blocked broad `task.list` and blocked named `task.get` rereads on the same draft parents
  Root cause: project-continuation rediscovery guard messages still said only “act on the named task directly,” which was too weak and generic after the PM lane already had concrete deliverable evidence. Impact: the PM lane could still waste turns bouncing off blocked named-task rereads instead of converting that evidence into the next direct `task.update` or replacement-child action.
- 2026-03-29 09:52 MDT - After the stronger named-task rediscovery wording landed, the PM lane still drifted into sibling planning artifacts. Fresh `3584` evidence:
  - turn `bd0f88eb-a9eb-4f89-8a9b-69d33962b752` showed the new stronger `task.list` guard text live
  - the next continuation then reread `planning/sambot-feature-spec.md`, decided the needed sections were already present, and pivoted to a missing companion planning artifact (`file.read -> not_found` on the original PRD path)
  - that branch then hit prompt compression again
  Root cause: the project-continuation file-read guard had no notion of “companion planning artifact drift” once the prompt already named the primary planning deliverable path. Impact: the PM lane could still burn turns on off-target `planning/prd-spec/...` / companion artifact reads instead of turning the already-read deliverable evidence into the next direct task action.
- 2026-03-29 09:59 MDT - The next PM continuation stall was in the engine’s own fallback logic after the model turn had already ended. Fresh evidence on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`:
  - continuation `7365` on `repo_version=3585` read `planning/sambot-feature-spec.md`
  - the assistant then jumped to broad `task.list`, which the rediscovery guard blocked
  - after the turn ended, the engine’s own `nextRunnableDraftProjectTask(...)` fallback selected draft task `90`, attempted to queue it, hit the bounded size policy, and emitted the system warning `[Project continuation found remaining draft task 90 ... violates the bounded size policy ...]`
  - no fresh focused retry message followed, even though the engine already has a bounded-size retry prompt path for model-produced bounded-size stops
  Root cause: [`handleCompletedProjectExecutionContinuationTurn(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only called [`retryProjectExecutionContinuationForBoundedSizeStop(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) when the just-finished turn itself already contained a bounded-size stop. When the bounded-size error happened later in the engine-owned post-turn auto-queue fallback, it appended the warning and returned without reissuing the narrower continuation. Impact: PM continuations could strand themselves on the broad-parent bounded-size warning instead of being pushed directly into “split this parent into smaller child tasks now.”
- 2026-03-29 10:06 MDT - Once the PM bounded-size retry was working, the next live blocker was stale satisfied replacement drafts that never settled. Fresh evidence:
  - the `3586` retry message `7384` did fire, and follow-on turn `697` moved beyond the old warning-only dead end
  - after the PM lane updated parent-orchestration satisfaction on the spec-part draft parents, the continuation summary compressed to `2 remaining draft project tasks`
  - direct DB inspection showed those remaining drafts were tasks `82` and `90`
  - both were still `work_status=draft` even though their metadata already contained `parent_orchestration.outcome_assessment.satisfied=true` with concrete `planning/sambot-feature-spec.md` completion evidence
  Root cause: [`tasksvc.SatisfiedDraftAutoCompletable(...)`](/Users/sam/dev/otter-camp/internal/task/service.go) required a planning-process contract plus artifact evidence and also rejected decomposable broad tasks outright, so simple single-file replacement drafts with explicit deliverable evidence could never auto-complete from their satisfied outcome metadata. Impact: clearly-finished replacement drafts stayed visible to PM continuations as actionable draft work, reopening the same “spec already complete” family instead of letting the runtime settle them out of the task tree.
- 2026-03-29 10:17 MDT - After leaf satisfied-draft auto-complete was widened, the next PM seam moved one level up to satisfied draft parents. Fresh live evidence on PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`:
  - current turn `718` reread `planning/sambot-feature-spec.md` and correctly concluded task `113`’s deliverable is already complete
  - the assistant then explicitly tried to mark parent tasks `113`, `106`, and `94` done
  - but the current continuation family still carried the older guard text `task 113 ... with 2 terminally blocked child lane(s) ... create the smallest fresh replacement child task beneath task 113 instead`
  Root cause: project-continuation focus and closeout logic only looked at child-lane counts. A draft parent with blocked children but `parent_orchestration.outcome_assessment.satisfied=true` still looked like a “replacement-child” parent unless it also had a dedicated done closeout child. Impact: the PM lane could keep reopening fresh replacement-child work for already-satisfied parent tasks instead of closing those parents directly and moving on to the remaining real draft work.
- 2026-03-29 10:26 MDT - After the closeout-ready classifier landed, the next seam turned out to be the retry handoff after the parent completion gate. Fresh live evidence on PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`:
  - turn `720` correctly treated task `113` as closeout-ready and attempted to close it
  - `task.update` then returned `parent task requires child verification and passed integration before completion`, naming required completed children `OC-115`, `OC-124`, and `OC-130`
  - turns `721-723` kept recognizing the closeout-ready state, but the generic PM continuation still fell back to rereading `planning/sambot-feature-spec.md` and relisting task `113`’s children instead of issuing the single `task.update` with `child_output_verifications` / `integration_check`
  Root cause: the engine had no dedicated retry path for the parent-orchestration completion stop. Once the runtime emitted the correct closeout guard, the next continuation still reused the generic PM prompt rather than carrying forward the exact child-task labels and the one narrow mutation shape needed to finish the parent. Impact: PM closeout-ready parents could still burn repeated read-plus-child-list loops after the runtime already told the lane exactly which child verifications were missing.
- 2026-03-29 10:32 MDT - The first production pass of that retry fix still had one live-only hole.
  - on `repo_version=3589`, the PM session again hit the correct task `113` parent-completion gate
  - this time the tool error came back in the shorter form `parent task requires child verification and passed integration before completion: all child tasks must complete before the parent can finish integration`
  - because the session had already reached retry depth `2`, the dedicated parent-completion retry did not re-fire, and the continuation fell back to the generic prompt / file-read / child-list loop again
  Root cause: the dedicated parent-completion retry inherited the generic retry budget ceiling, and it relied only on child labels embedded in the tool error text. Impact: long-lived PM sessions could still miss the sharper retry prompt exactly when they needed it most, and shorter runtime errors could lose the specific `OC-*` child labels that the next task.update needed.
- 2026-03-29 10:39 MDT - Once the sharper retry prompt was live, the final remaining miss moved into the actual PM closeout mutation.
  - on `repo_version=3590`, the newest continuation messages did contain `child_output_verifications`, `work_status=done`, and explicit `OC-115`, `OC-124`, `OC-130`
  - the PM lane then did the correct narrow discovery step `task.list(parent_task_id=113)` and explicitly named the matching child UUIDs
  - but the following `task.update` still reached the runtime without the required parent-orchestration payload, so the same `parent task requires child verification and passed integration before completion` error repeated
  Root cause: the turn engine had no preflight guard for “closeout-ready parent marked done without parent_orchestration evidence.” Even after the retry prompt became specific enough, the malformed `task.update` was still allowed to hit the native task transition path and bounce off the same runtime-owned completion gate. Impact: the PM lane could still waste one more doomed `task.update` per retry even when it already had the right parent, the right child IDs, and the right closeout intent.
- 2026-03-29 10:45 MDT - After the PM-side mutation guard landed, the runtime completion gate itself still blocked satisfied parents with stale blocked children. Fresh live evidence on `repo_version=3591`, session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`:
  - the PM lane focused task `113`, listed direct children, identified completed `OC-115`, `OC-124`, and `OC-130`, and attempted parent closeout
  - the runtime still returned `parent task requires child verification and passed integration before completion: all child tasks must complete before the parent can finish integration`
  - direct project state showed the only incomplete descendants were blocked stale children `114` and `129`, while the parent already had `outcome_assessment.satisfied=true`, passed integration, and completed replacement-child evidence
  Root cause: [`internal/taskorchestration/metadata.go`](/Users/sam/dev/otter-camp/internal/taskorchestration/metadata.go) only treated blocked children as terminal for malformed/procedural descendants or explicit completed closeout-child shapes. It did not consider blocked stale children terminal once the parent outcome was already satisfied, so [`ValidateCompletion(...)`](/Users/sam/dev/otter-camp/internal/taskorchestration/metadata.go) continued to append `all child tasks must complete before the parent can finish integration`. Impact: satisfied closeout-ready parents could still never transition to `done`, leaving the PM lane trapped between correct closeout attempts and impossible runtime completion requirements.
- 2026-03-29 10:54 MDT - After the parent-closeout gate was fixed, two satisfied single-file spec replacements still remained as visible draft work. Fresh live evidence on `repo_version=3592`:
  - remaining drafts included task `82` and task `90`
  - both already had `parent_orchestration.outcome_assessment.satisfied=true`, no child task ids, flow templates present, and a concrete single-file deliverable path `planning/sambot-feature-spec.md`
  - despite that, worker startup cleanup had not settled them, so they stayed in the PM continuation draft count
  Root cause: [`internal/task/service.go`](/Users/sam/dev/otter-camp/internal/task/service.go) rejected broad planning tasks inside [`SatisfiedDraftAutoCompletable(...)`](/Users/sam/dev/otter-camp/internal/task/service.go) whenever queue decomposition still classified them as broad. There was no carveout for the narrower case where the whole satisfied outcome is already embodied in one concrete workspace file. Impact: broad-but-single-file planning replacements could linger forever as satisfied drafts, forcing PM continuations to keep seeing already-finished spec work as unsettled backlog.
- 2026-03-29 11:02 MDT - The first pass at that classifier still missed the actual live failure branch for `82` / `90`.
  - direct local probe against the live task records showed `SatisfiedDraftAutoCompletable(...)` still returned `false` for both tasks and `TransitionStatus(... done, AllowSatisfiedDraftAutoComplete=true)` still failed `invalid status transition from "draft" to "done"`
  - the same probe showed `taskplan.Parse(...)` was false for both records and `PrepareQueueDecomposition(...)` returned `task exceeds bounded size policy ...`
  Root cause: the earlier patch widened only the “broad planning task with parsed planning metadata” path. It still returned `false` immediately on bounded-size decomposition errors, even when the satisfied outcome clearly pointed at one concrete file and there were no child tasks. Impact: live satisfied spec replacements remained stuck in draft after restart because startup cleanup never recognized them as auto-completable in the first place.
- 2026-03-29 11:12 MDT - Repeated PM continuation suppression still had no proof-of-progress exemption.
  - the repeated-suppression path in [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) only looked at `validation_loop_blocked` plus a small set of PM system-message prefixes
  - it never checked whether the latest blocked continuation turn had already produced a successful durable mutation like `task.update`, `task.create`, or `flow.review_decision`
  Root cause: suppression was keyed entirely off stop-family text, not the actual tool-result payloads from the latest terminal continuation turn. Impact: the worker could suppress a retry that should still happen because the prior blocked turn had already made concrete forward progress before stopping.
- 2026-03-29 11:20 MDT - PM continuations still lacked an exact stop condition for terminally blocked leaf deliverables already named in the prompt.
  - fresh live proof on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: prompt `8369` already named only `resume_policy=terminal_keep_blocked` leaf tasks that own `planning/sambot-feature-spec.md`, yet the same PM turn still paid `file.read planning/sambot-feature-spec.md` at `8372` after the broader `task.list` rediscovery guard had already fired at `8371`
  Root cause: [`shouldBlockProjectContinuationSnapshotRediscoveryTool(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only treated exact deliverable rereads as blocked when the prompt described `work_status=in_progress|review` ownership. It did not treat deliverables owned by terminally blocked leaf tasks as equally off-limits for the PM lane. Impact: supervisory PM turns could still burn one more useless deliverable reread on a blocked leaf lane even after the prompt had already named that lane, its deliverable path, and its terminal blocker policy.
- 2026-03-29 11:26 MDT - The first blocked-leaf reread patch still missed the exact live tool-call shape.
  - post-deploy live proof on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, turn `a67f8cf4-1be3-4cc3-a1bc-fb4eb7cf48c9`: prompt `8544` still contained `deliverable_path=planning/sambot-feature-spec.md` plus `resume_policy=terminal_keep_blocked`, but assistant message `8545` called tool alias `file_read`, not `file.read`, and the blocked deliverable reread still executed at `8546`
  Root cause: the new stop family matched the prompt correctly, but [`shouldBlockProjectContinuationSnapshotRediscoveryTool(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only switched on dotted names like `file.read` / `task.list` / `task.get` for that branch. Impact: live PM turns using underscored tool aliases could bypass the new supervisory stop even though the prompt and tool arguments already satisfied the blocked-leaf guard conditions.
- 2026-03-29 11:44 MDT - Pure blocked PM rediscovery batches still kept every blocked named-task reread in the transcript even after the stop family was already determined.
  - fresh live evidence on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: turns `8653-8661` showed the PM lane opening with named-`task.get` rereads on the already-listed leaf tasks and then stopping on the existing rediscovery guard
  - the lane was bounded, but the turn still accumulated blocked named-task reread evidence before the stop message instead of preserving only the minimum proof that the batch was pure rediscovery
  Root cause: [`dispatchTools(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only decided to stop after the entire blocked batch was collected, and it appended the whole blocked batch to the transcript before the early-stop message. Impact: longer pure PM rediscovery batches can still spam redundant blocked `task.get` tool_result messages into the session history even though no further inference is needed once the stop family is known.
- 2026-03-29 11:49 MDT - Decomposed SamBot spec child tasks could inherit a bogus bare-token deliverable path instead of the parent’s real `planning/...` file.
  - fresh live evidence:
    - task `145` metadata already held `recovery_file_write_checkpoint.target_path=SamBot`
    - task `142` metadata already held the same polluted recovery target
  - both live sessions were writing or recovering against `SamBot` even though parent task `139` explicitly says `... feature to planning/sambot-feature-spec.md`
  Root cause: the explicit-deliverable regex for `write/create/produce ... to PATH` only allowed 80 characters between the verb and the path. Parent task `139`’s description is longer than that, so the real `planning/sambot-feature-spec.md` path never matched. The parser then fell through to the weaker leading-verb fallback and treated the bare capitalized token `SamBot` from the parent title as an explicit deliverable path. Impact: decomposed child lanes can target and repeatedly rewrite a bogus bare file like `SamBot` instead of inheriting the actual single-file deliverable from their parent task contract.
- 2026-03-29 12:00 MDT - Once child tasks inherited the correct parent spec path, recovery still treated that shared single-file deliverable like an ordinary child-owned `file.write` target.
  - fresh live evidence on task `145` / session `c6bfa4ef-6f7c-4186-8014-3a620f065f96`: post-fix tool results `91`, `95`, and `102` each wrote `planning/sambot-feature-spec.md`, while the assistant kept reporting that the lane was being “redirected” into the parent spec instead of its intended bounded child output
  Root cause: recovery synthesis/population logic in [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only reasoned about “is there a valid target path and persisted draft?” It had no separate stop condition for decomposed child tasks that merely inherit a parent single-file explicit deliverable. Impact: section-scoped child recovery can overwrite the full shared parent document with replayed `file.write` drafts, creating unsafe file handling and expensive retry churn even after target-path inheritance itself is fixed.
- 2026-03-29 12:05 MDT - The first inherited shared-deliverable guard still left an auto-retry loop behind the safety stop.
  - fresh live evidence on task `142` / session `5c403c27-3662-4843-a19f-d957258f9ec5`: after deploy, messages `45/46`, `50/51`, and `55/56` all show the same blocked shared-deliverable stop repeating on new recovery resumes
  Root cause: [`haltRecoveryInheritedSharedDeliverableWrite(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) originally set only `stopReasonValidationBlocked` plus checkpoint metadata. It did not set `recoveryBlockReason` or cancel the recovery dispatch metadata on the triggering message, so the worker still treated the lane as eligible for the next automatic recovery resume. Impact: the shared parent file stopped being overwritten, but the same child lane could still spend repeated retries rediscovering and re-hitting the identical guard every few seconds.
- 2026-03-29 12:09 MDT - Even after the helper-level hard block was added, the live loop persisted because the stop was happening through a different engine branch.
  - fresh task-142 evidence on session `5c403c27-3662-4843-a19f-d957258f9ec5`: repeated turns through `12:07 MDT` kept ending on the generic blocked-tool system message `Task shared-deliverable guard blocked a decomposed child lane...`, while recovery user messages `192`, `197`, and `203` continued to appear with blank `cancelled_at` metadata
  Root cause: the actual live stop path was `dispatchTools(...)` handling a pure blocked `file.write` batch, not `haltRecoveryInheritedSharedDeliverableWrite(...)`. That blocked-batch branch stopped the turn, but it did not yet set `recoveryBlockReason` or cancel the triggering recovery dispatch metadata. Impact: the system still prevented the overwrite, but the worker kept generating fresh `task_recovery_resume` prompts for the same blocked inherited shared-file mutation.
- 2026-03-29 12:34 MDT - PM continuation wake logic still treated replacement-parent states as settled after the last live child blocked.
  - fresh live proof on Sam.blog project session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`: once task `142` blocked and its task session closed, the project session had no active task lanes, three draft tasks remained, and the idle-project recovery query matched the session, yet no new continuation message was pending
  Root cause: two related assumptions were still too narrow.
  - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only routed terminal task turns/status changes into the PM continuation path for `done` and `cancelled`, not `blocked`
  - both engine and worker settled the project whenever `remainingDraftTasks == 0`, even if the continuation snapshot still showed `ReplacementDraftLine` / `FocusTaskLine` and therefore real PM work remained
  Impact: replacement-parent drafts like SamBot task `139` could strand the PM lane idle immediately after the last live child blocked, even though the next correct move was to create or queue a fresh replacement child beneath that parent.
- 2026-03-29 12:39 MDT - Once the PM wake bug was fixed, the next live SamBot defect turned out to be malformed reference-only decomposition children.
  - fresh live evidence:
    - replacement task `146` was created correctly for the architecture slice
    - decomposition then produced task `147` (`Overview — One-paragraph summary ...`) and task `148` (`Reference planning/sambot-feature-spec.md for feature requirements ...`)
    - task `148` entered an execution lane and then had to be narrowed by downstream task-lane guards onto `planning/sambot-architecture.md`
  Root cause: [`internal/taskdecomp/decomposition.go`](/Users/sam/dev/otter-camp/internal/taskdecomp/decomposition.go) already rejected explicit procedural junk like `Use cli_execute ...`, but it still treated `Reference planning/...` instruction lines as executable deliverables during decomposition. Impact: PM decomposition can create malformed child tasks that pollute continuation focus, reopen task-lane churn, and hide the real parent work unit that should remain actionable.
- 2026-03-29 13:24 MDT - SamBot replacement task `154` exposed one more malformed decomposition family after the explicit-deliverable-path fix.
  - fresh live evidence:
    - task `156` / session `2c91777a-0ee4-44fd-b90c-70703796b95e`
    - task text: `Use Sam's voice and opinions as established in the SamBot feature spec at planning/sambot-feature-spec.md and the scraped blog content in content/posts/`
    - the lane immediately reread source artifacts, then attempted whole-file ownership of inherited shared deliverable `planning/sambot-example-conversations.md` until the later shared-deliverable guard stopped it
  - bug:
    - [`internal/taskdecomp/decomposition.go`](/Users/sam/dev/otter-camp/internal/taskdecomp/decomposition.go) did not classify support-only `Use ... as established in planning/... and content/...` child tasks as malformed procedural artifacts
  - impact:
    - PM continuations can still create executable child lanes with no concrete deliverable of their own
    - those lanes burn a full task turn before the downstream shared-file safety guards intervene
  - target fix:
    - treat those source-artifact support tasks as malformed procedural children up front so kickoff preflight blocks them before model call
- 2026-03-29 13:56 MDT - The SamBot conversations replacement branch still had one more malformed child family after the support-only fix.
  - fresh live evidence:
    - task `155`
    - title/description: `Produce the file planning/sambot-example-conversations.md containing 5-8 realistic example conversations between a site visitor and SamBot`
    - parent task `154` already owns that exact same single-file deliverable path
    - the child lane kept reopening the inherited shared-deliverable recovery path instead of representing a narrower section-scoped split
  - bug:
    - the malformed-child filters did not treat duplicate full-file children as malformed when they re-claimed the parent's exact single-file artifact
  - impact:
    - PM continuations can remain blocked behind a fake child lane that is not actually narrower than the parent
    - that child then churns into the later shared-deliverable write guard instead of being rejected immediately
  - target fix:
    - treat same-path `produce/write the file <parent path>` children as malformed duplicate shared-file artifacts so kickoff preflight and PM/worker snapshots both ignore them
- 2026-03-29 14:11 MDT - Shared-deliverable recovery stops still allowed one child lane to relaunch after a terminal missing-file halt.
  - fresh live evidence:
    - task `165` / session `bf26e68d-a8b0-4330-960a-d8d5a9e0e2d8`
    - earlier completed turns `096eb2b5-332c-455b-9eb2-b79bfa180962` and `4eddb044-98b3-41a1-8b26-b0c38abbce77` already stopped with `[Recovery shared-deliverable guard: inherited parent file \`planning/sambot-example-conversations.md\` is still missing ...]`
    - despite that, the task remained `in_progress` and the worker reopened a new recovery turn instead of leaving the child lane terminally blocked
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) custom branch `missingInheritedSharedDeliverableStop` marked the task `blocked` but left `agent_turn_validation_guard.blocked=false`
    - [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) active-execution recovery only suppresses blocked task sessions when that validation guard bit is terminal
  - impact:
    - inherited shared-file child tasks can keep relaunching after a terminal stop instead of draining and freeing the PM lane
    - PM continuation wakeup then appears idle again even though the real problem is a stale child execution being treated as resumable
  - resolved live after deploy:
    - task `165` no longer relaunches
    - the PM lane was able to cancel stale replacement family tasks `154` and `164-167`, proving the blocked child session was the real suppressor
- 2026-03-29 14:19 MDT - PM continuations still allowed planning-root rereads even when the exact planning targets were already named in the continuation prompt.
  - fresh live evidence:
    - PM turn `c788dd5c-2726-4896-a6df-784a8589c422` already had `planning/sambot-architecture.md` / `planning/sambot-feature-spec.md` in prompt context
    - after blocked named-`task.get` rereads, the lane still spent a tool on `file.list(path=planning)` before the turn budget ran out
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) blocked companion planning `file.read`s from PM continuations but still allowed planning-root `file.list` browsing
  - impact:
    - PM turns can remain mixed rediscovery batches instead of collapsing into the earlier pure blocked-rediscovery stop
    - that wastes the remaining tool budget on planning-root listing after the real next step is already named
- 2026-03-29 14:29 MDT - A stale PM provider invocation can still hold queue capacity indefinitely when the queue is already full.
  - fresh live evidence:
    - PM turn `fa05f520-df52-4b32-bffa-c954b7b9bb60` stayed `in_progress`
    - assistant message `b20bd1b1-c471-4502-ba21-d077db50fb9b` stayed `streaming`
    - model invocation `48ae5ea6-689a-462f-baa4-d42aa0af530e` stayed `in_flight` from `14:12 MDT`
    - claimed job `a12e81bf-461f-4724-a1f9-bdd9ee2f31c2` also stayed claimed with no heartbeat updates
    - worker log spam showed `job queue: no execution slots available`
  - bug:
    - the worker relied on background stale-maintenance to clean dead `in_flight` invocations, but `processAvailableJobs(...)` returned immediately on full capacity
    - that meant a dead slot could keep blocking fresh work even though the same process already had the cleanup primitives needed to release it
  - impact:
    - one dead PM continuation can occupy execution capacity indefinitely
    - queue saturation then masks the stale turn as simple “no slots” pressure instead of letting recovery happen promptly
- 2026-03-29 14:42 MDT - Worker terminal recovery repair still retried shared-deliverable dead ends because it only classified successful file mutations as decisive progress.
  - fresh live evidence before the fix:
    - blocked task `170` session `ce909f54-8146-4cf7-af40-c24d3b1ac107` had latest completed turn `6d32f585-ba40-4c0e-b436-b13938384c1d` ending `validation_loop_blocked`
    - that turn's terminal system message was `[Recovery shared-deliverable guard: inherited parent file \`planning/sambot-mvp-spec.md\` is still missing ...]`
    - yet the same session still carried fresh pending synthetic recovery prompt `14e50ef1-b36e-4e2f-b8a5-f164350bc660`
  - bug:
    - `RequeueTerminalRecoveryResumeSessionsWithoutLiveExecution(...)` only skipped consumed recovery resumes after successful `file.write` / `file.edit`
    - it did not treat terminal shared-deliverable guard families as decisive stop conditions for later pending `task_recovery_resume` prompts on the same execution
  - impact:
    - blocked child lanes could keep rearming identical recovery prompts even after the runtime had already proved the lane could not continue until a parent/shared-file prerequisite changed
- 2026-03-29 15:09 MDT - Task lanes can still write the task brief itself into the deliverable path and then bounce through review until max visits are exhausted.
  - fresh live evidence:
    - task `171` wrote `planning/sambot-mvp-spec.md`
    - the file body begins with `# Write SamBot MVP implementation plan to planning/sambot-mvp-spec.md (replaces blocked OC-170)` and then repeats the instruction scaffold sections `## Objective`, `## Deliverable`, `## What to produce`, `## Context`, `## DO NOT`, `## Validation Criteria`, and `## Evidence Expectations`
    - review sessions rejected the file as non-deliverable, but the work lane kept reopening and rewriting from the same bad scaffold until the flow finally blocked on `flow rejection max visits exceeded`
  - bug:
    - existing placeholder guards catch narrated intent, runtime handoffs, reviewer summaries, and content-migration scaffolds
    - they did not catch the more literal failure where the agent copies the task brief/instruction scaffold directly into the target deliverable file
  - impact:
    - a task can consume multiple work/review cycles while producing zero durable progress
    - PM sees the task family as “active but churning” instead of getting an early bounded stop on the first bad write/read/recovery reuse
- 2026-03-29 15:26 MDT - PM continuation snapshots can hide the real replacement parent when all of its child lanes are blocked on inherited shared-parent deliverables.
  - fresh live evidence:
    - PM continuation prompt `9329` on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` listed only blocked tasks `171/161/160/140`
    - draft parent `157` existed in the project but was missing from both `ReplacementDraftLine` and `FocusTaskLine`
    - the model then fell into repeated blocked `task.list` rediscovery because the snapshot gave it no actionable replacement parent to act on
  - bug:
    - `projectContinuationChildTaskActivity(...)` only counted blocked children as `replaceable_blocked_child_tasks` when their resume policy was already `terminal_keep_blocked` or `needs_replacement_work`
    - inherited shared-parent deliverable stops (`recovery halted because the decomposed child lane inherits shared parent deliverable ...`) did not map to either bucket, so the parent draft was neither shown as active child work nor promoted to replacement work
  - impact:
    - PM can loop on blocked child tasks even though the correct next action is to create or queue fresh replacement work under the hidden draft parent
    - a real draft successor can vanish from the continuation snapshot entirely, making the supervisory layer look idle or confused when the project still has bounded next work
- 2026-03-29 15:21 MDT - Explicit child-task deliverables can still inherit the wrong recovery target when the contract is expressed as a path-first title or a parenthesized `(path or similar)` hint.
  - fresh live evidence:
    - task `175` session `2c86ec19-566c-4d6e-a5b7-e591506d1b9f` is supposed to build `sambot/widget.html`, but its recovery target was still `planning/sambot-feature-spec.md`
    - task `176` session `12727f59-f7ea-4e3f-a8d0-ab3ff093110c` is supposed to build `sambot/api.js`, but its recovery target was also still `planning/sambot-feature-spec.md`
    - task `173` session `a6fbb2a0-cdd6-4dfd-87a5-70a50f4c2020` tried to treat the unrelated root file `SamBot` as the deliverable because its own `(sambot/widget.html or similar)` hint was never recognized
  - bug:
    - explicit deliverable parsing handled `deliverable:`, `output at`, `write to`, and similar phrasing, but it did not recognize titles like `sambot/widget.html (or sambot/index.html) — ...`
    - it also did not recognize description hints like `(sambot/widget.html or similar)`
    - worker-side deliverable hints ignored task titles, so PM/worker snapshots could miss these explicit child outputs even when the title carried the only precise path
  - impact:
    - child execution lanes spend retries fighting `recovery_target_focus_required` against stale parent-spec targets instead of writing their actual `sambot/...` deliverables
    - PM snapshots and worker-authored continuation prompts can under-specify the real output path for the active child work
- 2026-03-29 15:29 MDT - Broad non-markdown backend tasks can still accept the wrong historical/review target when the task contract has no explicit output path.
  - fresh live evidence:
    - after the explicit-path parser fix, task `176` correctly anchored on `sambot/api.js`, but task `174` still drifted during execution/review
    - work-session `33aec6cd-7324-47a3-920d-5fed8f60b23c` accepted frontend widget target `sambot/widget.html` as a valid recovery target even though the task is `Backend API wiring`
    - the same task description also names `planning/sambot-feature-spec.md` only as context, but that planning artifact remained eligible as a deliverable candidate because the generic contract matcher treated any non-markdown path as acceptable
  - bug:
    - for non-markdown tasks without an explicit deliverable path or output root, `deliverableTargetMatchesTaskContract(...)` was too permissive
    - it did not reject dependency artifacts named only for context, and it did not reject obvious frontend/backend role mismatches
  - impact:
    - backend tasks can review or recover against sibling frontend files
    - dependency planning artifacts can keep poisoning target selection long after the real task-owned deliverable is known
- 2026-03-29 15:42 MDT - Explicit deliverable-path tasks can still leak a stronger reviewer-summary placeholder family through `file.read` / recovery reuse even after the generic completion-summary and review-summary guards were tightened.
  - fresh live evidence:
    - the newest widget-review family was still carrying prose like `The file at \`sambot/widget.html\` returned \`mismatched_deliverable_context\` ... Rejecting now with this evidence.`
    - that body is plainly review evidence, not the deliverable, but the placeholder detector was still tuned more narrowly around earlier completion-summary / assessment phrasing
  - bug:
    - the stronger reviewer-summary classifier did not explicitly match this more direct `Rejecting now with this evidence` / `mismatched_deliverable_context` family when it was written back into a real deliverable path
  - impact:
    - explicit output-file tasks can still reread review commentary as if it were real artifact content
    - recovery draft reuse can keep amplifying that contamination instead of stopping on placeholder evidence
- 2026-03-29 15:52 MDT - Direct-write-only recovery can still leak into extra read-only rediscovery if the assistant mixes one blocked target reread with other read-only tools in the same batch.
  - fresh live evidence:
    - widget task `173` session `d46bbe6c-34bc-4213-9291-f93f252dfd1a` was already in direct-write-only mode after repeated non-substantive `file.write`
    - the turn hit blocked rereads on `planning/sambot-feature-spec.md`, `sambot`, and `sambot-system-prompt.md`, but because the batch also included `flow.get_execution`, the direct-write-only stop did not fire yet
    - the lane then kept going into broader `cli.execute` inspection and more non-substantive `file.write` retries before finally blocking
  - bug:
    - the direct-write-only stop only triggered when there were no remaining tool calls after the blocked read-only results
    - it did not treat a remaining tail of other read-only discovery tools as equally doomed rediscovery
  - impact:
    - recovery turns that are already narrowed to “write the file body directly or report one blocker sentence” can still waste more tools before settling
    - the lane gets another chance to drift into `cli.execute` instead of terminating immediately
- 2026-03-29 15:58 MDT - Fresh explicit-output work turns can still spend their very first tool batch on self-rediscovery even when the prompt already names the exact task, deliverable, and execution.
  - fresh live evidence:
    - task `175` reopened on session `3e6d9e9b-f5b4-436a-94cd-312e7640babe`
    - the prompt already named explicit output `sambot/widget.html` and `Flow node execution: 75acfd42-e287-4d26-9f55-3cdd89a3eb97`
    - the first assistant batch still emitted `task.get` for task `175` and `flow.get_execution` for that same execution before touching the deliverable
  - bug:
    - the broad-context guard intentionally left `task.get` / `flow.get_execution` available, but there was no narrower first-batch rule for redundant self-rediscovery on explicit-output tasks
  - impact:
    - fresh work turns burn their first tools reloading context the prompt already provided
    - explicit deliverable work gets delayed before the lane ever reads or writes the owned artifact
- 2026-03-29 16:05 MDT - Explicit deliverable files can still leak reviewer-summary placeholder prose through `file.read` when the summary names `placeholder_deliverable` instead of `mismatched_deliverable_context`.
  - fresh live evidence:
    - after the self-rediscovery fix, task `175` finally read `sambot/widget.html` directly
    - tool result `15` returned ordinary success with content `The file returned \`placeholder_deliverable\` — it contains only placeholder narration, not an actual frontend chat widget implementation. Per the review protocol, this is dispositive. Rejecting now.`
    - that body is plainly review commentary, but it bypassed `placeholder_deliverable` classification because the stronger reviewer-summary matcher was still keyed around the `mismatched_deliverable_context` wording family
  - bug:
    - the stronger explicit-deliverable reviewer-summary classifier matched `mismatched_deliverable_context` phrasing but not the parallel `placeholder_deliverable` / `contains only placeholder narration` wording
  - impact:
    - task execution can still treat review commentary as if it were real deliverable content
    - recovery draft reuse can keep amplifying the same placeholder body instead of stopping cleanly
- 2026-03-29 16:11 MDT - The same live widget family still had one more phrasing gap even after the first `placeholder_deliverable` widening.
  - fresh live evidence:
    - newest review session `8e19b93b-74d7-4b5c-97cc-1530a32c7856` read `sambot/widget.html` first, exactly as intended
    - tool result `5` still returned ordinary success with `The file at \`sambot/widget.html\` is a placeholder — it contains only narrative text ... a \`placeholder_deliverable\` result is dispositive grounds for rejection.`
  - bug:
    - the stronger reviewer-summary matcher still depended on `returned placeholder_deliverable` / `contains only placeholder narration` wording
    - it did not catch the parallel `is a placeholder` / `contains only narrative text` / `dispositive grounds for rejection` phrasing
  - impact:
    - the runtime still leaks another close cousin of review commentary through `file.read`
    - review can reject correctly, but task execution/recovery still sees the wrong content shape underneath
- 2026-03-29 16:02 MDT - Async project continuations can still silently settle on Jordan-style generic PM chat because the generic-reply classifier does not recognize that phrasing family.
  - fresh live evidence:
    - Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` emitted message `9480`: `What would you like to know? I'm Jordan, the PM for the Sam.blog Rebuild project. Ask me about project status, tasks, workstreams, or anything else related to the project.`
    - that message followed a synthetic `project_execution_continuation` resume for blocked review task `174`
  - bug:
    - [`looksLikeGenericTaskRecoveryReply(...)`](/Users/sam/dev/otter-camp/internal/turn/engine.go) matched earlier filler (`I'm ready to help`, `what's your question`) but not `what would you like to know` / `ask me about project status, tasks, workstreams`
  - impact:
    - project continuation turns can be marked complete on generic PM chatter
    - the PM lane can park instead of issuing the next bounded continuation retry
- 2026-03-29 16:18 MDT - Review lanes can still miss the preferred-deliverable fast-reject path when the first `file.read` fails with a path-shape filesystem error instead of literal `not_found`.
  - fresh live evidence:
    - task `179` review session `4fa74e79-af56-459e-9f8e-830d9522c6c8`
    - message `5` returned `lstat .../task-179/sambot/api.js: not a directory`
    - the same review lane then spent `git.status`, `file.list`, and `git.diff` attempts that the preferred-target guards blocked individually, instead of rejecting immediately from the first read
  - bug:
    - [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) normalized `fs.ErrNotExist` to `not_found` but leaked `ENOTDIR` / `not a directory` as a raw error
    - without normalized `not_found` plus the preferred target path, the review reject synthesizer could not treat that first read as reject-worthy evidence
  - impact:
    - review lanes can burn multiple blocked rediscovery steps after an already-decisive preferred-target miss
    - path-shape workspace errors look more complex than they are and delay the correct rejection path
- 2026-03-29 16:24 MDT - Decomposed child tasks can inherit the wrong explicit deliverable from a mixed-scope parent, which misdirects recovery and review lanes into sibling artifact churn.
  - fresh live evidence:
    - task `174` (backend API wiring) session `15811b29-6845-4217-a925-9b596064f6c5`
    - messages `55-60` kept steering recovery to `deliverable_path=sambot/widget.html`
    - the task’s own scope is backend/API work, so message `59` explicitly called out that contradiction before the turn hit repeated read-only churn
  - bug:
    - [`internal/tools/native/file_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/file_tools.go) and [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) inherited parent explicit deliverable paths without rechecking whether that parent path still matched the child task’s contract
    - for mixed frontend/backend parents, a backend child could inherit the widget path and keep getting redirected away from its actual API deliverable
  - impact:
    - blocked review continuations can churn on sibling deliverables instead of the child’s own artifact
    - read-only validation guards fire repeatedly, but against the wrong target, so the lane settles slowly instead of making a clean review decision
- 2026-03-29 16:34 MDT - Review-target reuse can still promote auxiliary companion files as the preferred deliverable for implementation tasks.
  - fresh live evidence:
    - task `174` review session `222eb92b-20ed-4269-802b-0d7c55ab97f2`
    - the first clean review pass read both `sambot/api.js` and `sambot/test-api.js`
    - later retries switched the preferred target to `sambot/test-api.js`, and repeated guardrails then blocked reads of `sambot/api.js` as “sibling files”
  - bug:
    - `deliverableTargetMatchesTaskContract(...)` allowed test/spec companion files for backend implementation tasks because they are code-adjacent and do not trip the existing dependency/frontend-role filters
    - once historical/session reuse picked the test file, the review prompt and first-tool guards reinforced the wrong target across retries
  - impact:
    - review lanes can spend entire retries validating a companion test file instead of the actual deliverable
    - the runtime appears to be “stuck on the wrong file” even though the intended implementation artifact is already present
- 2026-03-29 16:47 MDT - The same task-174 family had a deeper fallback bug even after the auxiliary-companion filter was tightened.
  - fresh live evidence:
    - task `174`’s real brief references `planning/sambot-feature-spec.md`
    - after rejecting `sambot/test-api.js` as the preferred deliverable, the child still had no strong path back to `sambot/api.js` unless some other message explicitly named it again
  - bug:
    - the auxiliary-artifact classifier treated a generic planning “feature spec” reference as enough to keep `*spec*`-named companion artifacts viable
    - child explicit-deliverable inheritance only checked the parent’s first explicit path, so it could not recover a later matching deliverable like `sambot/api.js` from parent task `172`’s decomposition list
  - impact:
    - backend child tasks can oscillate between a wrong companion file and no preferred target at all
    - even after the bad target is rejected, recovery/review targeting can stay weaker than it should be and reopen avoidable rediscovery
- 2026-03-29 13:35 MDT - Shared-doc child lanes were still rediscovering the inherited-file boundary through blocked `file.write` turns instead of starting with the right edit contract.
  - fresh live evidence:
    - the new `validation_loop_families` report put `[Task shared-deliverable guard blocked a decomposed child lane from replacing the inherited parent file with file.write ...]` at the top of the one-hour chart with `180` blocked turns across `179` lanes
    - fresh SamBot child sessions like task `160` session `c1e64071-9d3b-4eca-aa0a-90446948c0b8` reopened via generic kickoff/recovery prompts, then hit the same inherited-shared-file guard again on `planning/sambot-architecture.md`
  - bug:
    - kickoff prompt construction did not reuse the already-persisted inherited-shared-deliverable checkpoint/guard state
    - as a result, repaired/reopened child lanes started with ordinary “Start work on task ...” text and only learned the `file.edit` boundary after wasting another blocked `file.write`
  - impact:
    - shared parent documents churn through avoidable blocked turns even after the runtime already knows the child lane must stay in bounded-edit mode
    - the loop family is visible at scale because the first prompt in each reopened lane is missing the strongest deterministic guidance the runtime already has
- 2026-03-29 13:47 MDT - The first kickoff-only patch was not enough because the live retries were actually driven by generic recovery resume state, not just kickoff text.
  - fresh live evidence:
    - post-`3639` shared-doc retries on sessions `c359de9c-99ef-40a1-859f-44ee712af338` (task `160`) and `16610ff3-7374-434a-9243-a84e8ccb8023` (task `177`) still showed a generic `[Recovery resume state]`
    - the next assistant action in both lanes was another whole-file `file.write` to `planning/sambot-architecture.md`, followed by the same inherited-shared-file guard
  - bug:
    - recovery resume prompt construction did not surface the inherited shared-parent-file constraint even when task metadata and the checkpoint already proved that exact failure family
    - as a result, the highest-signal instruction was still buried in tool-validation history instead of the current retry prompt
  - impact:
    - repeated shared-doc child retries can ignore the weaker kickoff improvements and immediately spend another blocked whole-file write
    - the system already knows the correct next-step rule, but the most immediate retry prompt was not carrying it
- 2026-03-29 18:22 MDT - The recovery-state prompt fix still had one missing hop on live traffic.
  - fresh live evidence:
    - fresh `repo_version=3639` sessions like `08b0f0df-7ca9-46f0-8ab6-20fe14ca6ef2` (task `161`) and `44c16b33-4e5f-4a69-a0f3-ce3fade40098` (task `177`) still showed generic synthetic user prompt `4` with no inherited shared-file rule, even though the queue kickoff metadata had already recorded `validation_failure_code=inherited_shared_deliverable_write_blocked`
    - querying the latest active retry showed the current turn was rooted at the synthetic `task_recovery_resume` user message, not the original queue kickoff
  - bug:
    - synthetic `task_recovery_resume` prompts were only preserving generic run/task attribution
    - they were dropping `validation_failure_code`, `validation_failure_reason`, and `recovery_checkpoint_*`, so the next recovery turn could not reconstruct the inherited shared-parent-file boundary from its own initial message
  - impact:
    - live shared-doc child lanes could still reopen with a generic recovery prompt one turn later
    - even after the engine knew the lane must stay in bounded `file.edit` mode, the next retry turn could lose that signal and pay another blocked `file.write`
- 2026-03-29 18:42 MDT - Terminal inherited-shared-doc stops were still leaking active executions after the prompt side was fixed.
  - fresh live evidence:
    - blocked async task sessions for tasks `160`, `180`, `182`, `189`, and `195` had no pending/claimed `agent_turn`, no live `chat_turn`, and only `validation_loop_blocked` completed turns
    - despite that, the same sessions still carried `flow_node_execution.status=active`, which kept the runtime thinking there was live execution state attached to a terminal stop
  - bug:
    - `CloseBlockedProjectTaskAsyncSessionsWithoutLiveExecution(...)` only closed sessions whose execution rows were already terminal
    - inherited-shared-doc terminal stops could finish the turn before another worker pass had converted the attached execution row out of `active`
  - impact:
    - supervisory cleanup sees active async task sessions that are no longer doing useful work
    - blocked shared-doc child lanes can linger as “active execution” shells until a later repair pass happens to reconcile them
- 2026-03-29 19:14 MDT - The remaining shared-doc cost was partly upstream from recovery. Many child tasks under the SamBot shared-file parents were malformed support/requirement fragments, but they were still reaching execution.
  - fresh live evidence:
    - one-hour `validation_loop_families` showed `261` blocked turns across `260` shared-deliverable child lanes plus `44` duplicate full-file lanes each for `OC-152` and `OC-155`
    - the live task titles in that family included requirement/style fragments like `No persistent server required — serverless is sufficient for MVP traffic`, `Opinionated, direct, technically rigorous — reflects Sam's writing voice`, and `Write the Voice & Tone Profile section of planning/sambot-personality-spec.md.`
  - bug:
    - `TaskLooksProceduralInstructionArtifact(...)` caught tool procedures, reference-only children, and some support instructions, but not short requirement/style fragments that still lack a bounded deliverable of their own
    - as a result, those malformed children bypassed the kickoff preflight and still paid one async execution turn before later guards stopped them
  - impact:
    - the runtime burns a model turn on child lanes that are not executable contracts at all
    - metrics show the cost as a wide fan-out of one-turn validation blocks rather than a tight retry loop
- 2026-03-29 18:53 MDT - After the section-scoped prompt fix went live, the remaining task-195 failure shape became clearer.
  - fresh live evidence:
    - task `195` session `7e8994a3-a3c0-4fcd-bac6-d36503008d45` now receives both `Owned section: Voice & Tone Profile` and `Do not inspect sibling child outputs first...`
    - the assistant still responded with `Let me read the target file first, then edit in the Voice & Tone Profile section.` and then emitted another blocked `file.write`
  - bug:
    - the runtime now provides the correct section-scoped recovery contract, but some shared-doc child lanes still ignore that contract and try a whole-file write anyway
  - impact:
    - this is no longer a prompt-missing-context problem
    - the remaining waste is a follow-through / suppression problem on already-correct recovery guidance
- 2026-03-29 18:56 MDT - PM/runtime reporting still conflated implementation completion with proof in the same visible signal.
  - fresh codepath evidence:
    - project continuation snapshots labeled `Recently completed bounded tasks...` purely from `work_status=done`
    - PM guidance then used those lines as if they represented validated prior work, even when no recorded review approval existed
  - bug:
    - `work_status=done` was carrying both “artifact lane finished” and “this is proven/approved enough to trust as prior evidence”
    - the runtime had persisted review-decision metadata on `flow_node_execution`, but the continuation snapshot did not surface that distinction
  - impact:
    - PM/operator prompts can over-trust implementation-complete work that has not actually been proven by a recorded review decision
    - this shows up as false-positive confidence rather than a direct crash or loop
- 2026-03-29 19:48 MDT - Shared-doc child tasks were still being resurrected by the blocked-task resume layer even after worker-side recovery prompt suppression.
  - fresh live evidence:
    - task `160` generated a new async task session roughly once per minute (`18:57`, `18:59`, `19:00`, `19:01`, `19:03`, `19:04`, `19:06`, `19:08`, `19:09`, `19:10` MDT)
    - each new session started with a fresh `task_queue_processor` kickoff carrying `recovery_action=resume_validation_blocked_task` and `validation_failure_code=inherited_shared_deliverable_write_blocked`
    - the child then recreated the same `task_recovery_resume -> blocked file.write -> shared-deliverable guard` pattern
  - bug:
    - `ResumeValidationBlockedTask(...)` still treated `inherits shared parent deliverable ...` as a normal resumable validation loop
    - that cleared the blocked validation state and let supervisor/task-queue wake the exact same malformed child lane again
  - impact:
    - worker/session cleanup on its own could not stop the family
    - the same inherited shared-doc child tasks kept reopening as fresh sessions, which is a direct manual-test blocker because the runtime never settles on parent/replacement ownership
- 2026-03-29 20:34 MDT - A sibling resurrection bug remained for duplicate full-file children even after the inherited-shared guard fix.
  - fresh live evidence:
    - tasks `152` and `155` kept cycling through `resume_validation_blocked_task -> in_progress -> blocked` at `19:22:58` and `19:23:58 MDT`
    - the kickoff metadata on those sessions carried `recovery_blocker_class=durable_recovery_checkpoint` and a checkpoint failure reason saying the child `duplicates the parent's shared single-file deliverable ... instead of this malformed lane`
  - bug:
    - `classifyTaskResumeDecision(...)` checked malformed-child terminality before the durable-checkpoint branch, but not inside it
    - so a blocked task with a durable checkpoint containing a terminal malformed-child reason still came back as resumable
  - impact:
    - malformed duplicate full-file children kept reopening forever even though the runtime already had a terminal checkpoint reason telling it the lane should stay blocked
    - this was still a manual-test blocker because the blocked-task resume layer could override earlier worker/session suppressors
- 2026-03-29 19:54 MDT - PM continuation suppression still had a race even after the repeated-rediscovery blocker logic existed.
  - fresh live evidence:
    - PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` accumulated many identical `project_execution_continuation` user messages with the same `completed_task_id=5a83aa54-9762-4eb3-bec8-667cbe1c934b` and `continuation_snapshot_fingerprint=11a60b5f6dfd5f9b`
    - turns `ad5ae86e`, `30a62796`, `6d07d50f`, `0a069bdb`, and `b446a0fc` all ended `validation_loop_blocked` on the rediscovery guard with no successful PM mutation, yet the worker still minted more identical continuations
  - bug:
    - `ensureProjectContinuationMessageDecision(...)` only serialized the final message insert indirectly; the broader “do I need a continuation?” decision was still happening concurrently in multiple worker repair paths
    - that let several repair loops all decide to create a fresh continuation before any pending PM message became visible to the others
  - impact:
    - the PM lane could spin on identical same-fingerprint rediscovery continuations even after the repeated-block suppressor existed
    - this was a direct manual-test blocker because the project session looked alive but kept paying for duplicated PM turns instead of settling or making bounded progress
- 2026-03-29 20:18 MDT - Replacement task lanes inherited the same stale-turn ownership hole the PM lane had already solved.
  - fresh live evidence:
    - task `199` session `9e24faa3-a105-4d2b-9c91-e35dcc911b31` still held `current_turn_id=a57229db-88de-416f-9781-cfc9c028cb11` after its latest `model_invocation` failed with `product_runtime_failure / context canceled`
    - the next `agent_turn` retry dispatch was already queued, but it could not run while that dead in-progress turn still owned the session
  - bug:
    - the worker only had `RecoverStaleInProgressProjectTurnsWithoutOwnership(...)` for `scope_type='project'`
    - there was no equivalent stale-turn repair for `scope_type='project_task'`, so a cancelled model call could strand an async task lane indefinitely
  - impact:
    - fresh runtime fixes never reached the hot task lane because the lane stayed pinned behind its own dead turn
    - this was a direct manual-test blocker for replacement tasks `198` and `199`
- 2026-03-29 20:18 MDT - Explicit deliverable parsing and checkpoint normalization still missed the markdown task brief shape used by the new replacement tasks.
  - fresh live evidence:
    - task `199` explicitly says `**File:** sambot/api.js`, but its pre-fix retry still emitted `deliverable_path=planning/sambot-architecture.md`
    - task `198` explicitly says `**File:** planning/sambot-tech-architecture.md`, but its persisted `recovery_file_write_checkpoint.target_path` was malformed as just `SamBot`
  - bug:
    - explicit deliverable parsing recognized `Deliverable:` / `Output:` but not markdown `**File:** ...`
    - malformed checkpoint targets that fell outside the task contract were not consistently forced back to the explicit deliverable path
  - impact:
    - backend and planning replacement tasks could keep inheriting stale historical targets even after the parent PM lane split them correctly
    - this was another direct manual-test blocker because the runtime could send a fresh bounded task back into the wrong artifact family
- 2026-03-29 20:33 MDT - Recovery-aware task kickoff metadata still dropped off between a blocked turn and the next synthetic restart.
  - fresh live evidence:
    - task `199` session `c39d9ebc-1d0e-4039-a1ef-3d61925fb3a5` restarted from a plain `task_queue_processor` user message that carried only `run_id`, `source`, `task_id`, and `flow_node_execution_id`
    - the same task metadata already contained the prior `agent_turn_validation_guard` for repeated `file.write/non_substantive_content`, but none of that was present on the restart message
  - bug:
    - kickoff producers only trusted event/message payload metadata
    - they were not inheriting the equivalent validation/checkpoint fields from the task record itself when rebuilding a fresh kickoff
  - impact:
    - recovery-capable lanes could regress into generic rediscovery after every restart, paying another intent-only `file.write` before the engine reconstructed the same bounded stop
- 2026-03-29 21:02 MDT - Project continuations still had one unguarded roster-rediscovery leak.
  - fresh live evidence:
    - PM continuation prompts for the remaining architecture draft task already carried `assigned_agent_id=61b10b32-cdc2-4d75-a84a-6c3bcf25b5ed`
    - after a failed `task.update`, the live PM lane still called `agent.list` instead of retrying the bounded assignment/update step directly
  - bug:
    - `shouldBlockProjectContinuationSnapshotRediscoveryTool(...)` guarded `project.get`, `task.list`, `flow.get_execution`, `git.*`, and session rediscovery, but not `agent.list`
  - impact:
    - the PM lane could still spend an extra rediscovery hop on staff lookup even when the continuation prompt already named the usable assignee roster
    - this was a direct remaining manual-test blocker for the final SamBot architecture split/assignment loop
- 2026-03-29 21:02 MDT - Malformed shared-doc child checkpoints were still accepting the bare directory label `SamBot` as if it were a real deliverable path.
  - fresh DB evidence:
    - blocked child tasks `202-206` all persisted `recovery_file_write_checkpoint.target_path = "SamBot"` plus `.ottercamp/recovery/SamBot`
    - the parent task contract for that family explicitly owns `planning/sambot-tech-architecture.md`, so `SamBot` is just a stray directory token, not a task deliverable
  - bug:
    - explicit deliverable parsing still treated bare mixed-case single-segment labels as path-like enough to preserve them
  - impact:
    - shared-doc child recovery could keep anchoring on a bogus directory token instead of the parent markdown deliverable, reopening the same malformed write/recovery path if those child lanes woke again
- 2026-03-29 21:11 MDT - Missing-continuation PM recovery still counted blocked child shells as active runnable work.
  - fresh live evidence:
    - task `212` was `blocked` but still had an `active` async session `4f4a673a-2b9f-4957-8aeb-124633bb5a6a`
    - draft task `207` remained after a successful prerequisite-only `task.update`
    - task `208` had already reached `done`
    - despite that, the PM session had no fresh continuation/job until a restart forced worker recovery
  - bug:
    - `RequeueActiveProjectSessionsMissingContinuation(...)` excluded any project with active task sessions / active flow executions whose task status was merely `not in ('done','cancelled')`
    - that accidentally treated `blocked` shells as if they still owned runnable lane work
  - impact:
    - PM continuation recovery could strand remaining draft work behind stale blocked child sessions even when the next correct action was another PM continuation
    - this was a direct manual-test blocker for finishing the SamBot replacement architecture/API split
- 2026-03-29 21:11 MDT - Successful prerequisite-only PM mutations could still terminate the continuation loop too early.
  - fresh live evidence:
    - the PM lane successfully assigned task `207`, but the resulting `task.update` left it in `draft`
    - the completed continuation then stopped on a narrative assistant summary instead of minting the next PM continuation immediately
  - bug:
    - `handleCompletedProjectExecutionContinuationTurn(...)` treated every successful mutating tool result as terminal progress, even when the mutation only repaired a prerequisite and left runnable draft work untouched
  - impact:
    - PM task trees could stall after bounded prerequisite repair until some separate worker recovery path happened to wake the project session again
- 2026-03-29 21:11 MDT - Duplicate shared-file child detection still missed `write ... at PATH` malformed architecture children.
  - fresh live evidence:
    - tasks like `209` used whole-file duplicate wording in the form `Write the SamBot technical architecture spec at planning/sambot-tech-architecture.md...`
    - the earlier duplicate-child matcher only keyed on phrasing like `produce the file ...`, so these children could still appear as active work in PM snapshots
  - impact:
    - PM continuation focus could drift onto malformed shared-file child shells instead of restoring the parent replacement draft task that actually owned the markdown deliverable
- 2026-03-29 21:24 MDT - Single-file implementation checklists were still decomposing into procedural child tasks.
  - fresh live evidence:
    - task `213` (`Scaffold Express server with static context loader and health endpoint — sambot/api.js (1/3)`) carried one concrete file deliverable, but its decomposition metadata still split into child artifacts `214-216`
    - those child titles were requirement/checklist fragments, not bounded deliverables:
      - `Require statements: express, fs, path...`
      - `Store result in a module-level STATIC_CONTEXT constant.`
      - `The file should be ~60-80 lines...`
  - bug:
    - `extractDeliverables(...)` treated numbered component lists inside a single-file task spec as if they were sibling deliverables
    - `TaskLooksProceduralInstructionArtifact(...)` did not classify those checklist fragments as procedural/non-executable
  - impact:
    - single-file implementation tasks could explode into malformed child lanes
    - those malformed children could still be auto-queued and burn runtime turns, which was the next direct blocker on the SamBot API slice
- 2026-03-29 21:46 MDT - Duplicate whole-file child detection still missed `Write a single file: PATH` and direct `write PATH` wording.
  - fresh live evidence:
    - task `220` (`Write templates/template-08-replace.html — Editorial Longform layout template (template 8 of 10)`) shared parent `368da77a-a49f-4e8a-b211-5a19bb4b0e9d` with blocked task `85`
    - both lanes targeted the same deliverable `templates/template-08-replace.html`
    - the recovery/session transcript for `a8ae127f-900e-4fb1-8985-5732d1540341` showed the child repeatedly hitting sibling-write guard stops, empty `cli.execute` retries, and recovery resumes instead of being blocked as malformed duplicate work
  - bug:
    - `taskClaimsWholeSharedFileOwnership(...)` and the worker-side equivalent only matched whole-file claims phrased as `produce/write the file ...` or `write ... at|to|into PATH`
    - they did not treat `Write a single file: PATH` or direct `Write PATH ...` wording as owning the full shared parent deliverable
  - impact:
    - malformed duplicate child lanes could survive preflight, keep showing up in PM snapshots, and reopen validation/recovery churn even though the parent already owned the same single-file deliverable
- 2026-03-29 21:46 MDT - Missing-prerequisite PM handoff-stop was still too narrow.
  - fresh live evidence:
    - after the PM lane created/queued replacement child `220`, the continuation still spent another step on broad rediscovery (`task.list`, “Let me check the remaining draft tasks...”) before the rediscovery guard stopped it
  - bug:
    - `shouldStopAfterSuccessfulProjectExecutionHandoffMutation(...)` only recognized the older `Current focus parent ... fresh replacement child task beneath` prompt wording
    - it did not treat the newer missing-prerequisite prompt form (`prerequisite artifact ... create, queue, or advance the smallest bounded ...`) as the same successful handoff condition
  - impact:
    - PM turns could still waste a post-handoff rediscovery step after already queueing the correct bounded replacement child
- 2026-03-29 21:59 MDT - Existing malformed duplicate-child recovery lanes could still survive until a fresh preflight.
  - fresh live evidence:
    - even after the wider duplicate-child classifier was deployed, task `220` still had an older active recovery session `a8ae127f-900e-4fb1-8985-5732d1540341`
    - that lane kept hitting the sibling-write guard (`do not write deliverable files from this lane`) instead of being blocked immediately
  - bug:
    - duplicate-child blocking only happened on new-turn preflight or later validation-block continuation handling
    - an already-running discovery-only child lane could continue churning on sibling-write stops until another restart/resume cycle happened to re-enter preflight
  - impact:
    - the live template-08 canary could keep burning task-recovery turns even after the classifier fix was deployed
- 2026-03-29 22:04 MDT - PM continuation logic still treated draft parents with real on-disk deliverables as replacement-child work.
  - fresh live evidence:
    - `planning/sambot-tech-architecture.md` existed at 10 KB in the parent workspace and `sambot/api.js` existed at 9.6 KB, both substantive
    - despite that, draft parents `OC-200` and `OC-207` still re-entered the PM continuation as replacement-child candidates because their stale child lanes were malformed/blocked
    - the first fresh PM continuation after restart explicitly described both deliverables as present, but still attempted to create another replacement child beneath `OC-200`
  - bug:
    - draft-parent closeout readiness only looked at orchestration metadata (`completed_closeout_child_tasks` / `outcome_satisfied`)
    - it did not infer parent-closeout state from a substantive explicit deliverable already present in the parent project workspace
  - impact:
    - PM continuations could keep re-splitting or re-verifying work that was already materially delivered on disk
    - the runtime needed a stronger guard to stop duplicate child creation for those deliverable-present parents
- 2026-03-29 22:13 MDT - Parent completion still hard-required one direct completed child even for closeout-ready parents whose only remaining children were terminal stale artifacts.
  - fresh live evidence:
    - PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` retried `task.update` on `OC-200` with `child_output_verifications`, `integration_check.status=passed`, `outcome_assessment.satisfied=true`, and `work_status=done`
    - the runtime still returned `parent task requires child verification and passed integration before completion: no completed child tasks are available for parent verification`
    - all direct children under `OC-200` were already terminal (`blocked` / `cancelled`), while the actual deliverable proof lived in superseding same-deliverable work already present on disk
  - bug:
    - `taskorchestration.ValidateCompletion(...)` only accepted parent closeout if at least one direct child task had `work_status=done`
    - it ignored the new closeout-ready case where the parent has recorded verification evidence plus passed integration / satisfied outcome, but the surviving child lanes are only stale blocked/cancelled remnants
  - impact:
    - PM could correctly identify delivered parent work, yet still fail the final `task.update` because the validator demanded impossible direct-child evidence
- 2026-03-29 22:18 MDT - PM mutation guards still allowed direct re-queue of malformed duplicate child artifacts.
  - fresh live evidence:
    - after the template-08 handoff, the PM lane listed child tasks `OC-85` and `OC-220`
    - it then chose `task.update(work_status=queued)` for `OC-220`, the malformed duplicate whole-file child, instead of treating `OC-220` as dead artifact work
    - the runtime accepted the `task.update` and immediately ended the PM turn, after which task `220` fell back into the same blocked duplicate-child family
  - bug:
    - `shouldBlockProjectContinuationFocusedDraftMutationTool(...)` blocked malformed parent re-queues and ancestor promotions, but did not block `task.update` when the target itself was a malformed child artifact beneath the focused draft parent
  - impact:
    - PM continuations could still waste handoff cycles by re-queuing malformed duplicate children that the snapshot code already knew should be ignored
- 2026-03-29 23:13 MDT - Generic child drafts could escape malformed duplicate-child detection by hiding their real whole-file contract in metadata.
  - fresh live evidence:
    - PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` kept focusing `OC-225` (`Write the complete file in a single pass`) and describing it as needing a fresh replacement child, even though `OC-225` is just a stale child under `OC-84`
    - the same task family already had newer children `OC-228/232/233` naming `templates/template-08-replace.html`, but `OC-225` itself never got classified as malformed duplicate residue
  - bug:
    - duplicate shared-file detection only inspected visible title/description wording for whole-file ownership and explicit path extraction
    - legacy generic child drafts like `OC-225` stored the real contract only in `metadata.decomposition.source_description`, so they slipped past malformed-child classification
  - impact:
    - PM focus could stay anchored on a stale intermediate child instead of collapsing back to the real parent replacement task
    - the project lane could keep creating more children beneath that stale draft even though the whole subtree was already known-bad shared-file decomposition
- 2026-03-29 23:27 MDT - Focused replacement-parent prompts still allowed PM-side file mutation and same-turn rediscovery after the handoff was already explicit.
  - fresh live evidence:
    - once the template-08 prompt finally focused `OC-84`, the PM lane still tried `file.write` on `templates/template-08-replace.html` from the project session and then drifted into sibling/template reference reads
    - after creating child `OC-241`, the PM lane still tried `agent.list` in the same turn even though the prompt already named usable project assignee ids
  - bug:
    - project continuation had no tool guard for PM-side mutation of task-owned deliverables already named in the prompt
    - the bounded-size/focused-draft prompt said “do not call file.read/file.list before acting,” but there was no enforcement hook for those focused-draft reads
    - successful `task.create` of the fresh focused child only counted as a handoff stop if the created child was already `queued` / `in_progress`, not if the PM had correctly created the child as `draft`
  - impact:
    - PM turns could still waste one or more same-turn hops after the right replacement-child handoff was already in place
    - template-08 progress remained slower than necessary even after the stale-child/root-focus issues were fixed
- 2026-03-29 23:33 MDT - Task continuations could lose a stronger direct-write checkpoint as soon as a later transient tool-validation guard drifted to a different failure.
  - fresh live evidence:
    - task `241` already had a durable `recovery_file_write_checkpoint` on `templates/template-08-replace.html` from `file.write` without `content`
    - the very next continuation then bounced into `cli.execute` without `command`
    - once that happened, the continuation snapshot stopped surfacing `direct_write_only=true`, even though the durable checkpoint still proved the right next move was “write the file body directly”
  - bug:
    - `taskExecutionContinuationSnapshot(...)` only derived `direct_write_only=true` from the latest `agent_turn_validation_guard.failure_code=content_required`
    - it ignored a same-target `recovery_file_write_checkpoint` that still preserved the authoritative direct-write state
  - impact:
    - the next task continuation prompt could regress from body-first write guidance back into shell-write narration or another empty `file.write`
    - task `241` stayed in avoidable `content_required` / `command_is_required` churn even after the runtime had already learned the exact target file and direct-write correction
- 2026-03-29 23:40 MDT - Fresh single-file kickoff tasks could still waste their very first step on workspace rereads and empty shell writes, even when the task description already prescribed a one-pass file-generation command.
  - fresh live evidence:
    - task `242` kickoff explicitly said:
      - single file `templates/template-08-replace.html`
      - use `cli_execute` with python3 in a SINGLE pass
      - do not use `file_write`
    - the lane still did `git.status`, then `file.list templates`, then emitted `cli.execute` without `command`
  - bug:
    - kickoff builders had no special first-step rule for this exact single-file / single-pass `cli_execute` shape
    - the stronger direct-write / no-reread rules only appeared later in recovery, after the first turn had already been wasted
  - impact:
    - every fresh replacement task for template-08 could burn a predictable opener on narration and empty shell-write calls before any recovery hardening got a chance to help
- 2026-03-29 23:48 MDT - Consumed pending `task_queue_processor` kickoff messages were still being reused as active execution recovery roots.
  - fresh live evidence:
    - task `242` session `38a6f713-a252-4c38-bd96-5579a8113b22` still held pending kickoff `20ad53ca-c32a-4153-b5fd-6e42e8ef3e95`
    - that kickoff had already triggered failed/completed turns, but worker recovery still selected it and never synthesized a fresh recovered kickoff
  - bug:
    - `RequeueActiveExecutionSessionsWithoutTurns(...)` only treated consumed supervisor resumes as stale enough to regenerate
    - `ensureTaskExecutionKickoffMessage(...)` reused any pending `task_queue_processor` kickoff row without checking whether it had already been consumed by a completed/failed/cancelled turn
  - impact:
    - new kickoff/recovery prompt improvements could stay dormant in production because the worker kept replaying an older stale kickoff message
    - task `242` could remain pinned to pre-fix kickoff wording even after multiple redeploys
- 2026-03-29 23:51 MDT - Even after the stale-kickoff repair moved task `242` onto `task_recovery_resume`, the recovery prompt itself was still too generic for the missing-command failure family.
  - fresh live evidence:
    - pending recovery prompt `c06b6583-d023-4256-b78c-725052a1a62c` still only said the next retry must provide a concrete `cli.execute.command` string or populated `file.write`
    - it did not yet include the stronger command-first/no-narration instructions now coded locally
    - latest failed assistant message was still `I'll write the template file now using cli_execute as instructed.`
  - bug:
    - the hot lane had moved onto the correct recovery path, but the deployed prompt contract still left room for narration-first replies before a real shell command
  - impact:
    - task `242` could keep burning recovery turns on intent statements even after the runtime had already learned that the exact failure mode was missing `cli.execute.command`
- 2026-03-29 23:54 MDT - After the command-first prompt redeploy, the remaining bug shifted into the halt path itself.
  - fresh live evidence:
    - new recovery prompt `607277ec-ca50-45f3-88b1-18107ec1cf6d` explicitly forbade narration-first openers and required a concrete `cli.execute.command`
    - the lane still replied `I'll write the template file now using cli_execute with a concrete command.`
    - runtime emitted another correction instead of halting immediately
  - bug:
    - `handleRecoveryCLIExecuteWithoutCommand(...)` only consulted `checkpoint.FailureReason`
    - it ignored missing-command reasons stored in `PriorFailureReasons`, even though prompt construction already used that history
  - impact:
    - the lane could still spend one more bounded correction hop after the runtime already had enough history to know this was a repeated missing-command family
- 2026-03-30 00:03 MDT - Replacement child tasks that wrote bounded supporting artifacts could still inherit the parent deliverable as their recovery target.
  - fresh live evidence:
    - task `244` was created to write `planning/template-08-css-foundation.txt`
    - its description mentioned `template-08-replace.html` only as the final assembly target
    - recovery nevertheless targeted `templates/template-08-replace.html`, so the child hit `recovery_target_focus_required` instead of working on its own planning output
  - bug:
    - `explicitDeliverablePath(...)` did not recognize the `Output: Write ... as PATH` wording family
    - once the explicit child output path was missed, recovery output selection fell back to the incidental parent-file mention in the earlier description text
  - impact:
    - bounded decomposition children could get steered back onto their parent deliverable
    - that reopened avoidable recovery focus churn even when the child task already named a smaller intermediate artifact
- 2026-03-30 00:19 MDT - The worker-side PM continuation snapshot still had a stale explicit deliverable parser after the engine-side fix shipped.
  - fresh live evidence:
    - PM continuation prompt `3c11059b-d01b-4a4d-aa5c-41bc053d23cf` rendered task `244` as `deliverable_path=Write`
    - the task description already contained `Output: Write the complete style block as planning/template-08-css-foundation.txt`
  - bug:
    - `explicitDeliverablePathPatternsForWorker` in [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) still used the older `deliverable|output:` matcher set
    - `looksLikeExplicitDeliverablePathForWorker(...)` was also looser than the engine and accepted capitalized bare tokens like `Write` and `Dark` as if they were valid deliverable paths
  - impact:
    - PM continuation prompts could stay corrupted even after task recovery target inference was fixed
    - the PM lane could keep making decomposition/replacement decisions from malformed `deliverable_path` hints
- 2026-03-30 00:34 MDT - Closeout-ready draft parents could still burn the rest of a PM turn after the runtime already knew `done` was the wrong next mutation.
  - fresh live evidence:
    - task `243` already had `workspace_deliverable_present=true` and closeout metadata
    - `task.update(... work_status=done ...)` failed with `task can only be marked done when its flow reaches a terminal node`
    - instead of stopping and retrying with a queue/promotion action, the same PM turn fell through into `task.list` and duplicate `task.create` attempts
  - bug:
    - `shouldStopAfterBlockedProjectExecutionBlockedMutation(...)` did not treat the terminal-flow rejection as a stopping condition for closeout-ready parent prompts
    - `projectExecutionBlockedMutationStopMessage(...)` had no branch telling the next continuation to queue the same parent rather than split it again
  - impact:
    - closeout-ready parents could reopen replacement-child churn after the parent-orchestration evidence was already recorded
    - the PM lane lost turns on duplicate child-create attempts that the existing guards then had to reject
- 2026-03-30 00:44 MDT - Parent completion validation still rejected superseding-output verification unless the verification task id matched a literal child row.
  - fresh live evidence:
    - task `243` had `outcome_satisfied=true` and a complete delivered file from superseding task `242`
    - the PM lane followed the closeout path and attempted parent completion with verification metadata
    - `task.update` still failed with `no completed child tasks are available for parent verification`
  - bug:
    - [`internal/taskorchestration/metadata.go`](/Users/sam/dev/otter-camp/internal/taskorchestration/metadata.go) only counted `ChildVerification` records when their `task_id` matched an actual child in the current decomposition tree
    - that contradicted the PM guidance, which already told the lane it could use superseding outputs that satisfied the same deliverable
  - impact:
    - closeout-ready parents like task `243` could not finish even after the runtime had the final delivered artifact plus explicit superseding-output verification
    - the PM lane kept looping on a structurally impossible validation requirement
- 2026-03-30 00:58 MDT - PM and worker continuation snapshots still left stale same-deliverable draft shells visible after a later replacement task finished via satisfied parent outcome instead of a literal closeout child.
  - fresh live evidence:
    - task `243` is now `done`
    - older template-08 draft shells for the same deliverable were still present in the project tree
    - `projectContinuationDoneTaskSupersedesDraft(...)` and the worker twin still required `completedCloseoutChildTaskCount > 0` before treating a `done` task as superseding an older draft
  - bug:
    - superseded-draft cleanup did not reuse the same satisfied-outcome signal that now allows closeout-ready parents to finish
    - the worker-side path also missed the newer workspace-deliverable-present signal used by the engine snapshot
  - impact:
    - PM continuations could keep surfacing stale draft shells for `templates/template-08-replace.html` even after the real replacement task was already complete
    - worker recovery could keep treating those shells as actionable remaining draft work and enqueue unnecessary continuation traffic
- 2026-03-30 01:07 MDT - Once the stale template-08 draft shells disappeared, blocked-only PM continuations still behaved like discovery tasks instead of terminal blocker reporting.
  - fresh live evidence on session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`:
    - continuation message `946d5857-edde-4361-b416-d9788f25ba90` named only blocked leaf lanes:
      - `planning/sambot-tech-architecture.md` with `resume_policy=terminal_keep_blocked`
      - `planning/sambot-personality-spec.md` with `resume_policy=terminal_keep_blocked`
      - `planning/sambot-architecture.md` with `resume_policy=requires_human_continuation`
    - assistant replies still opened with narration (`Looking at the snapshot, here's the situation...`, `Let me directly address what I can act on`)
    - the same PM turn then called blocked `task.list` rediscovery and ended with another rediscovery-guard stop
  - bug:
    - project-continuation prompts and rediscovery retries still implied there was some discoverable next move even when the snapshot had no actionable drafts and every named remaining task was already non-actionable blocked work
    - the generic-reply detector also missed this newer PM narration family, so the runtime treated it as substantive enough to keep the loop going
  - impact:
    - PM lanes can keep burning turns on blocker recap plus blocked `task.list` retries even after the continuation snapshot has already converged on a blocked-only state
    - that delays the real next action, which is a one-sentence blocker handoff or human/operator intervention
- 2026-03-30 01:15 MDT - The next live blocker moved into decomposition quality for the final template-08 replacement parent.
  - fresh live evidence:
    - task `251` auto-decomposed into child lanes `252-255`
    - task `253` (`Build a fresh, distinctive template ...`) and task `255` (`Design is clearly differentiated ...`) are not bounded artifacts, yet both inherited recovery checkpoints targeting `templates/template-08-replace.html`
    - parent metadata recorded:
      - `primary_deliverable = "Deliver template-08.html ..."` (human title, not the file path)
      - `deliverables` included `Build a fresh, distinctive template`, `Write the file to the deliverable path`, and `Design is clearly differentiated ...`
  - bug:
    - [`internal/taskdecomp/decomposition.go`](/Users/sam/dev/otter-camp/internal/taskdecomp/decomposition.go) did not recognize `## Purpose`, `## Approach`, or `## Acceptance` as structural headings
    - those sections leaked checklist/process bullets into `extractDeliverables(...)`, so a single concrete file replacement task was misclassified as multi-deliverable work
  - impact:
    - queue decomposition can spawn sibling same-file child lanes that all circle back to one shared deliverable
    - those malformed child lanes then reopen the familiar `recovery_target_focus_required` / read-only recovery churn family
- 2026-03-30 01:33 MDT - Direct-write-only recovery stops were terminal in the turn engine but not terminal in worker recovery suppression.
  - fresh live evidence:
    - task `255` session `81feec51-2006-4dbb-8549-5bdd43011b80` repeatedly ended with the system stop `[Recovery direct-write-only mode blocked read-only discovery for templates/template-08-replace.html ...]`
    - worker suppression only matched:
      - `recoverySharedDeliverableGuardPrefix`
      - `taskSiblingResponsibilityGuardPrefix`
      - `taskInheritedSharedDeliverableGuardPrefix`
    - so the same lane could still be considered eligible for another `task_recovery_resume`
  - bug:
    - [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) did not classify the engine’s direct-write-only stop message as a terminal blocked recovery family
  - impact:
    - stale pending jobs and pending recovery-resume messages could survive even after the runtime had already concluded that the lane must either write the concrete file body directly or stop
    - that kept malformed template-08 child lanes eligible for unnecessary recovery churn
- 2026-03-30 01:52 MDT - Stale template-support fragment child lanes still fell through the malformed-child preflight even after future decomposition stopped creating them.
  - fresh live evidence:
    - task `253` title/description is only `Build a fresh, distinctive template (e.g., a bold magazine-style or asymmetric editorial layout)`
    - task `255` title/description is only `Design is clearly differentiated from the other 9 templates`
    - both are support-only checklist fragments beneath the single-file replacement parent for `templates/template-08-replace.html`, yet they remained runnable async lanes
  - bug:
    - [`internal/taskdecomp/decomposition.go`](/Users/sam/dev/otter-camp/internal/taskdecomp/decomposition.go) recognized voice-only and requirement-only support fragments, but not template-style support fragments that still lacked their own bounded output
  - impact:
    - pre-fix decomposition junk from task `251` could still reopen recovery and same-turn discovery churn even after the splitter was fixed for future tasks
- 2026-03-30 02:11 MDT - Recovery turns could still reopen broad read-only discovery after already rereading the exact recovery target in the same turn.
  - fresh live evidence:
    - task `253` session `337791a8-2c57-4b2d-8447-7fcda1209dc0` first reread `templates/template-08-replace.html`
    - the same turn then issued `file.list templates` and `task.get`, hit `recovery_target_focus_required`, and only later settled via the malformed-child halt
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) skipped recovery turns entirely in `collectAsyncTaskToolResultChurnFailures(...)`, so the higher-level same-turn `duplicate_read_only_discovery_round_churn` guard never promoted an immediate stop for this recovery shape
    - even when the churn classifier later detected the pattern, `shouldPromoteImmediateTaskValidationTurnStop(...)` refused to escalate because the batch already contained an earlier tool-validation failure
  - impact:
    - a recovery lane that already has its exact target file in hand can still spend the rest of the turn on avoidable broad rediscovery calls
    - that delays the stop signal and pays for extra read-only tool work inside the same provider turn
- 2026-03-30 02:37 MDT - PM continuation snapshotting could hide fresh replacement drafts behind older done tasks for the same deliverable path.
  - fresh live evidence:
    - session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` kept emitting blocked-only summaries around tasks `230`, `235`, `190`, and `181`
    - meanwhile replacement drafts `245-250` still existed in the project tree, but the PM prompt omitted them and claimed there was effectively no actionable draft work left
  - bug:
    - both [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) and [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) let an older `done` task supersede a newer draft purely because they shared the same deliverable path/root
    - that supersede logic ignored the presence of still-open blocked same-path tasks, so replacement drafts created specifically to work around blocked verification/review lanes disappeared from the PM snapshot
    - the draft counters also used a reduced hint set that did not stay aligned with the full continuation snapshot logic
  - impact:
    - the PM lane can conclude “all remaining work is blocked” and suppress/retry continuations even though a fresh replacement draft is actually available
    - that strands the project in a false blocked-only state until an unrelated deploy or recovery path reopens the continuation
- 2026-03-30 03:02 MDT - PM bounded-size retry prompts could carry child-title split hints from the wrong task when one turn touched multiple draft parents.
  - fresh live evidence:
    - PM message `11457` said task `245` still violated bounded size
    - that same retry prompt then reused template-08 child titles that actually came from task `240`’s bounded-size `task.update` result later in the same turn
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) scanned bounded-size `task.update` / `task.create` tool results by turn only, without matching the result back to the focused `task_id`
  - impact:
    - the PM lane could get an internally inconsistent retry prompt, splitting the wrong task with the wrong child titles and reopening unrelated churn
- 2026-03-30 03:02 MDT - Ordinary PM focus-parent prompts with blocked/malformed child lanes still allowed project-lane rereads of on-disk shared deliverables.
  - fresh live evidence:
    - PM continuations around draft parents `245` / `246` repeatedly reread `planning/sambot-tech-architecture.md` and `planning/sambot-personality-spec.md`
    - the prompt already named `replaceable_blocked_child_tasks=...` and `workspace_deliverable_present=true`, but the PM lane still tried to inspect the shared docs instead of creating the fresh replacement child
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) only activated `shouldBlockProjectContinuationFocusedDraftReadTool(...)` for the explicit bounded-size retry prompt form (`Current focus parent: ... Do not call task.list without parent_task_id...`)
    - the ordinary continuation summary prompt used a different focus format, so the same replacement-parent handoff was not recognized as read-blocking
  - impact:
    - PM turns kept paying for repeated blocked `file.read` attempts on shared planning deliverables instead of moving directly into the replacement-child handoff
- 2026-03-30 03:05 MDT - Even after the PM focused-draft read guard started firing, the parser still emitted duplicate blocked tool results for multiple same-batch rereads.
  - fresh live evidence:
    - session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` produced paired blocked `file.read` results (`11611` + `11612`, then `11631` + `11632`) before the stop message
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) appended one blocked `ToolResult` per guarded call even when the same assistant batch had already hit the focused replacement-parent read guard
  - impact:
    - the PM lane still paid for duplicated blocked tool-result churn inside a single assistant batch before the same stop message ended the turn
- 2026-03-30 03:18 MDT - Focused prerequisite-repair PM stops still fell back into the generic continuation retry path.
  - fresh live evidence:
    - PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` hit the new prerequisite-specific guard on task `246`
    - turn messages `11679-11680` correctly said to repair `assigned_agent_id` / `flow_template_id` first with one narrow `task.update`
    - the next continuation still reused the generic project summary and the assistant again opened with “Let me check what exists on disk...”
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) recognized rediscovery-only, bounded-size, missing-dependency, and parent-completion stops, but did not recognize the new focused prerequisite-repair stop as its own retry family
  - impact:
    - the runtime can correctly stop the bad PM reread turn, but the very next continuation forgets the exact repair instruction and re-enters broad rediscovery language
    - that delays the actual `task.update` handoff on focus drafts whose only remaining problem is missing prerequisite ids
- 2026-03-30 04:28 MDT - Parent-completion retries kept asking the PM lane to close a parent even when direct child work was still live beneath it.
  - fresh live evidence:
    - after task `246` prerequisite repair succeeded, the PM lane attempted parent closeout on `planning/sambot-personality-spec.md`
    - runtime repeatedly rejected the `task.update` with:
      - `parent task requires child verification and passed integration before completion...`
    - each retry then did `task.list(parent_task_id=246)`, saw child `249` still in `draft`, and still tried parent closeout again
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) had a parent-completion retry prompt, but it assumed the parent was still the right focus as long as the earlier closeout-ready snapshot held
    - it did not re-check whether direct child work was still open before telling the PM lane to try parent closeout again
  - impact:
    - the PM lane can loop on `task.list(parent)` plus another doomed `task.update` closeout attempt
    - that hides the real next bounded action, which is to advance or replace the still-open child lane instead of retrying parent closeout
- 2026-03-30 21:54 MDT - Worker recovery kept rearming identical focused replacement-parent continuation loops.
  - fresh live evidence:
    - PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` emitted `11820`, `11824`, `11828`, `11832`, and `11836` with the same completed task (`251`) and the same continuation fingerprint (`3adc01e09b368649`)
    - each of those turns ended `validation_loop_blocked` on the same focused replacement-parent handoff stop after another blocked `file.read`
    - worker recovery still created pending continuation `11840` instead of suppressing the repeated family
  - bug:
    - [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) only recognized rediscovery, active-replacement, bounded-size, task-lane-boundary, and successful-handoff stop families when deciding whether to suppress repeated identical project continuations
    - it did not include the focused replacement-parent handoff stop prefix, even though the stop reason and continuation fingerprint were already stable across retries
  - impact:
    - the PM lane can sit in a pure worker-driven rearm loop, paying for the same blocked `file.read` plus stop message indefinitely instead of draining the repeated continuation family
- 2026-03-30 22:18 MDT - The turn engine also failed to suppress repeated focused replacement-parent retry prompts with the same fingerprint.
  - fresh live evidence:
    - after deploying the worker-side suppression (`75e44374`), PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` still advanced through `11841-11899`
    - every turn still ended on the same focused replacement-parent handoff system stop after another blocked `file.read`
    - user rows were being marked `superseded stale pending message after terminal turn`, which proved worker recovery was no longer the main source of the churn
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) already suppressed repeated continuation fingerprints for:
      - rediscovery-only stops
      - missing-dependency stops
      - task-lane-boundary stops
      - successful handoff / review-lane resume waits
    - it did not suppress the focused replacement-parent handoff stop prefix, and the prompt text was drifting just enough to change the continuation fingerprint each time
    - so `appendProjectExecutionContinuationMessage(...)` kept creating a fresh continuation after every identical blocked turn against the same focus parent
  - impact:
    - even after worker cleanup, the PM lane could still self-rearm the same blocked continuation family from inside the turn engine itself
- 2026-03-30 22:34 MDT - Engine-side repeated-continuation suppression was still blind to the production trigger-row shape.
  - fresh live evidence:
    - repeated PM continuation user rows `11900` and `11904` in session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` had `turn_id=NULL`
    - the terminal blocked turns did exist, but only through `chat_turn.trigger_message_id`
    - the turn engine therefore kept missing the prior blocked stop and appended another focused replacement-parent continuation
  - bug:
    - [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) was resolving prior continuation terminal turns through `message.turn_id`
    - that works in some unit fixtures, but not in the live schema for user trigger messages
  - impact:
    - repeated focused replacement-parent continuation families can survive both worker cleanup and engine-side stop suppression because the engine fails to find the already-completed blocked turn in production
