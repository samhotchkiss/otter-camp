# 0327a Fix Log

- 2026-03-27 10:44:10 MDT - `158fdb5d` `Fix restart smoke coverage and kickoff retry churn`
  - tightened org-scope kickoff handling in [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so blocked duplicate `project.create` results stop the kickoff turn instead of paying for another model round
  - repaired stale restart-smoke integration coverage in [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go)
  - repaired stale kickoff smoke assertions in [`internal/controlplane/task_queue_processor_integration_test.go`](/Users/sam/dev/otter-camp/internal/controlplane/task_queue_processor_integration_test.go)
  - verified with the targeted integration tests and a full `./build/verify-restart-readiness.sh` PASS
- 2026-03-27 10:56:18 MDT - `24f98464` `Add direct TUI project archive commands`
  - added native `:archive-project` and `:archive-project all` support in the TUI via [`internal/tui/model.go`](/Users/sam/dev/otter-camp/internal/tui/model.go), [`internal/tui/runtime.go`](/Users/sam/dev/otter-camp/internal/tui/runtime.go), and [`cmd/ottercamp/tui.go`](/Users/sam/dev/otter-camp/cmd/ottercamp/tui.go)
  - updated the help view in [`internal/tui/view.go`](/Users/sam/dev/otter-camp/internal/tui/view.go)
  - added focused TUI coverage in [`internal/tui/model_test.go`](/Users/sam/dev/otter-camp/internal/tui/model_test.go)
  - verified with focused `internal/tui` tests plus `go test ./cmd/ottercamp -run '^$' -count=1`
- 2026-03-27 10:58:43 MDT - `9188f8a6` `Refresh dashboard after TUI project archive`
  - updated [`internal/tui/model.go`](/Users/sam/dev/otter-camp/internal/tui/model.go) so project-archive completion reloads the operator dashboard in addition to the sidebar
  - verified with focused `internal/tui` tests after the change
- 2026-03-27 11:02:56 MDT - `991be6f6` `Retry TUI cleanup commands on rate limits`
  - added short TUI-side retries for command-path local API 429s in [`cmd/ottercamp/tui.go`](/Users/sam/dev/otter-camp/cmd/ottercamp/tui.go)
  - covered the retry helper in [`cmd/ottercamp/tui_retry_test.go`](/Users/sam/dev/otter-camp/cmd/ottercamp/tui_retry_test.go)
  - verified with focused `cmd/ottercamp` retry tests plus the existing focused `internal/tui` command tests
- 2026-03-27 11:11:52 MDT - `8d70c21f` `Ignore archived projects in blocked dashboard counts`
  - filtered paused-project blocked counts/items to active projects only in [`internal/server/controlplane_dashboard.go`](/Users/sam/dev/otter-camp/internal/server/controlplane_dashboard.go)
  - filtered human-input blocked counts/items to active-project-linked inbox rows only while preserving org/global inbox items in the same file
  - added archived-project regression coverage in [`internal/server/controlplane_integration_test.go`](/Users/sam/dev/otter-camp/internal/server/controlplane_integration_test.go)
  - verified with focused `go test -tags=integration ./internal/server -run 'TestOperatorDashboard(IgnoresArchivedProjectTasksInBlockedCounts|IgnoresArchivedProjectsInPausedAndHumanInputBlockedCounts)$' -count=1`, then rebuilt/restarted and confirmed the live `oc-test` dashboard no longer shows the stale blocked backlog on a zero-active-work org
- 2026-03-27 11:44:10 MDT - `2de05532` `Tighten bootstrap checklist restart recovery`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so checklist-only restart detection keys off persisted bootstrap phase/checkpoint state instead of brittle resume-message text, while also preserving the recoverable bootstrap invalid-transition race bypass
  - tightened the bootstrap reread guard and staffing-discovery budget so fresh restart turns block broad rediscovery (`project.list`, `task.list`, `project.get`, `file.list`, `file.read`, `file.search`, `flow.list_templates`) and steer directly into staffing / `bootstrap.setup.persist`
  - added focused coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), including the live production shape where `initialMessageText` is empty
  - verified with `go test ./internal/turn -run 'Test(HandleProjectBootstrapUnhandledFailureSkipsRecoverableInvalidTransitionRace|ShouldBlockProjectBootstrapExcessStaffingDiscoveryTool|ShouldBlockProjectBootstrapRecoveryRereadToolBlocksChecklistOnlyRestartRediscovery|ShouldBlockProjectBootstrapRecoveryRereadToolBlocksChecklistOnlyRestartWithoutResumeText|ShouldBlockProjectBootstrapRecoveryRereadToolBlocksBroadRereads)$' -count=1`
  - rebuilt/restarted the runtime and live-proved the change on project session `fc60f57c-8154-431a-8b5c-23231e27e0fb`: blocked reread tool results fired at `11:44:10 MDT`, then the next continuations advanced through `bootstrap.setup.persist` for `staff-project`, `decompose-workstreams`, `validate-task-shape`, and `attach-validate-flow-templates` instead of archiving and restarting the project again
- 2026-03-27 12:17:44 MDT - `ed18a178` `Harden bootstrap setup structural persistence`
  - reworked [`internal/tools/native/mutation_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/mutation_tools.go) so `bootstrap.setup.persist` validates each requested checklist step against durable project state instead of trusting agent narration
  - added structural gating for repo binding, non-starter assignment presence, bounded task materialization, task-shape readiness, flow-template coverage, explicit/runnable first-wave selection, and required sign-off summary
  - delayed first-wave metadata persistence until the `select-first-wave` step itself is actually valid, preventing premature `bootstrap_first_wave_selected` writes
  - expanded regression coverage in [`internal/tools/native/native_integration_test.go`](/Users/sam/dev/otter-camp/internal/tools/native/native_integration_test.go) for blocked over-persistence, missing-flow/sign-off gating, explicit first-wave selection, alias expansion, and invalid first-wave choices
  - verified with the focused bootstrap integration slice and a broader `go test ./internal/tools/native -count=1` pass before rebuilding and restarting the runtime
- 2026-03-27 12:38:13 MDT - `e848b0d9` `Harden bootstrap staffing materialization`
  - finished the role-aware `bootstrap.setup.persist` staffing gate in [`internal/tools/native/mutation_tools.go`](/Users/sam/dev/otter-camp/internal/tools/native/mutation_tools.go) so `staff-project` and `record-frank-sign-off` require an active non-starter `project_manager`, `worker`, and `reviewer` roster instead of any single assignment
  - extended the persisted bootstrap-state payload with role-specific counts and missing-role messaging in the same file
  - repaired the affected fixtures and added partial-staffing coverage in [`internal/tools/native/native_integration_test.go`](/Users/sam/dev/otter-camp/internal/tools/native/native_integration_test.go)
  - verified with `go test -tags=integration ./internal/tools/native -run 'TestIntegrationBootstrapSetupPersist(CompletesRequestedSetupSteps|BlocksUnmaterializedChecklistAfterRepoBinding|BlocksPartialStaffingRoster|BlocksFlowAndSignoffOvertrust|RequiresExplicitFirstWaveSelectionAndPersistsIt|AcceptsNaturalStepAliases|AcceptsExpandedNaturalStepAliases|AcceptsFirstWaveTaskAliasExpansion)$' -count=1`, `go test ./internal/tools/native -count=1`, and the focused bootstrap staffing-discovery guard tests in `internal/turn`
- 2026-03-27 13:08:55 MDT - `d8bd09cf` `Require full bootstrap staffing checkpoints`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) and [`internal/turn/project_bootstrap_state.go`](/Users/sam/dev/otter-camp/internal/turn/project_bootstrap_state.go) so bootstrap success checkpoints now require a real PM/worker/reviewer assignment roster, while partial staffing drafts still count only as partial setup
  - added role-aware staffing counts to persisted bootstrap state, healed stale out-of-order checkpoint carry-forward for already-corrupted sessions, and tightened resume/scaffold guidance so partial-staffing turns stay focused on `agent.assign_project` rather than premature `bootstrap.setup.persist`
  - updated focused coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go), and [`internal/turn/project_bootstrap_state_test.go`](/Users/sam/dev/otter-camp/internal/turn/project_bootstrap_state_test.go)
  - verified with `go test ./internal/turn -run 'Test(BuildProjectBootstrapAutomaticFailureRecordUsesStaffingCheckpointForPersistedDrafts|ProjectBootstrapStateWithDerived.*|BuildProjectBootstrapRecoveryRereadToolGuardErrorScaffoldOnlyRecovery|ShouldBlockProjectBootstrapRecoveryRereadToolBlocksScaffoldOnlyRecoveryRereads|BuildProjectBootstrapResumeActionPromptForStaffingMaterializationStep|BuildProjectBootstrapResumeActionPromptRejectsChecklistOnlyCompletion|BuildProjectBootstrapResumeActionPromptStartsWithPersistAfterTaskTree|ShouldNotStopAfterBlockedProjectBootstrapRecoveryRereadScaffoldOnlyRecovery)$' -count=1` plus `go test -tags=integration ./internal/turn -run 'Test(TurnEngineIntegrationBootstrapProgressCountsStaffingDraftsWithoutAssignments|TurnEngineIntegrationProjectBootstrapFailsWhenReviewerAssignmentMissing|BootstrapInvariants/staffed_project_without_pm_fails_closed)$' -count=1`
- 2026-03-27 14:01:55 MDT - `842fce63` `Refresh stale bootstrap pause after setup persist`
  - extended the org-scope bootstrap repair hook in [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so successful `bootstrap.setup.persist` calls count as qualifying project mutations and trigger immediate project-session bootstrap refresh plus stale-pause invalidation
  - added focused integration coverage in [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go) for the live Sam.blog failure shape where an org-scoped `bootstrap.setup.persist` arrives after reviewer repair
  - verified with `go test ./internal/turn -run '^$' -count=1` and `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegrationRefreshesPausedBootstrapAfterOrgScoped(ReviewerRepair|BootstrapSetupPersist)$' -count=1`
  - rebuilt/restarted the watched runtime and live-proved the fix from `oc-test`: org turn `07bc2266-9967-4dbf-b4e1-521724092b81` called `bootstrap.setup.persist`, and project `a6dbd331-7205-42d9-b0df-10105d5b5330` immediately cleared its stale reviewer-missing pause instead of staying gated
- 2026-03-27 14:28:14 MDT - `789561c4` `Recover missing first-wave execution kickoffs`
  - widened [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so `RequeueActiveExecutionSessionsWithoutTurns(...)` can recover active execution sessions that never received a kickoff message, while still preserving the existing stale-run, cancelled-latest, halted-recovery, and generic-supervisor semantics
  - added a worker-side synthetic kickoff helper in the same file that creates an execution-bound `task_queue_processor` user message only when an active task execution truly has no usable kickoff (or only an already-consumed generic supervisor resume)
  - added focused regression coverage in [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go) for the no-message first-wave shape and reran the broader `RequeueActiveExecutionSessionsWithoutTurns` integration cluster
  - verified with `go test -tags=integration ./internal/jobqueue -run 'TestJobWorkerRequeueActiveExecutionSessionsWithoutTurns(ForTaskQueueKickoff|CreatesMissingTaskQueueKickoff)$' -count=1` and `go test -tags=integration ./internal/jobqueue -run 'TestJobWorkerRequeueActiveExecutionSessionsWithoutTurns' -count=1`
  - rebuilt/restarted the watched runtime, confirmed health was `ok`, and live-proved the fix on project `a6dbd331-7205-42d9-b0df-10105d5b5330`: first-wave task sessions that had been sitting at zero kickoff messages/zero jobs now show execution-bound `task_queue_processor` kickoffs and runnable `agent_turn` jobs
- 2026-03-27 14:57:04 MDT - `c284cb66` `Prefer execution-bound task recovery over supervisor resumes`
  - changed [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so active execution-bound task messages (`task_queue_processor`, `task_review_action`, `task_recovery_resume`) outrank a newer generic `supervisor recovery: resume task` across `PurgeStaleAgentTurnJobs`, `claimPendingAgentTurns`, and `RequeueActiveExecutionSessionsWithoutTurns`
  - taught the active-execution requeue path in the same file to treat a pending supervisor resume as supersedable when an older execution-owned kickoff for the same active execution already exists, and to dead-letter that stale supervisor dispatch before enqueuing a fresh execution-bound job
  - added focused regression coverage in [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go) for purge ordering, claim ordering, and the exact recovery shape where an older kickoff must replace a newer supervisor retry
  - verified with `go test -tags=integration ./internal/jobqueue -run 'TestJobWorker(RequeueActiveExecutionSessionsWithoutTurns(ForTaskQueueKickoff|CreatesMissingTaskQueueKickoff|PrefersExecutionKickoffOverNewerSupervisorRecovery|SkipsLogicallyCancelledLatestMessage)|PurgeStaleAgentTurnJobs(PrefersExecutionKickoffOverNewerSupervisorRecovery|KeepsSupervisorRecoveryJobForActiveExecutionWithoutTurn)|ClaimPendingAgentTurns(PrefersExecutionKickoffOverNewerSupervisorRecovery|PrefersNewestTaskSessionMessage|RequeuesStaleOrphanTaskExecutionDispatch))$' -count=1`, then rebuilt/restarted the watched runtime and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live Sam.blog evidence after deployment: task 27 (`c02673ac-645b-4d71-b5ec-ed2d7e5e609a`) moved off the stuck supervisor-only family, shows fresh execution-bound `task_queue_processor` wakeups at `2026-03-27 14:51:06 MDT`, and is now `done` with closed sessions
- 2026-03-27 15:07:29 MDT - `be146de0` `Claim oldest due task sessions first`
  - changed [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so ordinary cross-session `agent_turn` work is claimed oldest-due first, while project async continuations still keep newest-first ordering and same-session newest-message semantics still flow through `session_rn`
  - added [`TestJobWorkerClaimPendingAgentTurnsPrefersOldestDueTaskSessionAcrossSessions`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go) and repaired the fresh-org-over-project continuation fixture so the claim-order slice exercises a real unfinished project continuation
  - verified with `go test -tags=integration ./internal/jobqueue -run 'TestJobWorkerClaimPendingAgentTurns(PrefersOldestDueTaskSessionAcrossSessions|PrefersNewestTaskSessionMessage|PrefersExecutionKickoffOverNewerSupervisorRecovery|PrefersNewestProjectContinuation|PrioritizesFreshOrgWorkOverProjectContinuation)$' -count=1` and `go test -tags=integration ./internal/jobqueue -run 'TestJobWorker(ClaimPendingAgentTurns(PrefersOldestDueTaskSessionAcrossSessions|PrefersNewestTaskSessionMessage|PrefersExecutionKickoffOverNewerSupervisorRecovery|PrefersNewestProjectContinuation|PrioritizesFreshOrgWorkOverProjectContinuation|RequeuesStaleOrphanTaskExecutionDispatch)|RequeueActiveExecutionSessionsWithoutTurns(PrefersExecutionKickoffOverNewerSupervisorRecovery|CreatesMissingTaskQueueKickoff|ForTaskQueueKickoff)|PurgeStaleAgentTurnJobs(PrefersExecutionKickoffOverNewerSupervisorRecovery|KeepsSupervisorRecoveryJobForActiveExecutionWithoutTurn))$' -count=1`
  - rebuilt/restarted the watched runtime, confirmed health was `ok`, and live-proved the change on Sam.blog: task 18's `14:19:19 MDT` retry was claimed first at `15:07:01 MDT`, while newer task 23 work (`14:50:35 MDT`) correctly remained pending behind older due backlog
- 2026-03-27 15:30:12 MDT - `bf85ffa2` `Stop stale recovery resume rewrites`
  - changed [`internal/jobqueue/worker.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker.go) so `PurgeStaleAgentTurnJobs()` dead-letters pending `task_recovery_resume` dispatches whose trigger message already has a completed `file.write` with positive `byte_size`, and `RequeueActiveExecutionSessionsWithoutTurns()` skips recreating those consumed recovery-resume jobs
  - added focused coverage in [`internal/jobqueue/worker_integration_test.go`](/Users/sam/dev/otter-camp/internal/jobqueue/worker_integration_test.go) for both `TestJobWorkerRequeueActiveExecutionSessionsWithoutTurnsSkipsSuccessfulRecoveryResumeWrite` and `TestJobWorkerPurgeStaleAgentTurnJobsPurgesSuccessfulRecoveryResumeWrite`
  - verified with `go test -tags=integration ./internal/jobqueue -run 'TestJobWorker(RequeueActiveExecutionSessionsWithoutTurns(SkipsSuccessfulRecoveryResumeWrite|PrefersExecutionKickoffOverNewerSupervisorRecovery|CreatesMissingTaskQueueKickoff|ForTaskQueueKickoff)|PurgeStaleAgentTurnJobs(PurgesSuccessfulRecoveryResumeWrite|PrefersExecutionKickoffOverNewerSupervisorRecovery|KeepsSupervisorRecoveryJobForActiveExecutionWithoutTurn)|ClaimPendingAgentTurns(PrefersOldestDueTaskSessionAcrossSessions|PrefersExecutionKickoffOverNewerSupervisorRecovery|PrefersNewestProjectContinuation|PrioritizesFreshOrgWorkOverProjectContinuation))$' -count=1`, then rebuilt/restarted the watched runtime and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: by the time the new worker came up, the specific stale-successful recovery-resume rows from tasks 18/19 had already rolled forward to newer messages, so this slice is currently deployed and test-proven but waiting on the next fresh live instance of that exact recovery-resume family for direct production proof
- 2026-03-27 15:47:21 MDT - `ac297e3b` `Verify recovery writes against task worktrees`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so task-scoped recovery workspace resolution includes the task worktree before falling back to project compatibility roots, and recovery durability verification now uses that same recovery-root set instead of project roots alone
  - added [`TestTurnEngineIntegrationRecoveryTurnAcceptsTaskWorktreeWriteAsDurableEX323`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go), which initializes a real project repo, writes the recovered target only into the task worktree, and verifies the recovery checkpoint clears instead of falsely halting on “file not found”
  - verified with `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegrationRecoveryTurnAcceptsTaskWorktreeWriteAsDurableEX323$' -count=1` and `go test ./internal/turn -run 'TestHandleRecoveryPopulatedFileWriteOutcomePrefersDeliverablePathFromToolResult$' -count=1`
  - rebuilt/restarted tmux session `codex-e2e-20260324` on the new binary and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: immediate post-restart inspection found no active turns on Sam.blog tasks 18/19/23, so the exact HTML recovery path is deployed but waiting on the next recovery turn for direct production proof
- 2026-03-27 15:58:36 MDT - `3e00509e` `Skip supervisor resumes for blocked review turns`
  - changed [`internal/controlplane/supervisor.go`](/Users/sam/dev/otter-camp/internal/controlplane/supervisor.go) so `detectResumableBlockedTasks()` filters out blocked tasks whose validation guard already says `review_decision_required` or whose latest blocked reason starts with `review turn completed without calling flow.review_decision`, preventing supervisor from abandoning their active review execution and auto-reopening them
  - added [`TestSupervisor_ReviewDecisionBlockedTaskStaysBlocked`](/Users/sam/dev/otter-camp/internal/controlplane/supervisor_integration_test.go), which reproduces the live task-13 shape and proves ordinary validation blocks still auto-resume while blocked review-decision tasks remain blocked with their validation guard and active execution intact
  - verified with `go test -tags=integration ./internal/controlplane -run 'TestSupervisor_(ResumableBlockedTaskGetsAutoResumed|ReviewDecisionBlockedTaskStaysBlocked)$' -count=1`
  - rebuilt/restarted tmux session `codex-e2e-20260324` on the new binary and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: the task-13 leaked review turn was created before this deploy, so direct production proof for the supervisor suppression path now depends on the next blocked review cycle rather than the already-reopened pre-patch turn
- 2026-03-27 16:08:06 MDT - `79473bde` `Bound task empty cli.execute retries`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so ordinary async task turns treat an empty `cli.execute.command` as a synthetic validation failure before tier2 dispatch, letting the existing same-turn validation guard stop the second identical retry without spending malformed tool runs
  - updated [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go) so the non-recovery EX311 path now asserts two model calls, zero malformed tier2 dispatches, one correction message, and one same-turn validation-stop message
  - verified with `go test ./internal/turn -run 'TestHandleTaskCLIExecuteWithoutCommand(RewritesToFileWriteFromDraft|AppendsCorrectionWithoutHighConfidenceDraft)$' -count=1` and `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegrationNonRecoveryEmptyCLIExecuteStopsEarlyAfterCorrectionEX311$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: the post-restart one-hour token snapshot still shows older review/recovery blockers first, and the last `cli.execute command required` turn-stop in the report was at `2026-03-27 16:03:32 MDT`, before this deploy; fresh production proof for the cheaper empty-`cli.execute` path is waiting on the next natural task-session hit
  - note: older recovery EX311 integration expectations remain stale/red (`RecoveryTurnRepairsEmptyCLIExecuteBeforeDispatchEX311`, `RecoveryTurnBoundsRepeatedEmptyCLIExecuteWithoutReblockingEX311`) and were not part of this slice
- 2026-03-27 16:29:38 MDT - `017a10b9` `Normalize raw file.edit and bound missing-new_string retries`
  - changed [`internal/toolargs/file_write.go`](/Users/sam/dev/otter-camp/internal/toolargs/file_write.go) so `toolargs.Normalize(...)` now recovers `_raw` `file.edit` arguments, including malformed JSON-like envelopes containing `path`, `old_string`, and `new_string` / `replacement`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so non-recovery task turns stop pathful `file.edit` / `file_edit` calls that omit `new_string` before dispatch, using the same bounded same-turn validation pattern as the empty-`cli.execute` hardening
  - also changed the same file so successful recovered `_raw` calls are no longer classified as `malformed _raw arguments` validation failures purely because the original call envelope contained `_raw`
  - added coverage in [`internal/toolargs/file_write_test.go`](/Users/sam/dev/otter-camp/internal/toolargs/file_write_test.go), [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), and [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go)
  - verified with `go test ./internal/toolargs -run 'Test(NormalizeFileEditRecoversMalformedRawArguments|NormalizeFileWrite.*|File(Edit|Write|Read|List)AttemptFingerprint.*|CanonicalToolName.*)$' -count=1`, `go test ./internal/turn -run 'Test(ClassifyToolValidationFailureIgnoresSuccessfulRecoveredRawArguments|HandleTaskCLIExecuteWithoutCommand(RewritesToFileWriteFromDraft|AppendsCorrectionWithoutHighConfidenceDraft))$' -count=1`, and `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegration(NonRecoveryEmptyCLIExecuteStopsEarlyAfterCorrectionEX311|NonRecoveryFileEditWithoutNewStringStopsEarlyAfterCorrectionEX324)$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: the earlier task-21 `malformed _raw arguments` stop has not reappeared on the new binary; the next visible blocker in that lane is a separate tool-routing/content-drop defect that was logged independently
- 2026-03-27 16:46:12 MDT - `248d9ba7` `Recognize file_write aliases and reject tool-routing drafts`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so task/recovery file-write repair helpers and the task execution synthesis guard recognize the live `file_write` alias alongside `file.write`
  - changed the same file so tool-routing/operator-repair troubleshooting prose like “The tool infrastructure is routing all tool calls…” is rejected as tool-recovery troubleshooting instead of being reused as a substantive draft body
  - added focused coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for the alias path plus the stale recovery-artifact narration case
  - verified with `go test ./internal/turn -run 'Test(HandleTaskFileWriteWithoutContent(PrefersRecoveryArtifactDraftOverNarration|PrefersPriorSubstantiveDraftOverIntentReply|AppendsCorrectionWhenNoDraftExists|AcceptsFileWriteAlias|RejectsToolRoutingArtifactDraft)|RecoveryFileWriteDraftRejectReason|ClassifyToolValidationFailureIgnoresSuccessfulRecoveredRawArguments|HandleTaskCLIExecuteWithoutCommand(RewritesToFileWriteFromDraft|AppendsCorrectionWithoutHighConfidenceDraft))$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - note: `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegrationFileWriteMissingContentRetryStillBlocksTrulyInvalidCalls$' -count=1` remains red on this branch, but that fixture exercises older broader file-write retry behavior and appears to be separate existing test debt rather than a regression introduced by this alias/troubleshooting hardening
  - live note: no fresh post-restart task-21 turn has executed yet, so production proof for the stale-tool-routing-draft rejection is still waiting on the next natural HTML-task retry
- 2026-03-27 16:51:50 MDT - `ebff2033` `Continue content migration turns after checkpoints`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so content-migration checkpoints no longer short-circuit continuation setup before the normal task continuation summary/action prompt is appended
  - strengthened [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go) so the content-migration continuation prompt must include both the checkpoint state and the direct task-action / anti-generic-chat instructions
  - verified with `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegration(ContentMigrationContinuationUsesWorkspaceCheckpoint|ContentMigrationResumeUsesPersistedCheckpointState)$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: task 10 session `8048c518-b8e2-4e95-8e54-2fa763325b31` stopped emitting the generic checkpoint continuation reply and resumed direct task action on the next post-restart continuation turn
- 2026-03-27 16:59:44 MDT - `9c4edfe5` `Guide empty migration branches to upstream inputs`
  - changed [`internal/taskcheckpoint/content_migration.go`](/Users/sam/dev/otter-camp/internal/taskcheckpoint/content_migration.go) so empty content-migration checkpoints no longer return nil immediate-next-action guidance; zero-artifact / zero-script / zero-output task branches now get explicit instructions to stop re-listing the empty workspace, consume upstream manifest/index/source deliverables, and write the first artifact or output
  - added [`TestContentMigrationCheckpointGuidanceHandlesEmptyWorkspace`](/Users/sam/dev/otter-camp/internal/taskcheckpoint/content_migration_test.go) and kept the existing content-migration continuation integration slice green so the new guidance is covered both at the helper layer and through continuation prompt construction
  - verified with `go test ./internal/taskcheckpoint -count=1` and `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegration(ContentMigrationContinuationUsesWorkspaceCheckpoint|ContentMigrationResumeUsesPersistedCheckpointState)$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live proof: task 11 session `1aea2d41-cdfe-4df5-ac17-897c006e702f` logged the new empty-workspace checkpoint message at `16:59:01 MDT`, including the upstream-manifest/index directive, and immediately pivoted into the archive/index scraping path instead of staying in empty-branch rediscovery
- 2026-03-27 17:07:55 MDT - `0730824e` `Block active-only project continuation rediscovery`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so project continuation prompts now explicitly tell active-task-only PM turns not to begin with `session.list`, broad `task.list`, root `file.list`, `git.log`, or `git.status` when the prompt already names the active tasks
  - widened the existing project-continuation snapshot rediscovery guard in the same file so it activates when the prompt names either active tasks or actionable draft tasks, and now blocks `session.list`, `session.history`, `inbox.list`, `git.log`, `git.status`, root `file.list`, broad `task.list`, and `task.get` on already-named task ids in that lane
  - added focused coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for the new active-only guard paths plus the updated prompt wording
  - verified with `go test ./internal/turn -run 'Test(ShouldBlockProjectContinuationSnapshotRediscoveryTool.*|BuildProjectContinuationActionPrompt|BuildProjectExecutionContinuationPrompt)$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: the guard is deployed on the hot PM session shape, but the first fresh post-restart project continuation turn has not executed yet, so production proof is pending the next natural PM wakeup
- 2026-03-27 17:15:32 MDT - `c1975e83` `Retry generic synthetic recovery resumes`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so `isRecoveryResumeMessage(...)` now treats `metadata.source=task_recovery_resume` as a real recovery-resume trigger alongside explicit `recovery_action` metadata and supervisor resume text, allowing the existing generic-recovery-reply retry/block logic to fire for the engine's own synthetic continuation prompts
  - added [`TestHandleTurnCompletedEventRetriesGenericSyntheticRecoveryResumeReply`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), which reproduces the live task-11 shape and asserts that a generic assistant reply after a synthetic recovery-resume prompt enqueues a bounded retry with a fresh retry prompt message
  - verified with `go test ./internal/turn -run 'TestHandleTurnCompletedEventRetriesGeneric(RecoveryReply|SyntheticRecoveryResumeReply)$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live note: post-deploy task 11 session `1aea2d41-cdfe-4df5-ac17-897c006e702f` resumed concrete recovery work on the next synthetic recovery-resume continuation and wrote `content/technonymous/stop-preparing-your-kids-for-jobs.md` at `17:12:26 MDT`; the exact generic-reply retry branch is still waiting on the next natural generic response for direct production proof
- 2026-03-27 17:38:33 MDT - `693e3fe5` `Preserve specific recovery correction checkpoints`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so the first recovery correction for empty `cli.execute` or empty `file.write` persists a specific checkpoint failure reason when the task already has a concrete recovery target, repeated recovery-focus stops preserve or escalate that specific missing-command / missing-content reason instead of overwriting it with generic read-only churn, and successful `cli.execute` / `file.edit` writes now clear the recovery checkpoint the same way successful `file.write` already did
  - added focused coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for first-correction checkpoint persistence and `cli.execute` checkpoint clearing, and widened [`internal/turn/engine_integration_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_integration_test.go) so the successful recovered `cli.execute` file-output path now asserts the checkpoint is actually cleared
  - verified with `go test ./internal/turn -run 'Test(HandleRecoveryCLIExecuteWithoutCommand(PersistsCheckpointOnFirstCorrection|StopsAfterRepeatedResumeFailure)|MaybeClearRecoveryFileWriteCheckpointClearsOnCLIExecuteWrite)$' -count=1` and `go test -tags=integration ./internal/turn -run 'TestTurnEngineIntegrationRecoversMalformedCLIExecuteRawArgsForFileOutputEX307$' -count=1`
  - note: while checking adjacent recovery fixtures, `TestTurnEngineIntegrationRecoveryTurnPersistsCheckpointForRepeatedEmptyCLIExecuteWithFileContextEX321` and `TestTurnEngineIntegrationRecoveryTurnRewritesEmptyCLIExecuteToPersistedFileWrite` remained red on older expectations because the runtime now rewrites those malformed recovery turns into persisted-draft `file.write` work; I kept that fixture drift separate from this checkpoint-preservation slice
- 2026-03-27 17:51:29 MDT - `pending` `Redirect review retries away from missing planning artifacts`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so `reviewApprovalRetryPrompt(...)` now recognizes review turns that already proved deliverables exist via current `file.list` / `file.read` evidence but then wasted the turn on missing `planning/` companion files; the next retry prompt now keeps the review on the current deliverables, cites the missing planning paths, bans rereading `planning/`, `task.get`, `git.log`, and broad workspace discovery, and directs the turn back to bounded deliverable inspection plus `flow.review_decision`
  - added focused unit coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for the Sam.blog task-10 shape where `content/posts` is present, `planning/prd-spec/oc-34-*.md` reads are missing, and the retry prompt must redirect the review back to real deliverables instead of planning-artifact drift
  - verified with `go test ./internal/turn -run 'Test(BuildTaskReviewActionPrompt.*|ReviewApprovalRetryPrompt(RejectsMissingPreferredDeliverable|RedirectsMissingSecondaryPlanningArtifactsToDeliverables|CarriesForwardOrchestrationParentSummaryEvidence|RejectsWhenRequiredTestsCannotBeVerified|RejectsWhenRequiredTestsCannotBeVerifiedViaRecoveryFocus|RejectsWhenMissingTestsGuardAlreadyFiredThisTurn)|HandleTurnCompletedEventRetriesGeneric(SyntheticRecoveryResumeReply|RecoveryReply))$' -count=1`
- 2026-03-27 18:02:20 MDT - `b108bc7b` `Separate active-child draft parents from actionable project continuations`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so project continuation snapshots now compute direct child-task activity, exclude draft parents with active child execution from the actionable-draft count, and place them into a separate `Draft parent tasks already have active child work:` snapshot line instead of presenting them as runnable PM work
  - changed the same file so actionable draft task refs now carry explicit prerequisite blockers such as `assigned_agent_id=missing`, `flow_template_id=missing`, and `requires_human_review=true`, and the continuation prompt now tells the PM lane to repair those exact prerequisites before trying to queue a draft task
  - added focused coverage in [`internal/turn/engine_test.go`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go) for the live Sam.blog shape where a draft parent has two active child tasks and must move out of the actionable-draft line, while a genuinely actionable draft keeps its missing-prerequisite markers
  - verified with `go test ./internal/turn -run 'Test(ProjectExecutionContinuationSnapshotSummarizesProjectState|BuildProjectExecutionContinuationPrompt|ShouldBlockProjectContinuationSnapshotRediscoveryTool(BlocksBroadTaskList|BlocksSessionListForActiveTasks|BlocksGitLogForActiveTasks|BlocksRootFileListForActiveTasks|BlocksNamedTaskGet|BlocksNamedActiveTaskGet|BlocksFlowGetExecution|BlocksBroadResultsList|NotBlockProjectContinuationSnapshotRediscoveryToolForParentScopedTaskList)|BuildProjectContinuationActionPrompt)$' -count=1`
  - live note: the patch is aimed directly at Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, where parent draft tasks `33` and `34` were still being surfaced as actionable even though child tasks `37/38` and `35/36` were already active; fresh production proof now depends on the next post-restart PM continuation turn
- 2026-03-27 18:12:32 MDT - `3c00153d` `Root continuation turns on the synthetic action prompt`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so `appendContinuationSummaryAndAction(...)` now persists `historyStartID` / `trigger_message_id` to the appended synthetic continuation action prompt when one exists, instead of always rooting the next turn on the summary system message
  - this makes project/task/organization continuation guards operate on the real synthetic user prompt content that contains the actionable snapshot lines and anti-rediscovery instructions
  - added [`TestAppendContinuationSummaryAndActionRootsProjectContinuationAtSyntheticUserPrompt`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), which reproduces the live Sam.blog PM shape and asserts the continuation root becomes the synthetic `project_continuation_resume` prompt carrying the child-active draft guidance
  - verified with `go test ./internal/turn -run 'Test(AppendContinuationSummaryAndActionRootsProjectContinuationAtSyntheticUserPrompt|ProjectExecutionContinuationSnapshotSummarizesProjectState|BuildProjectExecutionContinuationPrompt|ShouldBlockProjectContinuationSnapshotRediscoveryTool(BlocksBroadTaskList|BlocksSessionListForActiveTasks|BlocksGitLogForActiveTasks|BlocksRootFileListForActiveTasks|BlocksNamedTaskGet|BlocksNamedActiveTaskGet|BlocksFlowGetExecution|BlocksBroadResultsList|NotBlockProjectContinuationSnapshotRediscoveryToolForParentScopedTaskList)|BuildProjectContinuationActionPrompt)$' -count=1`
  - live note: this directly addressed Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8`, where the synthetic prompt already contained the new guard lines but the runtime still rooted the next turn on `[Continuation summary] ...`; the follow-on same-turn handoff gap was logged separately below once live proof showed the current turn still held the older `initialMessageText`
- 2026-03-27 18:25:38 MDT - `pending` `Apply synthetic continuation prompts to same-turn guards`
  - changed [`internal/turn/engine.go`](/Users/sam/dev/otter-camp/internal/turn/engine.go) so every synthetic continuation action prompt appended by `appendContinuationSummaryAndAction(...)` now immediately becomes the live turn runtime’s effective initial prompt (`rt.initialMessageID` / `rt.initialMessageText`) for the rest of that same turn, not just the persisted history root
  - added same-turn guard assertions to [`TestAppendContinuationSummaryAndActionRootsProjectContinuationAtSyntheticUserPrompt`](/Users/sam/dev/otter-camp/internal/turn/engine_test.go), proving the appended project continuation prompt now blocks `session.list` and named active-task `task.get` during the current turn instead of only affecting future history
  - verified with `go test ./internal/turn -run 'Test(AppendContinuationSummaryAndActionRootsProjectContinuationAtSyntheticUserPrompt|ProjectExecutionContinuationSnapshotSummarizesProjectState|BuildProjectExecutionContinuationPrompt|ShouldBlockProjectContinuationSnapshotRediscoveryTool(BlocksBroadTaskList|BlocksSessionListForActiveTasks|BlocksGitLogForActiveTasks|BlocksRootFileListForActiveTasks|BlocksNamedTaskGet|BlocksNamedActiveTaskGet|BlocksFlowGetExecution|BlocksBroadResultsList|NotBlockProjectContinuationSnapshotRediscoveryToolForParentScopedTaskList)|BuildProjectContinuationActionPrompt)$' -count=1`
  - rebuilt `./bin/ottercamp`, restarted tmux `codex-e2e-20260324`, and confirmed `./bin/ottercamp health --output json` returned `status=ok`
  - live proof: Sam.blog PM session `5383ab5a-fecd-4a22-a403-d1e5620b96b8` immediately stopped rereading named active tasks and broad session/task state on the new binary; turn `1cda0a9d-649c-4e68-bde4-41bfa252e765` blocked named active-task `task.get` plus broad `task.list`, and the next turn `b6f310c5-0a20-4778-a560-c9e6c7ba4db3` blocked `session.list`, `inbox.list`, root `file.list`, and another broad `task.list` in the same PM lane
